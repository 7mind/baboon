name: dt_async_service
description: Baboon Dart async petstore service (GREEN regression guard for D25)
version: 0.0.1

environment:
  sdk: ^3.0.0

dependencies:
  baboon_runtime:
    path: packages/baboon_runtime
