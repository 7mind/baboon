model diff.ref.fixture

version "2.0.0"

import "1.0.0" { * }

// v2 adds a new DTO (AddedRecord) — a derivable change.
// The test-diff-ref lane asserts that AddedRecord appears in the diff output.
root data AddedRecord {
  tag: str
  count: i32
}
