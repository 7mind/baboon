# Investigate session log — D1 / H2 (Scala backend audit)

- Defect: D1 (generated identifiers collide with target-language reserved words)
- Hypothesis: H2 (defect class affects the other 8 backends)
- Role: explorer
- Returned lean: supports

### Session summary
- **Did:** gathered read-only evidence for H2 scoped to the SCALA backend (`translator/scl/`).
- **Achieved:** 9 numbered evidence items; lean supports.
- **Discovered:** Scala backend derives every target identifier from model names via `.capitalize` (type names, `ScTypeTranslator:123`), `.toLowerCase` (package/namespace segments + ADT-branch codec locals, `ScTypeTranslator:80/129/130`, `ScJsonCodecGenerator:201`), or verbatim (DTO field names `ScDefnTranslator:307`, contract methods `:420`, service methods `:457`, ADT branch case-capture `ScJsonCodecGenerator:211`). NO backtick-quoting / keyword / reserved / sanitize machinery exists anywhere — confirmed at the single terminal renderer `ScBaboonTranslator:283-290` which emits `name`/`inObject` raw. Exact H1 analog: `ScJsonCodecGenerator.scala:201` `branchName.toLowerCase` → used as pattern-capture binding at `:211` (`case $branchNameRef: $fqBranch =>`). Scala keywords don't include `default`, but `type`/`val`/`object`/`class`/`match` etc. trigger the same defect. Native fix: backtick-quoting `` `name` ``.
- **Issues:** static-only confirmation; raised a probeRequest for a scalac reproduction (orchestrator reproduced the C# analog directly instead).
