# Implement-flow session log

- **Task:** T11 — C# MCP local round-trip overlay + test-gen-cs-mcp/test-cs-mcp actions
- **Milestone:** M3
- **Role:** implement-worker (round 0)
- **Agent id:** a9d1082641c528afb
- **Returned status:** `pass` — resultCommit `8bdcba2b` (base 001b0e5e), branch `worktree-agent-a9d1082641c528afb`
- **Files touched:** .mdl/defs/tests.md, test/cs-stub-mcp-overlay/McpTests/McpTests.cs, test/cs-stub-mcp-overlay/McpTests/McpTests.csproj

---

### Session summary
- **Did:** implemented task T11 in worktree `worktree-agent-a9d1082641c528afb` (branch reset to `001b0e5e` T10 base)
- **Achieved:** pass; resultCommit `8bdcba2b26da28b3c5d48ed6849df58b0460ed89`; `dotnet test` green — 16 tests passed, 0 failed
- **Discovered:**
  - The worktree's `.git` file prevents sbt project loading; codegen was run via `sbt baboonJVM/runMain` from the main repo checkout.
  - NJsonSchema doesn't support Draft 2020-12 `$defs` keyword for `$ref` resolution — only `definitions` (Draft 4-7); workaround: normalize `"$defs":` → `"definitions":` and `#/$defs/` → `#/definitions/` in the schema string before NJsonSchema parsing. (The emitted schema is correct Draft 2020-12 — ajv validated it natively in T9; this is purely an NJsonSchema-version limitation on the test side.)
  - JsonSchema.Net 8.0.0 has a `$ref` resolution bug with prefix-overlapping `$defs` keys → avoided by using NJsonSchema.
  - The overlay uses a standalone `McpTests/` project (not shared `BaboonTests/`) because codegen only covers the mcp-stub-ok model.
  - Channel-B trigger for C#: `ping` with missing `seqno` → `asObject["seqno"]!.Value<int>()` throws NullReferenceException → caught as `BaboonWiringError.DecoderFailed`.
- **Issues:** none

**Deliverable:** `test/cs-stub-mcp-overlay/` standalone McpTests NUnit project driving generated `McpToolsMcpServer<object?>` through the full T7 scenario: initialize, tools/list (16 tests, NJsonSchema K1 validation, enum + required-field negative controls), tools/call success (ping, submitComposite), error paths (Channel-A -32602 + Channel-B isError=true). `test-gen-cs-mcp` + `test-cs-mcp` mdl actions; `test-cs-mcp` registered in the `:test` aggregator.
