# Implement-flow session log

- **Task:** T8 — Implement the TypeScript MCP server generator + runtime
- **Milestone:** M3
- **Role:** implement-worker (round 0)
- **Agent id:** a1215c23d32f156c1
- **Returned status:** `pass` — resultCommit `346ff334` (base b548d682), branch `worktree-agent-a1215c23d32f156c1`
- **Files touched:** TsMcpServerGenerator.scala, baboon-runtime/typescript/BaboonMcpRuntime.ts, .jvm test TypeScriptMcpServerEmissionTest.scala, BaboonModule.scala, BaboonRuntimeResources.scala

---

### Session summary
- **Did:** implemented task T8 (TypeScript MCP server generator + runtime) in worktree branch `worktree-agent-a1215c23d32f156c1`, reset onto base `b548d682`.
- **Achieved:** `status=pass`; resultCommit `346ff3345237203ec6168594e3c9474f8990b94a`. `sbt +compile` green (JVM + Scala.js); new JVM test `TypeScriptMcpServerEmissionTest` green; **tsc actually ran** (tsc 5.9.3 from nix store, `--noEmit`, strict, bundler resolution) on generated output → exit 0; generated inputSchemas match the T7 reference exactly; flag-off byte-identity confirmed.
- **Discovered:** (1) The TS generator has **pre-existing import-ordering nondeterminism** — two identical flag-off runs differ — so byte-identity must be judged modulo line order; the only flag-driven additions are `BaboonMcpRuntime.ts` + `mcp-server.ts`. (2) Foreign-codec error strings embed the `--output` dir name (artifact of differing output dirs, not the flag). (3) `OasTypeTranslator` was only bound in the OAS DI module; added a binding in the TS module so the T5 emitter can be constructed. (4) The nix `tsc` wrapper is a shell script — invoke `lib/node_modules/typescript/bin/tsc` via node directly.
- **Issues:** none blocking. **Design note (REPLICAS MUST MIRROR):** the generated `<Service>McpServer<Ctx>` takes an injected `invokeJson` delegate of shape `(BaboonMethodId, string, Ctx, BaboonCodecContext) => BaboonEither<BaboonWiringError,string>` (the errors-mode `dispatchJson` shape) rather than a raw `JsonMuxerCtx<Ctx,String>` — keeps `handle` synchronous/transport-abstract and supports the default errors mode the T9 round-trip (Channel-B `DecoderFailed`) requires. T10/C# and T12–T18 replicas should mirror this delegate shape. tsc was run on generated code directly (ts-stub `node_modules` absent in sandbox), using an equivalent tsconfig copied from `test/ts-stub/tsconfig.json`.

**Generated shape:** per-service `<Service>McpServer<Ctx>` with declaration-ordered tool registry (`<service>_<method>` names) + inputSchema from the shared T5 emitter, backed by static `BaboonMcpRuntime.ts` (JsonRpc types, McpSession, AbstractBaboonMcpServer `handle()` state machine, NO I/O loop); tools/call routes through the injected JSON delegate mapping `BaboonEither<BaboonWiringError,string>` → MCP Channel-A/B.
