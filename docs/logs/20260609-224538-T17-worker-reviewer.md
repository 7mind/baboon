# T17 (D4 Kotlin wiring escape) — worker + reviewer

## Worker (sonnet, agent a6f8d15ead53722d8) — pass, resultCommit bf91f9da (merged e33a1b43)
Routed all 12 `impl.${m.name.name}(...)` invocation receivers in KtServiceWiringTranslator through `KtTypeTranslator.escapeKtKeyword` (4 methods: noErrors/errors × json/ueba). String-literal when-branch keys (451/493/720/819) + transport args (867/884) left raw (on-wire method names). sbt baboonJVM/test 602 green; byte-identical existing fixtures. Noted latent: client-stub declarations (864/882) unescaped (→ filed D6).

## Reviewer (opus, agent abc6b9d4d69798644) — approve, 0 criticisms; filed 1 out-of-scope defect (D6)
Verified (operationally ran baboonJVM/test in a clean clone = 602/0): parent had 12 bare receivers, commit converted all 12, 0 bare remain; wire-name string literals correctly left raw; escapeKtKeyword identity for non-keywords → byte-identical. Client-stub declaration escape out of scope → D6 (low).
