# Implement-flow session log

- **Task:** T9 — TypeScript MCP local round-trip overlay + actions
- **Milestone:** M3
- **Role:** implement-reviewer (round 0)
- **Agent id:** a916dee3c59c87ede
- **Verdict:** `approve` — 0 criticisms, 0 questions, 0 defects

---

### Session summary
- **Did:** reviewed task T9 round 0 (diff 346ff334..58464250 — `test/ts-stub-mcp-overlay/{package.json,src/mcp.test.ts}`, `.mdl/defs/tests.md`)
- **Achieved:** verdict approve; 0 criticisms / 0 questions / 0 out-of-scope defects
- **Discovered:** Independently reproduced — codegen with `--ts-generate-mcp-server=true` into the overlay, `npm install`, `npx vitest run` → 16/16 green. Test drives the GENERATED `McpToolsMcpServer.handle()` (inherited `AbstractBaboonMcpServer` dispatch; `invokeJson_McpTools` injected as fake transport), not a reimplementation. All 8 T7 scenario steps covered. AJV Draft-2020-12 `ajv.compile()` runs on every inputSchema; confirmed `compile()` throws on a dangling `$ref`, so the gate is non-vacuous. Negative controls verified LIVE by flipping each assertion; restored to 16/16. `test-gen-ts-mcp` mirrors the `test-gen-ts-wiring-either` action verbatim + the MCP flag; `test-ts-mcp` registered in the `:test` aggregator.
- **Issues:** Two non-blocking observations: (1) §4.2 Channel-B trigger uses `nested:null` on `submitComposite` rather than T7's literal "missing seqno on ping" — documented sound adaptation to TS codecs not throwing on missing scalars; preserves §4.2 intent. (2) the worker correctly used the wiring-either flags (errors-mode + BaboonEither), the right set for an MCP test (regular-adt's no-errors=true would not produce the errors-mode wiring the MCP server needs).
