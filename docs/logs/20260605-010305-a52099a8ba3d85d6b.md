# Implement-flow session log

- **Task:** T17 — Swift MCP server generator + overlay + test-gen-swift-mcp/test-swift-mcp
- **Milestone:** M4
- **Role:** implement-worker (round 0)
- **Agent id:** a52099a8ba3d85d6b
- **Returned status:** `fail` — resultCommit `98e76227` (on worktree branch), blocked by D8
- **Files touched:** SwMcpServerGenerator.scala, baboon-runtime/swift/baboon_mcp_runtime.swift, BaboonModule.scala, .jvm SwiftMcpServerEmissionTest.scala, test/swift-stub-mcp-overlay/{Package.swift,Tests/McpTests/McpTests.swift}, .mdl/defs/tests.md

---

### Session summary
- **Did:** implemented task T17 (Swift MCP server generator + runtime + DI wiring + overlay + actions + JVM shape test) in worktree branch `implement/T17`.
- **Achieved:** `status=fail`; resultCommit `98e76227fe931f2b48f5dc705bb4b18a83c8bc1b`. Green: `sbt +compile` (JVM+JS), `SwiftMcpServerEmissionTest`, codegen output (5 inputSchema literals byte-equal to embedded T7 §2.3 refs), flag-off byte-identity (by construction). **Blocked:** `swift build`/`swift test` of `test-swift-mcp`.
- **Discovered:** (1) real CLI flag is `--sw-generate-mcp-server` (not `--swift-...`); already wired. (2) **The mcp-stub-ok model's recursive `Tree` DTO (`left: opt[Tree]`) does NOT compile under Swift**: `SwDefnTranslator` emits recursive value-type structs without indirection → `error: value type 'Tree' cannot have a stored property that recursively contains it` / `value type 'mcptools.processshape.in' has infinite size`. The other 8 language replicas tolerate this model. Pre-existing Swift recursive-value-type-DTO codegen gap, outside T17's MCP scope → filed as D8. (3) Swift `!` force-unwrap of a missing key TRAPS (non-catchable); the overlay triggers Channel-B via a non-object nested field instead.
- **Issues:** **CRITICAL — the worker damaged the MAIN checkout**: its early Bash calls ran in the MAIN checkout (not its worktree) and a `git reset --hard` there moved main off the merged work. **Orchestrator repaired main to `9eea5a27` via reflog (verified: 8 generators present, T17 commit reachable, 87 logs intact); the ledger MCP store was unaffected.** The MCP deliverable itself is complete and its own gates are green; the blocker is the separate Swift recursive-DTO codegen gap (D8).
