# Investigate explorer session — D8 / H1

- Defect: **D8** — Swift codegen: recursive value-type DTOs (`Tree { left: opt[Tree] }`) do not compile (BLOCKS T17)
- Hypothesis: **H1** — SwDefnTranslator emits recursive Baboon value-type DTOs as Swift `struct`s without indirection, producing infinite-size value types rejected by swiftc
- Role: explorer (read-only)
- Returned lean: **confirmed**
- Agent id: aca6d3712dc5065b7

## Verbatim summary

### Findings

1. `SwDefnTranslator.scala:409-418` — DTOs emitted as Swift value-type `public struct ... : Equatable, Hashable, $conformanceClause`. CONFIRMS — value type, no indirection at the type-declaration level.
2. `SwDefnTranslator.scala:350-355` — each DTO field becomes a bare stored property `public let $escaped: $t` from `asSwRef`. CONFIRMS — self-referential field emitted directly into the struct, no box.
3. `SwTypeTranslator.scala:63-65` — `opt[T]` → `T?` (Swift `Optional`). CONFIRMS — `left: opt[Tree]` → `let left: Tree?`, stored inline → infinite size.
4. `SwTypeTranslator.scala:66-68` — `lst[T]` → `[T]` (Array, heap-allocated). CONTEXT — `children: lst[Tree]` alone would NOT trigger infinite size; the `opt[Tree]` field is the trigger.
5. `mcp_stub.baboon:58-62` — `Tree { value: i32; left: opt[Tree]; children: lst[Tree] }`. CONFIRMS — direct nullable self-reference.
6. `mcp_stub.baboon:117-123` — `processShape(data in { shape: Shape; tree: Tree })` → `mcptools.processshape.in` carries `tree: Tree`, second infinite-size DTO. CONFIRMS.
7. `SwDefnTranslator.scala:822-831` — ADTs use `public indirect enum ...`. CONTEXT/CONFIRMS — the indirection mechanism exists for enums; DTOs get no equivalent.
8. `SwDefnTranslator.scala:388-407` — init constructor (fix blast radius); `Equatable`/`Hashable` auto-synthesized via the struct clause; JSON/UEBA codec field access lives in `SwJsonCodecGenerator.scala` / `SwUEBACodecGenerator.scala`.

lean: **confirmed** — DTOs are value-type structs (L410) with self-referential fields as bare stored properties (L354); `opt[Tree]`→`Tree?` (L63-65) stores the self-reference inline; ADTs already use `indirect enum` (L823) while DTOs get no indirection.

## Orchestrator validation
All 8 citations re-opened against source and verified [correct]: SwDefnTranslator.scala L350-355 / L388-418 / L822-831; SwTypeTranslator.scala L63-68; mcp_stub.baboon L58-62 / L117-123. H1 adjudicated **confirmed**.
