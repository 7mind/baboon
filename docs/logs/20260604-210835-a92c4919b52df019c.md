# Implement-flow session log

- **Task:** T9 — TypeScript MCP local round-trip overlay + test-gen-ts-mcp/test-ts-mcp actions
- **Milestone:** M3
- **Role:** implement-worker (round 0)
- **Agent id:** a92c4919b52df019c
- **Returned status:** `pass` — resultCommit `58464250` (base 346ff334), branch `worktree-agent-a92c4919b52df019c`
- **Files touched:** test/ts-stub-mcp-overlay/package.json, test/ts-stub-mcp-overlay/src/mcp.test.ts, .mdl/defs/tests.md

---

### Session summary
- **Did:** implemented task T9 in worktree `worktree-agent-a92c4919b52df019c` (reset to base commit `346ff334`)
- **Achieved:** pass; resultCommit `58464250af6e2efbdabb7c1746de2aa2c83e9d51`; 16/16 vitest tests green
- **Discovered:** `FFancyStr` is a foreign type whose generated TS codec throws `BaboonDecoderFailure` by design; the overlay registers a passthrough codec via the `Lazy`-instance override before tests. **`mdl` finds project root via a `.git` DIRECTORY; git worktrees have `.git` as a FILE, so `mdl :test-gen-ts-mcp` always traverses to the main repo — the tests.md actions are correct and work after merge to main; in the worktree the worker ran the action scripts directly.** AJV 8.x emits informational "unknown format int32/int64/double" warnings (ignored with strict:false). Channel B (`isError:true`) needs a real codec throw; demonstrated via `McpTools_submitComposite` with `nested:null` → TypeError → DecoderFailed.
- **Issues:** none

**Deliverable:** `test/ts-stub-mcp-overlay/` with a Vitest test (16 tests) driving the generated `McpToolsMcpServer<null>` through the full T7 scenario (initialize/tools-list/tools-call/error) via an in-process fake `invokeJson` delegate; AJV Draft-2020-12 validates each inputSchema at tools/list. Added `test-gen-ts-mcp` + `test-ts-mcp` to `.mdl/defs/tests.md` and registered `test-ts-mcp` in the `:test` aggregator. Four live negative controls: unknown-tool -32602, Channel-B isError from null-nested TypeError, enum violation, missing required field.
