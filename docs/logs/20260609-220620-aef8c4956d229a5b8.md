# Implement review log — T6 (round 1)

- Task: T6 — Role: implement-reviewer (opus) — Verdict: approve (0 criticisms/questions; 1 low out-of-scope defect → D4)

### Session summary
- `escapeKtKeyword` hard-keyword set matches Kotlin spec exactly (28 keywords); acceptance escaped/bare cases verified. Routed at field-param, UEBA capture-var + fieldsOf, JSON accessor + named-ctor-arg, key-encoder, conversion sites (kt-stub + kt-stub-kmp share the generator). Wire-neutral: JSON encode `put("name",...)` + decode `["name"]` use raw model name; UEBA positional. Type-preserving; new companion object valid. D3 Class-shadowing out of scope.
- Out-of-scope defect filed (D4, low): T6 escaped service-method DECLARATION (`fun \`when\``) but symmetric wiring call sites `impl.when(...)` in KtServiceWiringTranslator (lines 669/677/699/707/767/775/798/806) remain unescaped → asymmetry for a keyword-named service method. No fixture exercises it; build green. Fix in separate task.
