# Investigate session log — D1 / H2 (Python backend audit)

- Defect: D1
- Hypothesis: H2
- Role: explorer
- Returned lean: supports

### Session summary
- **Did:** gathered read-only evidence for H2 against the Python backend.
- **Achieved:** 9 numbered evidence items; lean supports.
- **Discovered:** Python backend derives identifiers with at most `.capitalize` (class/enum/method names) or verbatim (DTO field names, service/contract method names); NO reserved-word escaping/renaming anywhere. Clearest collision: DTO field site `PyDefnTranslator.scala:540` (`q"$fieldName: $fieldType"`) — a field named `class`/`def`/`import`/`lambda`/`None`/`is`/`in` → SyntaxError. Python has no escape syntax; PEP 8 mandates RENAMING (trailing underscore `class_`). The C# `.toLowerCase`d ADT-branch local has NO Python analogue (ADT dispatch uses `isinstance(value, <Branch>.capitalize)`, `PyUEBACodecGenerator.scala:179/184`); `.capitalize` neutralizes most lowercase keywords for class/enum-member names but NOT field/method names, and `none`/`true`/`false` capitalize into `None`/`True`/`False`.
- **Issues:** Grep unavailable; absence-of-escaping confirmed across 5 read files. Native fix differs from C#: rename, not escape.
