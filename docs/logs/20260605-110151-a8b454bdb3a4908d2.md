# Implement-worker session — task T23

- Task: **T23** — Fix Swift recursive value-type DTO codegen (reference indirection) + full Swift-matrix regression (resolves D8)
- Role: worker (frontier/opus)
- Returned status: **pass** — resultCommit `92ef0547f302d72dde5778bcccddfed0b4f29759`, branch `implement/T23` (based on `9eea5a27`)
- Agent id: a8b454bdb3a4908d2

## Verbatim session summary

- **Did:** implemented T23 on branch `implement/T23`. Fixed Swift recursive value-type DTO codegen via `@BaboonIndirect` reference-box indirection, wired the recursive `mcp-stub-ok`/`McpStub` model into the Swift test matrix, and added a recursive round-trip regression test.
- **Achieved:** status pass; resultCommit 92ef0547. All 5 acceptance criteria verified: (1) `swift build` of generated McpStub clean — infinite-size errors gone (reproduced the exact `value type 'Tree' cannot have a stored property that recursively contains it` first on the unfixed generator); (2) Full Swift matrix green: test-gen-regular-adt/test-swift-regular/test-gen-wrapped-adt/test-swift-wrapped/test-gen-compat-swift/test-manual-swift all success=true; (3) `sbt clean +compile` (JVM+Scala.js) green, no new warnings (only pre-existing IdentifierFieldKind.NestedId); (4) RecursiveDtoRoundTripTests (JSON+UEBA round-trip with value-equality across the box, unconditional throw, live negative control) passes in both regular and wrapped matrix sw-stub packages; (5) D8 resolvable.
- **Discovered:** (1) env-provided worktree cut from `e61a5068` (23 commits behind dispatched base, missing mcp-stub-ok + MCP infra); reset implement/T23 to `9eea5a27` and re-applied edits. (2) sbt-git (jgit) cannot build inside a linked git worktree (NoWorkTreeException: Bare Repository) — affects all sibling worktrees; built/tested in a /tmp copy with a real .git. (3) Swift stub Package.swift curated a subset that excluded McpStub (D8 had blocked it); wired it in. (4) codegen emits no fixture/test for Tree/processshape.in (not root-reachable), so a hand-written round-trip test was required.
- **Issues:** build/verification ran in a /tmp clone due to jgit-vs-worktree limitation; committed source lives correctly in implement/T23. Orchestrator should merge implement/T23 (92ef0547, based on 9eea5a27). The foreign-baboon-ref deref branch in recursiveFieldNames is not exercised by the corpus (conservative: returns false → no boxing → safe).

## Approach
Box recursive struct-DTO fields behind a new `@BaboonIndirect` property wrapper (final-class box, value-semantic, Equatable/Hashable-forwarding) so recursive value-type DTOs stay finite-size. Generator detects transitive self-recursion through INLINE edges only (scalar struct-ref + opt; lst/set/map are heap-backed and break the cycle) and boxes exactly those fields; init and JSON/UEBA codecs unchanged because the public field surface is preserved.

filesTouched: baboon_runtime.swift, SwDefnTranslator.scala, SwTypes.scala, test/sw-stub/Package.swift, RecursiveDtoRoundTripTests.swift
