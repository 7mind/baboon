# Implement-worker — T29 (D4+D5 Scala codec)

- Task T29; role worker (opus); status **pass**; resultCommit `adcdd2e5a5dda69802ed468271bb4d71ce910b5f` (parent f215f8de); agent a910752312cc9761b
## Verbatim
- Reproduced both first: D4 `object FFancyStr is not a member of package mcp.stub`; D5 `not found: value Color_JsonCodec` in nested listcollections package. Routed all 3 ScJsonCodecGenerator decoder sites (enum map-key :481, value decoders :552/:556) through the codecName helper -> real imported <X>_JsonCodec ScType. Removed FFancyStrShim.scala + listcollections/package.scala workarounds + stale shim refs in McpTests.scala.
- Discovered: the bare `${targetTpe}` not only failed to import the _JsonCodec companion but ALSO leaked the companion-less foreign FFancyStr into imports; codecName fixes both. _KeyCodec foreign path (:496-498) already correct, untouched.
- scala-stub-mcp compiles + 10 MCP tests pass WITHOUT shims; sc-stub regular 304/0 + wrapped 304/0 (240 cross-lang fixtures canceled in single-lang isolation, expected); sbt +compile (JVM+JS) green.
- filesTouched: ScJsonCodecGenerator.scala, McpTests.scala (overlay); REMOVED FFancyStrShim.scala + listcollections/package.scala. mdl :build/:ci = orchestrator gate. BOTH D4+D5 resolvable.
