# Investigate explorer — D10 / H10 (divergence REFUTED; coverage gap CONFIRMED)

- Defect D10; Hypothesis H10; role explorer; lean **mixed** (core divergence refuted, coverage gap confirmed); agent acc5506e1d98acebd
- Orchestrator validation: re-read OasTypeTranslator.scala:33-44 (resolveTypeRef recurses into Constructor args, L41) + OasBaboonTranslator.scala:165 (renderDto pre-resolves every field tpe before typeRefSchema/mapSchema) -> divergence refutation CONFIRMED.

## Verbatim findings
- OAS mapSchema (OasTypeTranslator.scala:146-160) matches enum keys via a DIRECT enumKeys.contains(id) test, no resolveTypeRef inside mapSchema (H10 part 1 literally true).
- MCP enumKey (McpInputSchemaEmitter.scala:278-309) resolves the key via resolveTypeRef first (H10 part 2 true).
- BUT OAS pre-resolves the WHOLE field at OasBaboonTranslator.renderDto:165 (resolveTypeRef on each field tpe), and resolveTypeRef recurses into map-constructor args (OasTypeTranslator:40-41) -> a map[ForeignRtEnum,V] key is resolved to the enum BEFORE mapSchema runs, so enumKeys.contains matches -> string-keyed object, SAME as MCP. REFUTES the divergence. Both typeRefSchema callers (DTO field 177, ADT branch -> renderDto 228) are pre-resolved.
- Reachability of input: a foreign with rt=Enum is legal (Typedef.scala:60) and the m19 map-key validator accepts a Foreign key at face value without following rt (BaboonValidator.scala:238-241) -> the model is constructible, but the divergent OUTPUT is unreachable on current code.
- COVERAGE GAP CONFIRMED: the only enum-keyed map in the entire corpus is `byColor: map[Color,str]` in mcp_stub.baboon:112; no OAS-exercised model/golden covers the T30 OAS string-keyed-object branch (OasTypeTranslator:149-151); no Oas*/MCP-emitter unit test .scala exists.
- FIX SITE: (a) add an OAS-exercised model+golden with an enum-keyed map (+ ideally a foreign-rt-to-enum-keyed map) to pin behavior + the renderDto:165 pre-resolution invariant; (b) optionally harden OasTypeTranslator.mapSchema to resolve the key itself (mirror MCP enumKey) so correctness is local, not caller-dependent.
- **lean: mixed** — divergence refuted, coverage gap confirmed.
