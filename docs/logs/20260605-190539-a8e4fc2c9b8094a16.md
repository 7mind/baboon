# Implement-worker — T30 (D6 Python enum map-key + inputSchema reconcile)

- Task T30; role worker (opus); status **pass**; resultCommit `77af9962eafe05eb9c7b762b61bab391c2a5db5a` (parent f215f8de); agent a8e4fc2c9b8094a16
## Verbatim
- Reproduced first: base Python mkJsonKeyEncoder/Decoder had no Typedef.Enum arm -> enum key fell to inner BUG-throw (PyJsonCodecGenerator.scala:344/:439). Added enum arm to both: encode `{k.value: ...}`, decode `{Color(k): v ...}` -> string-keyed JSON object. Reconciled MCP (McpInputSchemaEmitter) + OAS (OasTypeTranslator) inputSchema for enum-keyed maps to string-keyed-object form (object + additionalProperties + propertyNames constrained to enum wire values) matching the wire all backends emit. Replaced byColor:{}/entry-array test dodges with NON-empty byColor round-trips across all 9 MCP overlays + the Scala golden.
- Discovered: the only enum-keyed map in the corpus is byColor in mcp-stub-ok; the byColor reference schema is duplicated across all 9 overlays + golden (all moved entry-array -> string-keyed-object); C#(NJsonSchema)+TS(ajv) validate the conforming instance against the emitted schema so their byColor instances changed too. py-stub has no jsonschema dep -> python test validates structurally; full-validator check covered by cs/ts lanes.
- sbt +compile (JVM+JS) green; baboonJVM/test 589; ALL 9 MCP lanes green; test-openapi/python-regular/python-wrapped/graphql/sbt-basic green; McpInputSchemaEmissionTest golden updated 6/6.
- filesTouched: PyJsonCodecGenerator.scala, McpInputSchemaEmitter.scala, OasTypeTranslator.scala, OasBaboonTranslator.scala, McpInputSchemaEmissionTest.scala, + 9 overlay test files (py/scala/kotlin/java/rust/dart/swift/cs/ts). D6 resolvable.
