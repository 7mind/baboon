# Investigate session log — D1 / H2 (Kotlin backend audit)

- Defect: D1
- Hypothesis: H2
- Role: explorer
- Returned lean: supports

### Session summary
- **Did:** gathered read-only evidence for H2 against the Kotlin backend.
- **Achieved:** 9 numbered evidence items; lean supports.
- **Discovered:** Kotlin backend derives all surface identifiers verbatim with no escaping at any stage — type names via `tid.name.name.capitalize` (`KtTypeTranslator:162`), DTO `val` property/param names via `${f.name.name}` (`KtDefnTranslator:270`), enum entries via `.capitalize`, contract `val`/service `fun` names (`KtDefnTranslator:409,443`), JSON-codec field locals/named-args (`KtJsonCodecGenerator:157,159,166`), UEBA field locals/named-args (`KtUEBACodecGenerator:354,358`). Closest C# twin: UEBA ADT-branch capture `val castedName = branchName.toLowerCase` declared `val $castedName = instance` (`KtUEBACodecGenerator:185,204`) — VALIDATED by orchestrator. Final renderer `KtBaboonTranslator:236-246` emits `t.name`/package parts verbatim. No backtick/keyword/reserved/escape/sanitize logic anywhere. Native fix: backtick-quoting `` `name` `` for hard keywords (`fun`/`val`/`object`/`is`/`when`/`class`/etc.).
- **Issues:** Grep/Glob unavailable to the explorer.
