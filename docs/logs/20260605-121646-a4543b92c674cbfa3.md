# Implement-worker session — task T21 (CI closeout) — FAIL (regression found)

- Task: **T21** — Full-pipeline CI closeout: mdl :build :test + :ci green, off-by-default verified
- Role: worker (standard/sonnet)
- Returned status: **fail** — regression at bedabb3d blocks `mdl :test`; resultCommit `b97e27cd41a58fc1d7b4d1d8baa1393fb3adf4c5` (CLAUDE.md doc update on implement/T21, NOT merged)
- Agent id: a4543b92c674cbfa3

## Verbatim session summary
- **Did:** ran `mdl :build` (GREEN, native-image 3m5s), `mdl :test` (RED — 8 lane failures), off-by-default diff verification, updated CLAUDE.md.
- **Achieved:** fail; `mdl :build` green, `mdl :test` red (8 wiring lanes fail).
- **Discovered:** Regression at bedabb3d (main HEAD). `mcp-stub-ok/mcp_stub.baboon` is in the SHARED test-resources dir `baboon-compiler/src/test/resources/baboon/`. All wiring test actions use `--model-dir` pointing at this shared dir, so they pick up the MCP model and generate fixture code. The fixture generator emits cross-namespace type references (Color, PointPage_Fixture, FFancyStr) WITHOUT proper import qualifications in the fixture files (In_Fixture.*), causing compile failures in Scala (4 wiring variants: either/hkt/outcome/result), Rust (2: either/result), Kotlin (1), Java (1). The MCP-specific lanes (which use --model-dir .../mcp-stub-ok/ specifically) are unaffected and passed (8/9 confirmed; java-mcp+scala-mcp aborted on pipeline stop). Off-by-default verified: no-MCP-flag codegen produces zero MCP files; with-MCP diff = exactly 2 files (BaboonMcpRuntime.ts, mcp-server.ts).
- **Issues:** Real regression at bedabb3d blocking `mdl :test`/`mdl :ci` green. Root cause = fixture import generation for the mcp-stub model when picked up by full-model-dir invocations. Fix: (a) move mcp-stub-ok out of the shared dir, OR (b) fix fixture generation to emit fully-qualified imports for cross-namespace types. NOT fixed in T21 (scope guard — correctly reported as fail).

## Orchestrator confirmation
Structurally confirmed: `mcp-stub-ok` is under `baboon-compiler/src/test/resources/baboon/`; multiple actions use `--model-dir ./baboon-compiler/src/test/resources/baboon/` (shared root, e.g. tests.md L59/172/376/478/1112). Filed as defect **D9**; T21 set `blocked` pending the fix. Note: option (a) relocate conflicts with T23, which deliberately wired McpStub INTO the shared swift matrix to exercise the recursive Tree — the fix approach must preserve that coverage (defer to investigate/plan).
