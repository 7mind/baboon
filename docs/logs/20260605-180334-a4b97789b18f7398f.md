# Investigate explorer — D1 / H3 (REFUTED)

- Defect D1 (MCP inputSchema emitter drops ADT common fields); Hypothesis H3; role: explorer; lean: **refuted**; agent a4b97789b18f7398f
- Orchestrator validation: spot-checks of sibling explorers (D4/D5/D6) matched source exactly; H3's refutation is logically sound (adt.fields derived from contracts; contracts merged into branches). Disposition: D1's fix is a VERIFYING test (prove branch schemas carry the common fields); if it fails, add the allOf merge.

## Verbatim
- adtSchema (McpInputSchemaEmitter.scala:209-212) emits only `oneOf` of branch refs, ignoring `adt.fields` — CONFIRMS the mechanism but NOT a defect.
- `adt.fields` is derived SOLELY from `adt.contracts` (BaboonTranslator.scala:436-446).
- Branch DTOs inherit the parent ADT's contracts (BaboonTranslator.scala:289-306) and merge their fields into the branch DTO's own `fields` (BaboonTranslator.scala:360-362) at typing time.
- So each branch `$defs` DTO (emitted via dtoBody, McpInputSchemaEmitter.scala:174) already carries the common fields; the proposed `allOf adt.fields` would DUPLICATE them.
- OAS emitter uses the identical oneOf-of-refs convention (OasBaboonTranslator.scala:239).
- K6 stub `Shape` (mcp_stub.baboon:66-69) has no common fields; existing ADT test McpInputSchemaEmissionTest.scala:260-279 asserts oneOf+2 branch refs but nothing about common fields.
- **lean: refuted** — common fields are already in each branch via contract-merge; static typer-path evidence (not run). Recommend adding a contract-bearing ADT to mcp_stub + asserting each branch's properties includes the contract fields (expected: already present).
