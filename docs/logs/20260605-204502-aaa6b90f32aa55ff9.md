# Implement-reviewer — T32 (D10 OAS coverage) — DISAPPROVE (1 criticism, autonomously fixable)

- Task T32; reviewer (opus); verdict **disapprove** (1 crit/0 q/0 def); agent aaa6b90f32aa55ff9
- Ran in /tmp clone @366ae7ff: all 6 OasEnumKeyMapSchemaTest pass + 2 source mutations. Mutation A (disable mapSchema enum-key branch) fails Cases 1+4, str/uid/i32 guards stay green -> Cases 1-3 non-vacuous + load-bearing. Mutation B (no-op resolveTypeRef's Constructor arg-recursion at OasTypeTranslator:41-42 — the production-critical resolution for a foreign key INSIDE a map at renderDto:165) leaves ALL 6 tests GREEN -> Case 4 does NOT pin the invariant it names.
- CRITICISM: Case 4 ('foreign rt resolves to enum key') resolves a bare TypeRef.Scalar(foreignId) (only the Scalar(User) branch) then hand-builds the map with the already-resolved key, bypassing the production path. The production OAS path (OasBaboonTranslator.renderDto:165) resolves the WHOLE field TypeRef.Constructor(map,[foreignKey,val]) via resolveTypeRef's Constructor arg-recursion (OasTypeTranslator:40-42). FIX: route Case 4 through resolveTypeRef(TypeRef.Constructor(TypeId.Builtins.map, NEList(foreignRef, valRef)), foreignResolutions) then typeRefSchema(.., enumKeys) and assert propertyNames — so a break of the Constructor recursion / renderDto:165 invariant is caught.
- Scope clean: one test file, no src/main change, T1-T31 untouched.
