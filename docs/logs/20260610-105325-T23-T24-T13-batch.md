# Implement batch log — T23/T24/T13 — worker + reviewer

## T23 — D7 C# System.Type FQ (worker a766a13d / reviewer abd560e3d6969c97c) — pass / approve
Worker (b127e202): CSDomainTreeTools L57/L97 `$csTpe`→`${csTpe.fullyQualified}` (System.Type). dotnet build of reserved-words-ok C#: pre-fix 24 CS0508/CS0738, post-fix 0. Repo has NO committed cs goldens (cs matrix generates fresh + dotnet-builds) → rebaseline a no-op; source-only 1-file change. Reviewer: both sites FQ'd; `${csTpe.fullyQualified}` provably renders System.Type via renderSimpleType; type-identical edit, zero compile risk. 0 findings.

## T24 — D8 Java enum-parse + D9 TS type/enum escape (worker a52266648 / reviewer a4951faa71ebb4a98) — pass / approve
Worker (100d59f8): D8 JvDefnTranslator:494 enum parse param → `${jvString.fullyQualified}`; D9 routed TS class/type/enum names + lowercase-mode enum ident through escapeTsKeyword (asTsTypeKeepForeigns + makeEnumRepr), wire `value` unchanged. +compile (JVM+JS) + 602 green. Reviewer: D8 FQ correct + <T>Codec exclusion sound; D9 wire values/discriminator preserved (only TS-side ident escaped); byte-identical existing fixtures; +compile green (clean-clone verified). Filed 1 low PRE-EXISTING out-of-scope defect → D10 (lowercase-mode enum ident vs _values capitalization mismatch; not introduced by T24, no fixture exercises it).

## T13 — matrix wiring (worker aa9ade013 / reviewer a5a9e0e3e07ae8de4) — pass / approve
Worker (9478a082): option (a) — test-gen-{regular,wrapped}-adt already scan the shared model-dir (reserved-words-ok) with --generate-{json,ueba}-codecs-by-default=true across all 9 backends; documented in .mdl/defs/tests.md + added ReservedWordsScalaEmissionTest (2/2 green). Reviewer verified the action defs (codecs-on + shared-dir scan confirmed), all 9 per-lang regular/wrapped lanes exist, C# branch-lowercase collision site exercised; shape test green. 0 findings.
