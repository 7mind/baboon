# Implement-flow session log

- **Task:** T3 — Design the transport-abstract MCP dispatch runtime contract
- **Milestone:** M2
- **Role:** implement-worker (round 0)
- **Agent id:** ab68645134bca8eaa
- **Returned status:** `pass` — resultCommit `de570ae6` (parent d3e0557d), branch `worktree-agent-ab68645134bca8eaa`; created decisions item **K5**
- **Files touched:** docs/research/mcp-dispatch-runtime-contract.md

---

### Session summary
- **Did:** implemented task T3 in worktree branch `worktree-agent-ab68645134bca8eaa` (reset onto base `d3e0557d` first, since the worktree was cut at the wrong commit `e61a5068`).
- **Achieved:** pass; resultCommit `de570ae678cb9edadea2baf54029a6b4fd7835b3` (parent = intended base `d3e0557d`); committed `docs/research/mcp-dispatch-runtime-contract.md` and created locked `decisions:K5` under milestone M1 (K1–K4 untouched). No build check applies — design/decision task; "check" is internal consistency + feasibility grounded in real runtime contracts (read actual C#/TS/Swift/Python/Rust service-wiring runtimes).
- **Discovered:** The Swift type-eraser pattern (`AnyJsonServiceCtx<Ctx,R>` at sw:185) and Python `Ctx = TypeVar` + `Protocol[Ctx,R]`/`Generic[Ctx,R]` (py:189/193/206) are already in the shipped service-wiring runtime, so the MCP `AnyMcpServer<Ctx>` and `Generic[Ctx]` sketches reuse a proven idiom. The muxer is `R`-parametric for sync/async; deliberately made `IBaboonMcpServer` `Ctx`-only-parametric because the JSON-RPC response envelope is fixed.
- **Issues:** none. Follow-up for downstream: T5/T6 must extend `ServiceContextResolver` (currently 9 langs, no MCP awareness) for the none/abstract/type trichotomy; the per-language `*McpTranslator` (sibling of `*ServiceWiringTranslator`) plus `baboon-runtime/<lang>/` resource files are net-new in T8/T10/T12–T18.

Contract summary (K5): `handle(JsonRpcRequest, McpSession, Ctx, BaboonCodecContext) -> Option<JsonRpcResponse>` entrypoint; additive runtime family `IBaboonMcpServer<Ctx>` + `JsonRpc{Request,Response,Error}` + `McpToolEntry` + `McpSession` (mirrors IBaboon{Json,Ueba}ServiceCtx); transport-injection seam (no generated I/O loop); tool-registry shape; ctx threading transport→dispatch→service method; `baboon-runtime/<lang>/` resource home; 9-language feasibility sketch incl. Swift/Python/Rust/C# hard cases.
