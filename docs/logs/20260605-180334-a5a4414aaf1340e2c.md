# Investigate explorer — D2 / H4 (CONFIRMED mechanism; reachable at typer/validator gate)

- Defect D2 (MCP inputSchema: contract in reachable closure -> dangling $defs ref); Hypothesis H4; role: explorer; lean: **supports** (mechanism confirmed; end-to-end repro not run); agent a5a4414aaf1340e2c
- Orchestrator validation: citations consistent with the McpInputSchemaEmitter structure validated for D1. Disposition: real latent defect; fix = fail-fast guard.

## Verbatim
- emitNamedType returns `List.empty` for NonDataTypedef (Service/Contract) (McpInputSchemaEmitter.scala:166-169) — no $defs body.
- The closure walk enqueues any referenced TypeId.User with no contract exclusion (McpInputSchemaEmitter.scala:147-158) and localRefs it `#/$defs/<name>` (:231-235, :305-306).
- Contract carries a TypeId.User which IS a Scalar (Typedef.scala:71, TypeId.scala:29).
- convertTpe (BaboonTranslator.scala:540-551) admits a contract id as a field type — only a `Scalar` guard, no data-type check. So a field `foo: SomeContract` resolves to TypeRef.Scalar(contractId) — DECISIVE reachability.
- No validator pass rejects a plain contract-typed field (BaboonValidator.scala:462-473; the Contract->IneligibleUserType guard is map-KEY only, :302-310/:269).
- **lean: supports** — mechanism + reachability source-confirmed; NOT reproduced end-to-end. Fix site: McpInputSchemaEmitter.scala:169 — replace silent List.empty with a fail-fast throw (matching :181/:253 style); root-cause guard belongs upstream (reject a field whose resolved user type is NonDataTypedef in convertTpe/validator).
