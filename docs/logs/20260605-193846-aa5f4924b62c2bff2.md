# Implement-reviewer — T28 (D3 TS import sort) — DISAPPROVE (2 criticisms, autonomously fixable)

- Task T28; reviewer (opus); verdict **disapprove** (2 crit/0 q/0 def); agent aa5f4924b62c2bff2
- Lexical-order sub-test is LIVE (fails on reverted base: `../DocResp` before `../DocReq`; passes post-fix). Cross-build `+baboonJVM/compile`+`+baboonJS/compile` green. Both nondeterministic sites (renderTree + facade collectTypes) now total+deterministic -> D3 functionally resolved.
- CRITICISM 1: renderTree (TsBaboonTranslator.scala L474) sorts modules by `moduleId.path.mkString(" ")`, but the rendered import is `relativeImportPath(...)` (a `../`-relative string) — orderings can diverge on asymmetric `../` depths (src a/b/c/file: target a/b/y -> ../y vs a/x -> ../../x; by-path = ../y,../../x but rendered-lexical = ../../x,../y), so the test's own `sources == sources.sorted` (on rendered sources) would FAIL on such a model. Fix: sort renderTree typesByModule by the rendered import source string, matching the facade site L218 (already sorts by rendered importPath).
- CRITICISM 2: the byte-identity-across-two-translations sub-test is VACUOUS — it PASSES on the reverted (pre-fix) base (immutable-HashMap iteration is deterministic within one JVM process), so it cannot catch the cross-run/cross-JVM nondeterminism it claims to guard. Make it meaningful (assert rendered block == its own lexically-sorted form, or a fixed expected) or remove.
