# Implement-flow session log

- **Task:** T12 — Scala MCP replica (criticism round 1: K1 fix)
- **Milestone:** M4
- **Role:** implement-worker (round 1)
- **Agent id:** a05bc3d968ba4b235
- **Returned status:** `pass` — resultCommit `a36e3bf5` (parent e1a24f53), branch `worktree-agent-a05bc3d968ba4b235`
- **Files touched:** test/scala-stub-mcp-overlay/src/test/scala/McpTests.scala

---

### Session summary
- **Did:** implemented task T12 (criticism round 1), resetting to commit `e1a24f53`
- **Achieved:** pass; resultCommit `a36e3bf5`; all 10 ScalaTest assertions green
- **Discovered:** `-Wvalue-discard` (error) fires when `assert()` (returns `org.scalatest.Assertion`) is called inside a Unit-returning helper; suppressed with `@nowarn("cat=w-flag-value-discard")`.
- **Issues:** none
- **Fix:** replaced the vacuous K1 self-round-trip with (a) well-formedness check + (b) per-tool structural equality against embedded T7 §2.3 reference literals for all 5 tools + (c) a negative control (wrong ref with extra 'WRONG' field → TestFailedException). `normaliseRequired` makes `required` order-insensitive per §5.4. Only the overlay test changed; no generator/runtime touched.
