# Implement-reviewer session — task T23 (round 0)

- Task: **T23** — Fix Swift recursive value-type DTO codegen (resolves D8)
- Role: reviewer (opus)
- Verdict: **approve** — 0 criticisms, 0 questions, 0 defects
- Agent id: a4882ff98e5f4468b

## Verbatim session summary

- **Did:** reviewed T23 round 0 (diff 9eea5a27..92ef0547); read all 5 changed files; verified recursion-detection logic against the Typedef/DomainMember/ForeignMapping model; compiled the JVM project at the commit; ran the JVM baboon compiler on mcp-stub-ok to emit real Swift; typechecked generated McpStub+BaboonRuntime with swiftc; reproduced the pre-fix infinite-size error by un-boxing.
- **Achieved:** verdict approve; 0 criticisms / 0 questions / 0 defects.
- **Discovered:** Generated `struct Tree` boxes exactly `@BaboonIndirect public var left: Tree?` while `value`/`children: [Tree]` stay `public let` — surgical and correct. Real generated McpStub module (recursive Tree, mcptools.processshape.in carrying tree: Tree, JSON+UEBA codecs, conversions, service wiring) passes `swiftc -typecheck` clean; un-boxing reproduces exactly `value type 'Tree' cannot have a stored property that recursively contains it`. `sbt baboonJVM/compile` green, no new warnings-as-errors. `@BaboonIndirect` wrapper, init-via-init(wrappedValue:), Equatable/Hashable forwarding, value-semantics all typecheck under -O. Detection handles 2-hop cycles (A→opt[B]→opt[A]) and does not over-box heap-backed lst/set/map. Codecs construct via init and read value.left → wire surface identical to unboxed form (no extra JSON level). Test has genuine unconditional-throw harness + live require(false) negative control. Scope confined to exactly 5 files.
- **Issues:** Could not execute the Swift test RUNTIME nor the full mdl Swift matrix in-sandbox (FHS swift-suite vs sandbox glibc: `undefined symbol: __nptl_change_stack_perm`), and did not run the Scala.js `+compile` half — these (acceptance #2 runtime round-trips, #3 JS half) are corroborated by the worker's reported green matrix + diff-level + swiftc-typecheck + JVM-compile evidence; the change touches no Scala.js exhaustive-match site and adds no new TyperIssue, so the documented cross-build failure mode does not apply.

verdict approve; rationale: real generated McpStub passes swiftc -typecheck clean; un-boxing reproduces the exact infinite-size error; only `left: Tree?` boxed; sbt baboonJVM/compile green; detection handles 2-hop cycles, never over-boxes lst/set/map; codec wire surface unchanged; test has unconditional throw + live negative control; scope confined to 5 files.
