# Investigate explorer — D6 / H8 (CONFIRMED round-trip failure; "entry-array" framing nuance)

- Defect D6 (Python codec mishandles non-string-keyed maps); Hypothesis H8; role: explorer; lean: **mixed** (round-trip failure confirmed; entry-array framing inaccurate for the data codec); agent a0ec413510e4eb3f5
- Orchestrator validation: PyJsonCodecGenerator.scala:317-345 re-read — no `Typedef.Enum` arm; enum hits `case _ =>` throw at :344. VALIDATED.

## Verbatim
- mkJsonKeyEncoder (PyJsonCodecGenerator.scala:317-345) has arms for identifier-DTO / 1-field-DTO / Foreign but NO enum arm -> a Typedef.Enum key hits `case _ =>` BUG-throw at :344. Symmetric decoder mkJsonKeyDecoder (:396-439) throws at :439. So `map[Color,str]` raises at runtime (T18 used byColor:{} to dodge).
- Routing trigger: fieldHasUserKeyMap (:222-231) forces enum-keyed maps through the explicit key-walker.
- Scala HAS the enum map-key arm (ScJsonCodecGenerator.scala:317-321 stringifies); Python lacks it -> the Python-specific defect.
- NUANCE: the OAS/MCP inputSchema declares non-string-keyed maps as an ENTRY-ARRAY of {key,value} (OasTypeTranslator.scala:141-159), but BOTH Scala (:402-405) and Python (:277-284) DATA codecs emit a STRING-KEYED OBJECT. So the codec wire form is string-keyed-object; the schema-vs-codec entry-array mismatch is a separate cross-language question.
- FIX: add a `Typedef.Enum` arm to mkJsonKeyEncoder (stringify, e.g. $ref.value) + mkJsonKeyDecoder (reconstruct via enum class), mirroring Scala. Reconcile with the MCP inputSchema representation for enum-keyed maps so a non-empty byColor round-trips AND validates.
- **lean: confirmed (round-trip failure); entry-array framing inaccurate for the data codec.**
