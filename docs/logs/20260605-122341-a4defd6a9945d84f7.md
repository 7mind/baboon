# Investigate explorer session — D9 / H2

- Defect: **D9** — Full `mdl :test` red: mcp-stub-ok in shared model-dir breaks 8 wiring lanes' fixture compilation
- Hypothesis: **H2** — wiring/regular/wrapped lanes use the shared `resources/baboon/` model-dir (now containing mcp-stub-ok) + `--fixture-output`, generating MCP stub fixtures with unqualified cross-namespace refs that fail to compile in sc/rs/kt/jv
- Role: explorer (read-only)
- Returned lean: **confirmed** (11 evidence items)
- Agent id: a4defd6a9945d84f7

## Key validated evidence
1. mcp_stub.baboon declares `Color` (enum), `Page[T]`/`PointPage`, `FFancyStr` (foreign) — the symbols in the D9 errors.
2. tests.md:1262-1276 (test-gen-sc-wiring-either): `--model-dir ./baboon-compiler/src/test/resources/baboon/` (shared root) + `--fixture-output` (L1269). Same at sc-result/outcome/hkt, rs-either/result (1878-1885/1927-1934), kt-wiring (2218-2225), jv-wiring (2265-2272) = all 8 failing lanes.
5. tests.md:1610-1622 (ts-mcp) + 2686-2700 (python-mcp): `--model-dir .../mcp-stub-ok/` curated subdir, NO --fixture-output → MCP lanes unaffected (the asymmetry).
6. 7 MCP lanes hardcode `.../mcp-stub-ok/` as model-dir (ts:1611, cs:1665, rust:1722, kotlin:1775, java:1829, scala:2633, python:2687); dart/swift mcp lanes beyond read window.
7. Package.swift:162-181 — T23 wired `McpStub` target into the Swift matrix; RuntimeTests depends on McpStub.
8. RecursiveDtoRoundTripTests.swift:20,31-35 — recursive coverage uses ONLY `Tree` (never Color/Page/FFancyStr) → a minimal recursive-only model preserves it.
10. SwCodecFixtureTranslator.scala:270-281 — Swift fixture emitter QUALIFIES cross-namespace refs (package + importAs) → Swift absent from failing set.
11. D4/D5 ledger reproduction — Scala main codegen for mcp-stub emits unresolvable cross-namespace refs (FFancyStr, Color_JsonCodec) under plain Scala flags.

## Orchestrator validation
Re-opened and verified: tests.md:1263+1269 (sc-wiring shared model-dir + fixture-output), tests.md:1611 (ts-mcp curated subdir), Package.swift:162-181 (McpStub target + comment confirming T23 wired it for recursive Tree). The actual 8-lane compile failures were reproduced by the T21 worker's full `mdl :test` run. H2 adjudicated **confirmed**.

## Explorer-noted limitation
Explorer runtime lacked Bash/Grep; could not open the Scala/Rust/Kotlin/Java fixture translators directly (items 10/11 lean on the SwCodecFixtureTranslator contrast + D4/D5 ledger reproduction). Mechanism (shared model-dir + fixtures → mcp-stub picked up) is directly source-cited; proximate unqualified-ref failure reproduced by T21.
