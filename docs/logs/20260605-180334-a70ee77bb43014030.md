# Investigate explorer — D3 / H5 (real ordering defect; literal "run-to-run" framing refuted)

- Defect D3 (TS nondeterministic import ordering); Hypothesis H5; role: explorer; lean: **refuted** (literal run-to-run) / a real latent non-lexical ordering defect exists; agent a70ee77bb43014030
- Orchestrator validation: fix-site lines consistent; disposition: fix = add deterministic lexical sort (robustly correct regardless of the run-to-run-vs-toolchain framing).

## Verbatim
- Import SET collected from immutable TextTree traversal + `.distinct` (TsBaboonTranslator.scala:453-457) — not a Set/HashMap.
- Module-line ORDER: `groupBy(_.moduleId).toList.sortBy { path.size + types.size }.reverse` (TsBaboonTranslator.scala:461) — NON-injective sort key; ties fall back to immutable-HashMap iteration order.
- Symbol order within `{}`: `partition` preserves groupBy order, no sort (TsBaboonTranslator.scala:467-473); render joins without re-sort (:498).
- Scala immutable HashMap iteration is hash-stable PER BINARY (not per-run seeded) -> NOT run-to-run-varying on the same binary; it IS non-lexical / unstable across toolchain/library changes.
- The barrel/re-export path already sorts (`names.toList.sorted`, TsBaboonTranslator.scala:566-568) = the fix model.
- **lean: refuted** (literal run-to-run) but a genuine latent defect: import order lacks a deterministic lexical sort. Fix: sort modules by full path (:461) and symbols by name (:467) before render, mirroring :566. Add a determinism regression (no golden exists today).
