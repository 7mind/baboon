# T36 G7/D15 Close-out Gate — Worklog

**Date:** 2026-06-11  
**Base commit verified:** 69428dd7 (T35 merged tip on `main`)  
**Branch:** implement/T36  
**Worker:** sonnet-4-6

## Scope

Verification gate for the complete G7/D15 change set (T29–T35, merged at `69428dd7`).

## Pre-flight

Worktree reset to `69428dd7`:
```
git reset --hard 69428dd7
```
sbt-git/jgit worktree caveat: cloned repo to real directory for sbt to work:
```
git clone --shared /home/pavel/work/safe/baboon-projects/baboon /tmp/baboon-t36-clone
git -C /tmp/baboon-t36-clone checkout 69428dd7
```

## Step 4 — TyperIssue exhaustive-match check

T29–T35 are all LSP-layer changes (byte-accurate framing, dispatch barrier,
in-process integration test, workspace input dedup, URI-to-path guard,
hover lower-bound guards, DocumentSymbol owning-domain fix).

No new `TyperIssue` cases were introduced. The three exhaustive-match sites
(`DiagnosticsProvider.scala`, `WorkspaceState.scala`, `BaboonJS.scala`) require
no update. 3-site update pattern: N/A.

## Step 5 — Probe replay

`/tmp/probe-h10/` absent. Step skipped.

## Gate 1 — `sbt +compile` (JVM + Scala.js cross-build)

Command (run in `/tmp/baboon-t36-clone`):
```
sbt +compile
```

Result:
- Exit code: **0**
- Completed: 2026-06-11 22:13:17
- Duration: 67 s
- No errors; pre-existing test-source warnings only (unused imports, unused private methods — not promoted to errors under prod `-Wconf`).

## Gate 2 — `sbt baboonJVM/test` (full JVM test suite)

Command (run in `/tmp/baboon-t36-clone`):
```
sbt baboonJVM/test
```

Result:
- Exit code: **0**
- Completed: 2026-06-11 22:15:04
- Duration: 93 s
- Tests: **643 succeeded, 0 failed, 3 canceled, 0 ignored, 0 pending**
- "All tests passed."

## Gate 3 — `mdl --seq :build :test` (CI-equivalent full acceptance matrix)

Command (run in `/tmp/baboon-t36-clone`):
```
mdl --seq --simple-log :build :test
```

Result:
- Exit code: **0** (`MDL_EXIT_CODE: 0`)
- Total wall time: 2201.5 s (~36.7 min, serial mode)
- All actions returned ✅
- Execution completed successfully.
- Native binary: `baboon-compiler/.jvm/target/graalvm-native-image/baboon`

Key completed actions (representative):
- build (GraalVM native-image)
- test-gen-regular-adt, test-gen-wrapped-adt, test-gen-manual
- test-cs-{regular,wrapped,manual,mcp}, test-scala-{regular,wrapped,manual,mcp}
- test-rs-{regular,wrapped,manual,mcp}, test-ts-{regular,wrapped,manual,mcp}
- test-kt-{regular,wrapped,manual,mcp}, test-kt-kmp-{regular,wrapped,manual,mcp}
- test-jv-{regular,wrapped,manual,mcp}, test-dt-{regular,wrapped,manual,mcp}
- test-swift-{regular,wrapped,manual,mcp}
- test-{cs,sc,rs,ts,kt,jv,dt,sw}-wiring-{async,errors,hkt,outcome,result}
- All compat lanes (cs, scala, rust, typescript, kotlin, kotlin-kmp, java, dart, swift)

## Summary

All three gate commands exited 0. G7/D15 change set (T29–T35) passes the full
CI-equivalent gate. No substantive failures or regressions observed. No new
TyperIssue cases; no exhaustive-match debt.
