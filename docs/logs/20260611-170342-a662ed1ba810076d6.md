# Plan-flow session log — G6 (bootstrap, first clarifying round)

- **Goal:** G6 — Auto-extracted contracts: `has mirror` / `has contract` interface emission (milestone M15)
- **Role:** planner (`plan-advance` subagent, agent-id `a662ed1ba810076d6`, single-planner mode)
- **Invoked by:** /cq:plan bootstrap
- **Returned status:** `awaiting-answers`

### Session summary
- **Did:** Single state-driven step for goal G6 (rule 2(a), fresh goal in `clarifying`, not defect-seeded): grounded read-only in the repo, then filed the first batch of 8 clarifying questions.
- **Achieved:** Created questions Q5–Q12 (status `open`, `ledgerRefs: ["goals:G6"]`, milestone M15) covering: Q5 host-surface scope (`data`/`id`/`adt`/`contract`, template vs plain); Q6 field-extraction rule (resolved-vs-declared input, transitive type-param-mention exclusion, empty-contract degenerate case); Q7 first-class-ness of the synthesized contract (referenceability, LSP, ContractDiff, imports); Q8 `has contract` = implicit `is B` with every monomorphized instantiation implementing one shared non-generic B, `has mirror` = no relationship; Q9 naming/collision/cardinality policy; Q10 @root output-inclusion rule incl. uninstantiated-template edge; Q11 GraphQL/OpenAPI parity (contracts currently skipped there); Q12 acceptance bar (typer tests, shared-matrix fixture, assignability assertion, byte-identical-when-unused, optional two-version evolution fixture). G6 remains in `clarifying`.
- **Discovered:** Baboon ALREADY has first-class contracts — `contract` keyword (`Keywords.scala:25`), top-level defs with optional template heads (`/home/pavel/work/safe/baboon-projects/baboon/baboon-compiler/src/main/scala/io/septimalmind/baboon/parser/defns/DefContract.scala`), `is Name` member refs (`DefDto.scala:95`), `Typedef.Contract extends User with NonDataTypedef` (codec-free; `/home/pavel/work/safe/baboon-projects/baboon/baboon-compiler/src/main/scala/io/septimalmind/baboon/typer/model/Typedef.scala:71`), evolution-diff participation via `ContractDiff`/`ContractOp` (`TypedefDiff.scala`), and ADT-level contract members. Templates monomorphize via `TemplateInstantiator` from raw bodies in `TemplateRegistry` — template declarations never become `Typedef`s, so "A implements B" must mean per-instantiation implementation. GraphQL/OpenAPI skip all `NonDataTypedef`s (`GqlBaboonTranslator.scala`). Net: G6 is mostly a parser+typer feature (`has` is a new keyword; new `RawDtoMember` arm; synthesis pass), with backends reusing existing contract emission — the M29/M33 3-site exhaustive-match pattern (DiagnosticsProvider/WorkspaceState/BaboonJS) will apply to any new `TyperIssue` cases.
- **Issues:** None. Next planner pass should re-use the grounding above (persist it to `fields.grounding` at the 2(b) step) instead of re-exploring.
