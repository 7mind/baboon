# Investigate session log — D1 / H2 (Java backend audit)

- Defect: D1
- Hypothesis: H2
- Role: explorer
- Returned lean: supports

### Session summary
- **Did:** gathered read-only evidence for H2 against the JAVA backend.
- **Achieved:** 8 numbered evidence items; lean supports (strongly).
- **Discovered:** Decisive item — Java UEBA codec `.toLowerCase`s the ADT branch name into a Java pattern-binding variable `if (value instanceof <Branch> <lower>)` (`JvUEBACodecGenerator.scala:182,199`) — VALIDATED by orchestrator; EXACT C# H1 mechanism, and `default` IS a Java keyword (same trigger word as C#). Java has NO `@`-escape syntax, so a keyword identifier must be RENAMED. Other verbatim sites: DTO record-component names + accessors (`JvDefnTranslator:385`, `JvJsonCodecGenerator:170`), package segments via `.toLowerCase` (`JvTypeTranslator:118,165`), enum constants via `EnumWireStyle.capitalize`, class names (`JvTypeTranslator:159`). The JSON codec ADT branch uses a fixed local `branchVal` (NOT lowercased), so that one site does not collide. No keyword/reserved/escape/sanitize references anywhere.
- **Issues:** Grep/Glob/codegraph unavailable to the explorer.
