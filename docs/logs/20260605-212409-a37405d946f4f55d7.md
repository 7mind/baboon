# Plan-reviewer session — goal G1 (SCOPE 5 D11-fix plan)

- Goal G1; plan-reviewer; verdict **revise** (review **R39**), 1 criticism; agent a37405d946f4f55d7
## Verbatim
- T34 faithful to D11 root cause + orchestration correct (bidirectional D11<->T34; T33 rewired dependsOn=[T32,T34] blocked->planned; DAG acyclic; T1-T32 untouched; M6 open).
- CRITICISM: T34 UNDER-SCOPES. The per-language MCP round-trip tests hard-code tool-COUNT (5) AND exact tools/list ORDER for the pre-processTagged 5-tool set (verified in test/cs-stub-mcp-overlay/McpTests/McpTests.cs: Assert.AreEqual(5, tools.Count) L242; exact tools[0..4] names L247-251; StubMcpTools impl of 5 methods L74-90). Adding ONLY the interface method clears CS0535 but the round-trip then FAILS at runtime on the count/order assertions. All 7 other statically-typed replicas mirror this T7 scenario; the Python overlay also fails if it asserts count. So T34's "python fails only if dispatched" framing is inaccurate. T34 must ALSO update the tool-count (5->6) + tools/list order assertions across ALL 9 overlays (insert processTagged at its correct declaration-order position).
- Did not open the other 8 overlay files individually (no Bash/Grep); grounded on the confirmed-broken C# overlay + the R3/K1 structural-replica mandate.
