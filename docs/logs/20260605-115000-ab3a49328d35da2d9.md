# Implement-worker session — task T20

- Task: **T20** — Expose --<lang>-generate-mcp-server in the playground (all 9)
- Role: worker (standard/sonnet)
- Returned status: **pass** — resultCommit `bedabb3d565c01e8a406873f2b09aa0445d49f87` (parent = main `b84bf3b4`)
- Agent id: ab3a49328d35da2d9

## Verbatim session summary
- **Did:** implemented task T20 in worktree implement/T20.
- **Achieved:** pass; resultCommit bedabb3d; `tsc --noEmit` zero errors (full `vite build` deferred — requires Scala.js artifact not buildable in-sandbox due to jgit/worktree limitation).
- **Discovered:** worktree initially on an older branch predating T6; created implement/T20 from correct b84bf3b4 base. **BaboonJS.scala already had `generateMcpServer = false` HARDCODED for all 9 language targets — T6 added the field to the options structs but did NOT expose it from JS.** Fix required adding `generateMcpServer: js.UndefOr[Boolean]` to all three JS option traits (JSCSOptions, JSScOptions, JSCommonLangOptions) and wiring `opts.generateMcpServer.getOrElse(false)` through (replacing the hardcoded false in all 9 createTargets cases).
- **Issues:** Full Scala.js recompilation (`sbt baboonJS/fastLinkJS`) not attempted (jgit NoWorkTreeException in worktrees). TS-side `tsc --noEmit` passed clean; BaboonJS.scala field name `generateMcpServer` matches both sides. Scala.js cross-compile to be verified via `sbt +compile` (orchestrator/T21).

## Files
filesTouched: baboon-playground/src/options.ts, baboon-playground/src/compiler.ts, baboon-compiler/.js/src/main/scala/io/septimalmind/baboon/BaboonJS.scala

## Orchestrator note
T20 modifies **BaboonJS.scala** — the documented cross-build trap (CLAUDE.md: a missed .js update fails CI on `sbt +compile`). Must verify `sbt +compile` (JVM+Scala.js) green before/at merge; reviewer + final T21 CI closeout cover this.
