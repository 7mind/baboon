# Investigate explorer — D7 / H9 (CONFIRMED)

- Defect D7 (Python recursive pydantic models NameError without future-import); Hypothesis H9; role: explorer; lean: **confirmed**; agent acb345f878fd62012
- Orchestrator validation: fix-site (renderTree preamble) consistent with the T18/R21 ledger record that a renderTree future-import was reverted for changing flag-off output. VALIDATED (corroborated by ledger).

## Verbatim
- PyBaboonTranslator.renderTree (PyBaboonTranslator.scala:297-299) builds the module = imports + class body; NO `from __future__ import annotations` (no __future__ token in the file).
- DTO fields emitted as BARE annotations `name: <type>` (PyDefnTranslator.scala:532-541), not quoted forward-refs.
- `opt[Tree]` -> `Optional[Tree]` with `Tree` a bare PyType token (PyTypeTranslator.scala:33-36, PyTypes.scala:145).
- Self-type filtered from imports (same module) and rendered as bare `Tree` (PyBaboonTranslator.scala:244-248, :301-303).
- No model_rebuild()/update_forward_refs(); shared runtime lacks the future-import (baboon_runtime_shared.py:5-19). Pydantic v2 evaluates self-ref annotations at model creation -> bare self-ref NameErrors on py3.12.
- SCOPE: regular/wrapped Python lanes scan the shared --model-dir (tests.md:78-85) and run python3 -m unittest; no checked-in Python golden. Emitting the future-import changes flag-off output (ledger R21/C1 reverted exactly this) — Q7 "fix all" approves the baseline change.
- FIX: emit `from __future__ import annotations` as the FIRST line of every generated Python module (recommend UNCONDITIONAL — fully correct via PEP 563, covers mutual recursion + ordering, no recursion-detection logic). Update the Python baseline.
- **lean: confirmed.**
