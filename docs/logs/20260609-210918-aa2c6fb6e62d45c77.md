# Implement worker log — T1 (wire-key contract + site inventory)

- Task: T1 (G1/D1)
- Role: implement-worker (opus/frontier)
- Returned status: pass — resultCommit 3abab2d637fa8d01e86cde1ee84eec5d4433655b

### Session summary
- **Did:** implemented T1 — committed design doc `docs/drafts/20260609-g1-wire-key-contract-and-site-inventory.md`. No source files changed (design/doc task on a Scala/sbt repo, no build).
- **Achieved:** pass. Doc covers Part 1 (wire-key invariant for the 4 no-escape RENAME backends) + Part 2 (per-backend identifier-emission site inventory with file:line citations across all 9 backends). All citations re-verified against source (worktree HEAD ba30bee3).
- **Discovered:** (1) Java's hand-rolled codec ALREADY emits the model-name string literal as the JSON wire key (`obj.set("<f.name.name>", ...)`), so `@JsonProperty` is needed only for non-codec Jackson consumers, not the codec itself. (2) Python has TWO wire-key paths — transparent pydantic `model_dump_json` derives the key from the attribute name (rename needs `Field(alias=...)`), while the explicit-walker path emits a literal. (3) UEBA rename-neutrality confirmed (ADT discriminator = branch index CSUEBA:201/218; DTO fields positional CSUEBA:285). (4) Rust `#[serde(rename)]` (RsDefnTranslator:1066-1073) is the canonical wire-key-preservation precedent.
- **Issues:** none. Orchestrator should lock the `decisions` item from the doc after merge.
