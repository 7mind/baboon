# Implement-flow session log

- **Task:** T2 — Lock the MCP scope + protocol-mapping decision
- **Milestone:** M2
- **Role:** implement-worker (round 0)
- **Agent id:** aef410a29233b5d63
- **Returned status:** `pass` — resultCommit `3c9f08c0`, branch `implement/T2`; created decisions item **K4**
- **Files touched:** docs/research/mcp-protocol-contract.md

---

### Session summary
- **Did:** implemented task T2 in worktree branch `implement/T2` — wrote and committed the language-agnostic MCP v1 protocol contract design doc and created the locked `decisions` ledger item.
- **Achieved:** pass; resultCommit `3c9f08c0f846847868f1f6a179b5ec853c6deb57`; no build check applicable (design/decision task — verified internal consistency against acceptance criteria); created `decisions:K4` (status `locked`, M1, `ledgerRefs goals:G1`).
- **Discovered:** the decisions ledger already contained K1, K2, and K3 — K3 is the *inputSchema-emitter* decision (T4/T5), so the new item became K4 (cross-referenced to K1+K3 via `dependsOn`). The dispatched worktree was on its own throwaway branch at HEAD `e61a5068`, not the base; `implement/T2` existed but was held by the main shared checkout, which the worker released via `git symbolic-ref HEAD refs/heads/main` (both refs at the same commit, no tree mutation; main checkout dirty docs preserved).
- **Issues:** none blocking. Orchestrator verified post-hoc: main checkout is on `refs/heads/main` @ 8ada6961 with dirty docs intact.

Contract summary (K4): one server per Baboon service; method→one tool (1:1); `<service>_<method>` verbatim-cased tool naming w/ fail-fast collision; tools-only protocol (initialize/tools/list/tools/call); exact JSON-RPC 2.0 envelope shapes; two-channel error mapping (JSON-RPC -32600/-32601/-32602/-32603/-32700 vs tools/call isError:true mapping BaboonWiringError); explicit v1 exclusions (resources/prompts/completions/sampling/notifications/pagination). Grounded in JSON-RPC 2.0 + MCP 2025-06-18.
