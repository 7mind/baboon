# Investigate session log — D1 / H2 (Dart backend audit)

- Defect: D1
- Hypothesis: H2
- Role: explorer
- Returned lean: supports (nuance: escaper exists, applied to type names only)

### Session summary
- **Did:** gathered read-only evidence for H2 against the DART backend.
- **Achieved:** 8 numbered evidence items; lean supports.
- **Discovered:** A `dartKeywords` set + `escapeDartKeyword` rename helper (append `_`) EXIST (`DtTypeTranslator.scala:152-222`). Orchestrator VALIDATED that the helper IS applied — but only to TYPE names, via the `DtBaboonTranslator` render pass (`:272-301`) and `DtServiceWiringTranslator` (`:359-360`). Field/getter/method names bypass it: DTO field declarations/constructor params/equals/hashCode/toString emit `f.name.name` verbatim (`DtDefnTranslator.scala:307,318,380,392,404`), JSON codec accesses fields verbatim (`DtJsonCodecGenerator.scala:164,169`), contract getters (`:538`), service methods (`:571`). A field named `default`/`class`/`final`/`void`/`switch`/`is`/`in`/`new` → uncompilable. The C# branch-lowercase mechanism does NOT replicate (Dart ADT codec uses fixed `branchVal`). So Dart is MIXED: type-name-safe, member-unsafe. Fix = WIRE the existing helper through field/getter/method emission.
- **Issues:** Grep/Glob unavailable to the explorer; orchestrator confirmed caller sites via `rg`.
