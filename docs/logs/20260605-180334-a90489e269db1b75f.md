# Investigate explorer — D4 / H6 (CONFIRMED)

- Defect D4 (Scala: foreign FFancyStr imported but no bare symbol); Hypothesis H6; role: explorer; lean: **confirmed**; agent a90489e269db1b75f
- Orchestrator validation: ScJsonCodecGenerator.scala:550-556 re-read against source — bare `${targetTpe}_JsonCodec.circeDecoder` confirmed; codecName helper at :598-600 confirmed. VALIDATED.

## Verbatim
- mcp_stub.baboon:26-36 `foreign FFancyStr { scala = "java.lang.String" }` (Custom -> host primitive); :105-111 `submitComposite.in.fancy: FFancyStr`.
- Generated mcptools_submitcomposite.in.scala imports bare `FFancyStr` while field renders to `String` (host primitive) -> bare import unresolvable.
- Foreign emits only `_KeyCodec` (ScDefnTranslator.scala:505-541) + _JsonCodec/_UEBACodec; NO bare `object/type FFancyStr` (ScDefnTranslator.scala:476-482).
- ROOT CAUSE / FIX SITE: ScJsonCodecGenerator.getDecoder lines 550-556 interpolate bare `${targetTpe}_JsonCodec.circeDecoder` (string concat) -> import collector (ScBaboonTranslator.scala:232-263) harvests the bare `FFancyStr` ScType node.
- Correct sibling idiom: encode path uses `codecName(...)` (:386-392) -> single `_JsonCodec` ScType node (imported); the _KeyCodec path (:492-494) likewise. FIX: `q"${codecName(targetTpe)}.circeDecoder"` at 552/556.
- Overlay workaround FFancyStrShim.scala:3-21 confirms generated code doesn't compile without a bare `object FFancyStr`.
- **lean: confirmed.**
