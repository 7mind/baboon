# Implement batch log — T19/T21/T8/T4/T7 (worker + reviewer)

Batch dispatched under /cq:advance implement stage (turn 2026-06-10). All 6 workers ran in parallel; T20 returned a zero-change audit finding (already done). The 5 below merged.

## T19 — D5 Java FQ (worker a... / reviewer a3270dae86a9190bb) — pass / approve
Worker (cce1b150→32e43749): routed bare Object/String in JvDefnTranslator emptyRecordMethods equals/toString + identifier-toString through `.fullyQualified`. javac of reserved-words-ok 15→0 errors; 602 green; JVM+JS compile green. Reviewer: built compiler + ran test-gen-regular-adt + test-java-regular GREEN; FQ scoped to in-record-body sites, codec classes untouched. Filed low follow-up: enum `parse(String)` (JvDefnTranslator:494) shadows only if a model enum is named `String` (→ D8).

## T21 — D6 Kotlin client-stub (worker a95b2205 / reviewer a0eb347e47de3c1e2) — pass / approve
Worker (2e23cb64→f701d174): :864/:882 client-stub declarations routed through escapeKtKeyword (whole-token backtick for Json variant); transport string args raw. 602 green; byte-identical. Reviewer confirmed all 4 acceptance points.

## T8 — Rust self/super/crate + type/variant (worker a284d7e / reviewer acf59032631697aaa) — pass / approve
Worker (5cc4f448→aa034c78): nonRawEscapable={self,super,crate,Self} renamed `_`-suffix + #[serde(rename)]; extended rustKeywords (final etc.); escapeRustTypeName across 6 files. cargo build green; 602; serde round-trip confirmed. Reviewer independently built native + `mdl :test-rust-regular` GREEN under -D warnings; holder.rs confirms self_/super_/crate_ + serde rename. (Worker's Cargo.toml edition note was inaccurate — reviewer verified no real mismatch.)

## T4 — C# general keyword pass (worker a0f0f1d9 / reviewer a59195e44a21368b4) — pass(scope) / approve
Worker (8162ade0→357102b6): escapeCsKeyword routed through all model-derived C# identifier sites (8 files); wire keys preserved; byte-identical to baseline. Acceptance's dotnet-build-green is blocked by the SEPARATE D7 (System.Type shadowed by `data Type {}`; C# sibling of D3/D5) — filed. Reviewer approved T4's scope; verified wire-key preservation + base/baseRef local-stem split; D7 is the out-of-scope blocker.

## T7 — Swift case-name escape (worker a1305199 / reviewer a81b2880eedc78447) — pass / approve
Worker (27193b5d→427dfa42): ADT-branch + enum-case names routed through escapeSwiftKeyword at the 3 cited sites; JSON discriminator strings + UEBA ordinals unchanged; +2 supporting fixes (metatype dot-shorthand for case `Type`; `_self` init-param for field `self`). swift build green. Reviewer approved; both supporting fixes sound/necessary/guarded. Filed medium follow-up: reserved-words-ok has no CI compile lane → already covered by planned task T13.
