# Implement review log — T9 (round 1)

- Task: T9 (Java reserved-word rename + wire-key preservation)
- Role: implement-reviewer (opus, single-reviewer fallback)
- Verdict: approve — 0 criticisms, 0 questions, 1 out-of-scope defect (low)

### Session summary
- reviewed the 4 Java translator source files at 68e42024; ignored the stale-base reserved.baboon + emission test.
- `escapeJvKeyword` = full JLS §3.9 set + true/false/null + contextual keywords; applied at record components (JvDefnTranslator:384), contract methods (:559), service methods (:607), identifier accessor (:737), JSON DTO accessor (JvJsonCodecGenerator:170), UEBA DTO accessor (:355), UEBA ADT branch capture (:182). WIRE KEY PRESERVED: encoder `obj.set("${f.name.name}", ...)` + decoder `jsonObj.get(...)` use ORIGINAL model name; positional record constructor decouples escaped components. Class/enum type leaf names `.capitalize`-d so can't collide with lowercase keywords. Identity for non-keywords (id→id). Worker-reported sbt baboonJVM/test 605/605 green.
- Out-of-scope defect filed (low): ADT-name package segment in renderOwner (JvTypeTranslator:166) not keyword-escaped — latent edge, ADT names conventionally capitalized; route through escapeJvKeyword in the general pass.
