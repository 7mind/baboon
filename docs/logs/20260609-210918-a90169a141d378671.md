# Implement worker log — T2 (reserved-word test model)

- Task: T2 (G1/D1)
- Role: implement-worker (sonnet/standard)
- Returned status: pass — resultCommit 0b3d5c73fe897f0b77f9567a12c9a530b8e3717a

### Session summary
- **Did:** implemented T2 — added `baboon-compiler/src/test/resources/baboon/reserved-words-ok/reserved.baboon`. Verified via the clone-to-real-dir workaround (sbt-git can't build in a linked worktree): cloned, ran the JVM compiler with `--generate-{json,ueba}-codecs-by-default=true`, compiler exit 0 ("C#: done", retained 12 definitions).
- **Achieved:** pass; resultCommit 0b3d5c73. enum KindEnum (Type/Object/Default/Is/In members), adt AvatarItem (8 keyword-named branches: Default/Type/Object/Class/When/Match/Is/In), root data Holder (17 keyword-named fields spanning all backends' reserved words).
- **Discovered:** Baboon's `fieldName`/`enumMemberName` rules use `idt.symbol` with NO keyword exclusion — all target-language keywords (import/type/default/class/etc.) are valid Baboon identifiers; no names rejected by the parser. `true`/`false` field names conservatively omitted (potential bool-literal parse conflict, untested).
- **Issues:** Worktree-placement quirk — the harness placed the worker in `worktree-agent-a90169a141d378671` but it also touched the pre-existing stale `implement/T2` worktree; committed the model there (resultCommit 0b3d5c73). Merge by SHA.
