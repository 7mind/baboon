# Investigate session log — D1 / H2 (TypeScript backend audit)

- Defect: D1
- Hypothesis: H2
- Role: explorer
- Returned lean: supports (nuance: escaper exists but is dead code)

### Session summary
- **Did:** gathered read-only evidence for H2 against the TypeScript backend.
- **Achieved:** 9 numbered evidence items; lean supports.
- **Discovered:** A reserved-word escaper `escapeTsKeyword` EXISTS (`TsTypeTranslator.scala:310-319`) but is NEVER applied at any identifier-emission site — VALIDATED by orchestrator: zero callers tree-wide (dead code). Model `.name` flows verbatim (or `.capitalize`/`.toLowerCase`) into DTO field getters/params, UEBA `const <field>` local (`TsUEBACodecGenerator.scala:169` — closest C# analog), enum members, type/class/interface symbol names, ADT branch symbol names, service method names. ADT branch dispatch uses `instanceof`/numeric index, NOT a lowercased local. TS-native fix: bindings/types/enum-members must be RENAMED (no escape syntax); only property access can use bracket-string form.
- **Issues:** Grep unavailable to the explorer; orchestrator confirmed dead-code claim via tree-wide `rg`.
