# Implement-flow session log

- **Task:** T7 — Define the shared language-neutral MCP round-trip scenario
- **Milestone:** M3
- **Role:** implement-worker (round 0)
- **Agent id:** a77ba78a93ede5ead (worktree agent a77ba78a93ede5ead)
- **Returned status:** `pass` — resultCommit `69d2a2fb` (parent 60b6a26e), branch `implement/T7` / worktree `worktree-agent-a77ba78a93ede5ead`
- **Files touched:** docs/research/mcp-roundtrip-scenario.md

---

### Session summary
- **Did:** implemented task T7 in worktree branch `implement/T7`
- **Achieved:** pass; resultCommit `69d2a2fb589454e379f51792252749020c0074c6`; check: documentation-only task — validated expected values derived from `McpInputSchemaEmitter.scala`, `OasTypeTranslator.scala`, and `McpInputSchemaEmissionTest.scala` golden assertions; no compiler sources modified
- **Discovered:** The `$defs` key ordering in `processShape`'s schema has top-level types (`Shape`, `Tree`) before ADT-branch types (`Shape_Circle`, `Shape_Rect`) because `TypeId.toString` uses `/:` prefix for top-level owners (ASCII 58) and `/[` for ADT-owned types (ASCII 91), so colon sorts before bracket. The doc notes this and instructs tests to use key-lookup comparison, not order-sensitive.
- **Issues:** The `tools/call` test assumes stub handlers return `{"ok":true}` — documented as a convention the per-language overlay tests implement consistently, not a wire-level requirement from the stub model. Also: `PointPage` schema name is `mcp_stub_PointPage` (the alias name), NOT `mcp_stub_Page` — the generic template `Page[T]` is instantiated under the alias name.

**Deliverable:** `docs/research/mcp-roundtrip-scenario.md` — exact 5 tool names, concrete inputSchema JSON for all 5 tools, 2 successful tools/call examples (ping, submitComposite), 2 error paths (Channel A -32602 unknown tool, Channel B isError:true DecoderFailed), and an assertion-discipline section (unconditional asserts, liveness/negative-control, K1 validation tiers).
