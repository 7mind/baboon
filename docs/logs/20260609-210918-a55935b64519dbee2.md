# Implement worker log — T9 (Java reserved-word rename + wire-key preservation)

- Task: T9 (G1/D1)
- Role: implement-worker (sonnet/standard)
- Returned status: pass — resultCommit 68e42024 (+eaf4fd48); source merged to main as 137bf305

### Session summary
- **Did:** added `JvTypeTranslator.escapeJvKeyword` (full JLS §3.9 keyword set) and applied it to DTO record components, contract/service method names, identifier-toString accessor, DTO field accessors in JSON+UEBA codecs, and the UEBA ADT branch pattern variable. Wire-key string literals in `obj.set(...)`/`jsonObj.get(...)` left unchanged (T1 contract).
- **Achieved:** pass. sbt baboonJVM/test 605/605 green (incl. 3 new emission-test cases); javac --release 21 of all generated Java = 0 errors. `default` field → `default_` component + `value.default_()` accessor; `obj.set("default", ...)` wire key unchanged; UEBA branch capture `instanceof ... default_`.
- **Discovered:** baboon model syntax uses `root data`/`data BranchName {}`. JSON codec is hand-rolled (no reflective Jackson), so wire-key preservation needs no `@JsonProperty` — string literals are inherently independent of the Java identifier. UEBA positional → rename-neutral. The `default` switch label is legal Java (keyword as label, not identifier) — no handling needed.
- **Issues:** worktree cut from stale base → worker's reserved.baboon variant + emission test were NOT merged (orchestrator kept T2's canonical model); only the 4 translator source files merged. Matrix coverage of keyword model lands in T13.
