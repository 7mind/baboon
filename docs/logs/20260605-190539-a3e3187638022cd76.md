# Implement-worker — T26 (D1 verify)

- Task T26; role worker (sonnet); status **pass**; resultCommit `26fbd79c` (parent f215f8de); agent a3e3187638022cd76
## Verbatim
- Added contract-bearing ADT `Tagged`/`HasId` (id:str) + `processTagged` to mcp-stub-ok/mcp_stub.baboon; added empirical lock test in McpInputSchemaEmissionTest asserting each branch's $defs properties include the contract field WITHOUT allOf; updated K1 size guards 5->6 in ScalaMcpServerEmissionTest + PyMcpServerEmissionTest.
- EMPIRICAL RESULT: H3 CONFIRMED REFUTED — TagA $defs = properties{id,tag} required[id,tag]; TagB = properties{id,weight}... both branches already carry contract field `id` with no allOf. NO generator fix needed (merge happens in BaboonTranslator:289-306 adtContracts -> branch finalFields).
- sbt +compile green; new/updated 10 MCP tests pass; baboonJVM/test 592 pass / 1 pre-existing RTCodecTest failure (needs mdl :test-cs-regular external setup, unrelated).
- filesTouched: mcp_stub.baboon, McpInputSchemaEmissionTest.scala, ScalaMcpServerEmissionTest.scala, PyMcpServerEmissionTest.scala. D1 resolved (behavior was correct).
