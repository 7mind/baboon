# Implement-flow session log

- **Task:** T4 — Review openapi JSON-Schema emitter; decide tool inputSchema strategy
- **Milestone:** M2
- **Role:** implement-worker (round 0)
- **Agent id:** a1fdda4c72e5d6185
- **Returned status:** `pass` — resultCommit `ddbbc633`, branch `worktree-agent-a1fdda4c72e5d6185` (parent e61a5068); created/locked decisions item **K3**
- **Files touched:** docs/research/mcp-inputschema-gap-analysis.md

---

### Session summary
- **Did:** implemented task T4 — reproduced the OpenAPI JSON-Schema emitter's behaviour against the full Baboon type system used as MCP tool request types, wrote the gap-analysis doc, and locked the inputSchema strategy decision.
- **Achieved:** pass; resultCommit `ddbbc633142c702c29aa6ab8fa45f4f8ec675ce9`; verification = OpenAPI backend ran green on a purpose-built model and emitted well-formed JSON (json.tool exit 0), all 12 constructs captured. Locked decision **K3** under milestone M1.
- **Discovered:** (1) Despite the emitter doc-comment saying services are "skipped", the typer reifies each method's inline `data in` block into a standalone DTO `<Pkg>_<Service>_<method>_in`, so the per-tool request schemas ARE emitted. (2) The decisive gap: those request schemas use bare `$ref` into `#/components/schemas`, which is dangling in a standalone MCP `inputSchema` (no shared registry on the MCP wire) — 3 of 4 tool requests in the repro affected. (3) Foreign types with language mappings but no Baboon→Baboon `rt` emit opaque `{type:object}` even when they map to `string` everywhere. (4) `map[Point,str]` is rejected by the typer — multi-field DTO keys are ineligible; only single-primitive wrappers, id types, enums, foreign types are legal keys (used `map[Color,str]` instead).
- **Issues:** none. The prebuilt native binary used for repro was from commit `e61a5068` (parent of the T4 base `8ada6961`); the `translator/openapi/` sources are byte-identical between the two, so the captured output is faithful to the base. The repro model lived at `/tmp/t4-repro/` (outside the repo) and is reproduced verbatim in §6 of the committed doc.
- **Locked strategy:** build a DEDICATED shared language-agnostic MCP inputSchema emitter (reusing OAS fragment logic with a self-contained per-tool `$defs` closure), NOT extend the OpenAPI emitter — grounded in the reproduced dangling-`$ref` gap. Drives T5.
