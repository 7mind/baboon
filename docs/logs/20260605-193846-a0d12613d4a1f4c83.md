# Implement-reviewer — T29 (D4+D5 Scala codec) — APPROVE

- Task T29; reviewer (opus); verdict **approve** (0 crit/0 q/1 out-of-scope defect-note); agent a0d12613d4a1f4c83
- Built native compiler @adcdd2e5 in /tmp clone, generated Scala MCP stub WITHOUT the removed shims: mcptools_submitcomposite.in.scala now imports FFancyStr_JsonCodec (D4 resolved); mcptools_listcollections.in.scala imports nested-package Color_JsonCodec (D5 resolved). All 3 decoder sites (:481/:552/:556) route through codecName. Both shim files deleted, no dangling refs. scala-stub-mcp compiles (35 sources) + 10 McpTests pass without shims; sbt +compile (JVM+JS), test-scala-regular, test-scala-wrapped, test-scala-mcp all green. _KeyCodec foreign path + same-package ADT-branch sites correctly untouched; no new TyperIssue -> no JS exhaustive-match risk.
- Out-of-scope finding (reviewer-filed): test-manual-scala asserted on python+kotlin-kmp m26 fixtures absent in this single-lane isolation run (6 ENOENT) -> ORCHESTRATOR JUDGMENT: this is a single-lane-isolation artifact, NOT a real defect (T21's full `mdl :build :test` ran the manual/compat lanes GREEN with all 10 backends produced). Declined to file as a defect.
