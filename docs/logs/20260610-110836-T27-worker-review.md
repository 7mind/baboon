# T27 (D11 editor-grammar gap) — worker + reviewer

- Task: T27 (G5/D11) — worker opus (a56e218a) — pass, resultCommit 98e026e6 (merged 8b15da11)
- Reviewer opus (a1086dfcf40d0765c) — approve, 0 findings

Worker: reproduced FAIL reserved.baboon (ERROR[45,4], `is` field). Discovered grammar.js is in a DOUBLY-NESTED submodule (baboon-zed → baboon-zed-grammar), outside this repo's commit scope + harness-uneditable → options (a)/(b) not viable. Chose option (c): added `-not -path '*/reserved-words-ok/*'` to the test-tree-sitter.sh find collection + rationale comment (best-effort editor grammar; fixture stays in shared model-dir for T13 codegen; D9 mcp-stub-ok precedent). test-editors exits 0 (corpus 47/47, real 80/80).
Reviewer: VERIFIED submodule claim (.gitmodules nesting; git ls-files returns nothing for grammar.js in parent repo) → (c) is the correct in-scope resolution; exclusion scoped to editor find ONLY (no mdl/codegen lane touched), matches exactly one fixture (80 vs 81), reserved-words-ok remains in shared model-dir; rationale documented; test green 80/80. Optional non-blocking follow-up: if baboon-zed-grammar submodule later re-admits keyword-named identifiers (option a), remove the exclusion.
