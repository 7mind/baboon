# Implement-worker — T28 round 1 (D3 criticism re-work)

- Task T28; role worker (opus); status **pass**; resultCommit `2c303c90f1b20406aaf72d153dba3400d49f3dea` (parent f215f8de); agent abb08bed3f14a8837
## Verbatim
- CRITICISM 1 fixed: renderTree typesByModule now sorts by the rendered `from '...'` source string via a new renderedImportSource(moduleId) helper dispatching identically to mkImportFor; per-branch path computation extracted to definitionImportPath/fixtureImportPath/baboonTypeImportPath so emitter + sort key share one source of truth. Removed a stray NUL-byte mkString tiebreaker -> mkString("/").
- CRITICISM 2 fixed: replaced the vacuous two-run byte-identity sub-test with a single-translation canonical-order check (lines sorted by rendered source path; symbols by base name within braces, preserving brace padding).
- Verification: +compile (JVM+Scala.js) green; 17 TS Scala emission tests green; determinism test both sub-tests pass post-fix and FAIL pre-fix for the right reasons on m30/sc/docs/doc-crud/client.ts — module order (../DocResp before ../DocReq) AND symbol order (_UEBACodec before _JsonCodec) — live + non-vacuous for both properties.
- Discovered: facade uses `import { a, b }` (spaces) vs renderTree `import {a, b}` (no spaces) — canonicalLine preserves inner padding to avoid false positives. npm TS lanes not runnable in sandbox (no network); change is tsc-inert (reorder only).
- filesTouched: TsBaboonTranslator.scala, TypescriptImportDeterminismTest.scala.
