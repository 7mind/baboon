# Investigate session log — D1 / H2 (Rust backend audit)

- Defect: D1
- Hypothesis: H2
- Role: explorer
- Returned lean: contradicts (broad) / narrow residual gap

### Session summary
- **Did:** gathered read-only evidence for H2 against the RUST backend.
- **Achieved:** 9 numbered evidence items; lean contradicts (Rust HAS reserved-word escaping infrastructure applied to field/fn/module paths; a narrower residual gap exists on type/variant names).
- **Discovered:** `RsDefnTranslator` defines `rustKeywords` (L1491-1529), `isRustKeyword`, `escapeRustKeyword` (`r#` prefix, L1533-1535), `escapeRustModuleName` (`_` suffix / `in`→`input`, L1540-1546). `toSnakeCase` wraps raw snake-case in `escapeRustKeyword`, so field names (L664,683,690), contract methods (L1428), service methods (L1460) ARE escaped; module/file names via `escapeRustModuleName`. The C# precise mechanism does NOT reproduce: Rust ADT serde binds branches to fixed `v` (L1320,1333) and `default` is not a Rust keyword. Residual gap: type/struct/enum/trait names emitted as bare `tid.name.name.capitalize` (`RsTypeTranslator` L155); enum/ADT variant names use `.capitalize` un-escaped. Because Rust keywords are lowercase (except `Self`), `.capitalize` dodges most — but a model identifier `self`→`Self` is illegal AND cannot be fixed with `r#` (web-confirmed: `crate`/`self`/`super`/`Self` are not raw-identifier-able).
- **Issues:** Glob/Grep unavailable; did not open every codec-impl file.
