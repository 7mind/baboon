# Investigate session log — D1 / H2 (Swift backend audit)

- Defect: D1
- Hypothesis: H2
- Role: explorer
- Returned lean: mixed

### Session summary
- **Did:** gathered read-only evidence for H2 against the SWIFT backend.
- **Achieved:** 8 numbered evidence items; lean mixed.
- **Discovered:** Swift HAS a `swiftKeywords` set + `escapeSwiftKeyword` backtick-quoter (`SwTypeTranslator.scala:283-345`), applied (a) as a CENTRALIZED render-pass over every `SwType`/`SwTypeName` leaf (`SwBaboonTranslator.scala:331-340`) and (b) per-site at DTO field/init/contract/identifier/service-FQ sites — so the literal "no escaping anywhere" framing is contradicted for type/field names. BUT ADT branch case names and enum case names are computed as RAW Scala Strings (`branchName.head.toLower.toString + branchName.tail`; `EnumWireStyle.wireName`) and interpolated directly into the TextTree, bypassing the escaper: ADT branch cases (`SwDefnTranslator.scala:889-894`; `SwJsonCodecGenerator.scala:109-128`; `SwUEBACodecGenerator.scala:150-178`) and plain-enum cases (`SwDefnTranslator.scala:824-828`). A branch named `Default` emits `case default(...)` — illegal. Fix = route case names through `escapeSwiftKeyword`.
- **Issues:** Grep/Glob/Bash unavailable; did not exhaustively read wiring/conversion/fixture translators.
