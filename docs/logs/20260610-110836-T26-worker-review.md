# T26 (D12 harness fix) — worker + orchestrator review

- Task: T26 (G5/D12) — worker haiku (a65c6efd) — pass, resultCommit 76df6fdb (merged b24e491f)
- Review: orchestrator-adjudicated (one-line verified change; go-ahead)

Worker appended `|| true` to the `output=$(tree-sitter parse "$f" 2>&1)` capture (test-tree-sitter.sh:46). Verified: `bash test/editors/test-tree-sitter.sh .` now runs to completion — "Real files: 80/81 passed; FAIL: reserved-words-ok/reserved.baboon; Summary printed; exit 1" — instead of silently aborting. Diff is exactly the one capture line (confirmed by orchestrator `git show`). This both fixes D12 AND empirically bounds D11 to the single reserved.baboon file (80/81 others parse clean).
