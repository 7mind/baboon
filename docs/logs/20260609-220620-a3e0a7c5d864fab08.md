# Implement worker log — T6 (Kotlin backtick keyword-escaping)

- Task: T6 (G1/D1)
- Role: implement-worker (sonnet)
- Status: pass — resultCommit 721fa198 (source merged to main)

### Session summary
- Added `KtTypeTranslator.escapeKtKeyword` (backtick-quote 28 Kotlin hard keywords; identity for non-keywords) routed at: DTO field params + contract field decls, service method names, JSON codec field accessors + named ctor args, UEBA ADT branch capture var + fieldsOf accessor/decode, conversion source-field/local-val/ctor-arg. Covers kt-stub + kt-stub-kmp (shared generator). Files: KtTypeTranslator, KtDefnTranslator, KtJsonCodecGenerator, KtUEBACodecGenerator, KtConversionTranslator.
- Verified (clone-build): `sbt +compile` green; `sbt baboonJVM/test` 602/602; generated Kotlin backtick-escapes `object`/`is`/`when`/`val`/`in`/`class`/`super`/`true`/`false`; Jackson wire keys unescaped.
- Discovered orthogonal defect (filed D3): ADT branch `Class` shadows `java.lang.Class` in `baboonAdtType: Class<*>` metadata field → ~32 AvatarItem.kt errors. NOT keyword-related, NOT caused by T6.
