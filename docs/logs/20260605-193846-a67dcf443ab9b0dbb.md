# Implement-reviewer — T31 (D7 Python future-import) — APPROVE

- Task T31; reviewer (opus); verdict **approve** (0 crit/0 q/0 def); agent a67dcf443ab9b0dbb
- Built compiler @12e0807b in /tmp clone, generated Python from recursive-ok under py3.12 + pydantic 2.11.7: `from __future__ import annotations` is LINE 1 of all 13 generated modules (DTOs, codecs, facade, conversions, verbatim runtime resources, __init__) and all py_compile clean (would SyntaxError if misplaced). Emission unconditional via the single renderTree path. Pre-fix repro: `NameError: name 'Tree' is not defined` at `left: Optional[Tree]`; post-fix imports + constructs nested instance cleanly — future-import is the sole delta. Recursive round-trip test non-vacuous (nests both recursive field kinds). Shim fully removed from .mdl/defs/tests.md (0 refs). Scope confined to 3 files. (A facade encode_to_json domain_version AttributeError in isolated single-model generation is a one-model-dir artifact, independent of T31; the import — the D7 fix — succeeds.)
