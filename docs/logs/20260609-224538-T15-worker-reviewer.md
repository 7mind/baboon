# T15 (D3 Class-shadowing fix) — worker + reviewer

## Worker (opus, agent a5811308916d38b59) — pass, resultCommit 6d8e82cb (merged e4cc800b)
Made the per-ADT `baboonAdtType` metadata field render fully-qualified `java.lang.Class` at the emission site in all 3 JVM-family domain-tree-tools via `.fullyQualified` (Scala `_root_.java.lang.Class[?]`, Kotlin `java.lang.Class<*>`, Java `java.lang.Class<?>`). Shared `javaClass` predef unchanged (immutable `.copy(fq=true)`), so other call sites keep short form. Java A/B compile: short→8 `Class does not take parameters` errors, FQ→0. sbt baboonJVM/test 602 green. C#/non-JVM untouched. Discovered sibling defect: `data Object {}` shadows java.lang.Object in Java `equals(Object)` (→ filed D5).

## Reviewer (opus, agent ac9f69bf8c43e4caa) — approve, 0 criticisms; filed 1 out-of-scope defect (D5)
Verified all 3 emission sites call `.fullyQualified`; renderers emit full pkg for fq=true ignoring predef; `javaLangPkg` carries no `_root_` prefix so Scala `_root_.java.lang.Class` is a single valid FQ ref (no double-prefix); predefs unchanged so no other call site regresses; fq/predef types excluded from import blocks. Object-shadowing sibling out of scope → D5 (high).
