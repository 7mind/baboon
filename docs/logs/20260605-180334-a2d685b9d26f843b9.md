# Investigate explorer — D5 / H7 (CONFIRMED)

- Defect D5 (Scala: Color_JsonCodec referenced but not imported in nested-package codec); Hypothesis H7; role: explorer; lean: **confirmed**; agent a2d685b9d26f843b9
- Orchestrator validation: ScJsonCodecGenerator.scala:472-481 re-read — bare `${targetTpe}_JsonCodec.instance.decode` confirmed at :481; same root-cause family as D4. VALIDATED.

## Verbatim
- Map-with-enum-key JSON decoder (getKeyDecoder enum branch, ScJsonCodecGenerator.scala:472-481) references `${targetTpe}_JsonCodec.instance` by bare string-concat on the bare enum ScType (`Color`).
- Import set computed ONLY from ScValue.ScType nodes in the tree (ScBaboonTranslator.scala:231-239); a string-suffix symbol is invisible -> no import for `Color_JsonCodec`. Bare `Color` IS a node -> imported; `Color_UEBACodec` imported because UEBA uses codecName (ScUEBACodecGenerator.scala:503-508/666-668) -> the asymmetry.
- Final render drops pkg qualification for non-fq ScType (ScBaboonTranslator.scala:283-290) -> bare `Color_JsonCodec` renders unresolved from the nested package.
- FIX SITE: route ScJsonCodecGenerator.scala:481 through the existing `codecName` helper (:598-600), mirroring UEBA + the JSON encode path. Same fix idiom as D4 (lines 552/556); D4+D5 are ONE coherent fix.
- **lean: confirmed.**
