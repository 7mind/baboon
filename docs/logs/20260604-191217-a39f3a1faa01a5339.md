# Implement-flow session log

- **Task:** T22 — Author the MCP test stub model + lock its type inventory
- **Milestone:** M2
- **Role:** implement-worker (round 0)
- **Agent id:** a39f3a1faa01a5339
- **Returned status:** `pass` — resultCommit `484e1617` (parent de570ae6), branch `worktree-agent-a39f3a1faa01a5339`; created decisions item **K6**
- **Files touched:** baboon-compiler/src/test/resources/baboon/mcp-stub-ok/mcp_stub.baboon

---

### Session summary
- **Did:** implemented task T22 in worktree branch `worktree-agent-a39f3a1faa01a5339`, based on commit `de570ae6` (T1/T2/T3/T4 base).
- **Achieved:** pass; resultCommit `484e16177e402b65c9924eaf6760bb69859aa970`; model compiles clean under all 9 regular-adt language backends (cs/scala/python/rust/typescript/kotlin/java/dart/swift) and the OpenAPI backend — zero typer/validator errors, 30 retained definitions, 12 unreachable; decisions ledger item K6 created and locked.
- **Discovered:** (1) `map[Point,str]` with a multi-field DTO key is rejected by the typer — the legal non-string key form is an enum key (`map[Color,str]`), confirmed in the T4 gap analysis. (2) The worktree was initially on `e61a5068` rather than `de570ae6`, requiring a `reset --hard` to the correct base before any work. (3) The prebuilt GraalVM native binary at `baboon-compiler/.jvm/target/graalvm-native-image/baboon` is usable from any worktree.
- **Issues:** none — model compiles cleanly and all 11 type-inventory constructs are exercised across the 5 service methods.

**Model:** root service `McpTools` with 5 methods (listCollections, submitComposite, processShape, pagePoints, ping); request DTOs exercise all 11 constructs in the K6 locked inventory: DTOs (plain+nested), ADT (Shape: Circle/Rect), enum (Color), list, set, map[str,V], map[Color,V] (enum key), opt, foreign type (FFancyStr), type alias (PointPage=Page[Point]), recursive DTO (Tree).
**Check:** `baboon --model-dir .../mcp-stub-ok/ :cs :scala :python :rust :typescript :kotlin :java :dart :swift` (regular-adt flags) → all 9 backends + OpenAPI done, zero errors.
