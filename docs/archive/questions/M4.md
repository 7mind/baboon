## M4

### Q6 — answered

- createdAt: 2026-06-05T01:03:54.761Z
- updatedAt: 2026-06-05T08:56:52.819Z
- author: user
- session: 9fa3b56d-80fc-4604-bf8e-7daadaf5957b
- question: "Swift MCP (T17) is blocked by defect D8: the shared MCP stub model's recursive value-type DTO `Tree` does not compile under Swift (the other 8 languages do). The Swift MCP generator itself is complete and correct (sbt +compile green, JVM shape test green, codegen matches the T7 reference); the only failing gate is `swift build` of the stub model's recursive `Tree`. Unblocking requires a deliberate decision because the fix changes generated Swift for ALL recursive-DTO users (an external/public-API representation change). How do you want to proceed?"
- context: "D8 root cause: SwDefnTranslator emits recursive Baboon DTOs as Swift value-type structs without indirection -> infinite-size value type, rejected by swiftc. Swift `indirect` is enum-only, so no harness/overlay workaround exists (unlike the Scala/Java/Python pre-existing-gap shims). A proper fix (reference box / final-class wrapper or indirect-enum encoding in SwTypeTranslator/SwDefnTranslator + init/Codable/Equatable/Hashable updates + full Swift-matrix regression) is a substantial, design-laden generator task that alters generated Swift output for every recursive DTO (baboon's generated code is a versioned external API). 8/9 MCP backends (ts, cs, scala, rust, kotlin, java, dart, python) are merged + verified; Swift is the only one outstanding. T17 blocks the W4 integration tasks (T19 aggregator registration, T20 playground, T21 CI closeout) because they dependsOn all per-language replicas incl. T17."
- suggestions: ["Fix Swift recursive-value-type DTO codegen now (new task: box recursive DTOs via reference/final-class or indirect-enum, regress the full Swift matrix), then re-dispatch T17 -> ship MCP for all 9 languages","Defer Swift MCP to a follow-up goal: mark T17 abandoned for v1, drop --swift from the v1 MCP scope, and let T19/T20/T21 proceed for the other 8 languages (ship 8/9 now, Swift later behind the same flag once D8 is fixed)","Keep T17 blocked and pause the whole feature here until the Swift recursive-DTO fix lands (no partial ship)"]
- recommendation: "Defer Swift MCP to a follow-up (suggestion 2): D8 is a substantial, external-API-affecting Swift-generator change well beyond the MCP feature's planned scope, and 8/9 backends are already complete + verified. Shipping MCP for 8 languages now (with Swift gated off until D8 is fixed and T17 re-run) delivers the feature without entangling it with a risky core-Swift-codegen redesign. If you prefer full-9 parity in this goal, pick suggestion 1 and I'll plan the Swift recursive-DTO fix as a prerequisite task."
- ledgerRefs: ["tasks:T17","defects:D8","goals:G1"]
- answer: "Fix Swift recursive-value-type DTO codegen now (new task: box recursive DTOs via reference/final-class or indirect-enum, regress the full Swift matrix), then re-dispatch T17 -> ship MCP for all 9 languages"
