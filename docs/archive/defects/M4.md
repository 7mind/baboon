## M4

### D4 — resolved

- createdAt: 2026-06-04T22:41:06.312Z
- updatedAt: 2026-06-05T20:11:24.371Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "Scala codegen: foreign type FFancyStr imported but no bare symbol emitted (pre-existing)"
- description: "Filed by the T12 reviewer (round 0, out-of-scope, PRE-EXISTING — reproduced by generating mcp-stub-ok with the regular Scala flags WITHOUT --scala-generate-mcp-server). Generated mcptools_submitcomposite.in.scala (package mcp.stub.mcptools.submitcomposite) imports `mcp.stub.{... FFancyStr ...}`, but the generated FFancyStr.scala only defines FFancyStr_JsonCodec/_KeyCodec/_UEBACodec objects — no bare `object FFancyStr` or `type FFancyStr`, so the import does not resolve. Independent of MCP; surfaced authoring the Scala MCP test overlay (worked around with a FFancyStrShim). Triage: candidate for separate fix or wontfix; does not affect MCP feature correctness."
- severity: medium
- suggestedFix: "In ScJsonCodecGenerator.getDecoder (lines 552 and 556), wrap the codec reference with the generator's existing `codecName` helper (:598-600): `q\"${codecName(targetTpe)}.circeDecoder\"`, so the emitted node is a single `FFancyStr_JsonCodec` ScType (resolvable, imported) rather than the bare `FFancyStr` + text suffix. This matches every sibling site (encode :386-392, UEBA, the _KeyCodec idiom :492-494). Then remove the FFancyStrShim test workaround. NOTE: shares the exact fix idiom with D5 (route get(Key)Decoder through codecName) — fold D4+D5 into ONE Scala-codec task."
- ledgerRefs: ["tasks:T12","goals:G1"]
- rootCause: "Confirmed (H6, source-validated). ScJsonCodecGenerator.getDecoder (ScJsonCodecGenerator.scala:550-556) interpolates the codec reference as bare string-concat `q\"${targetTpe}_JsonCodec.circeDecoder\"` where targetTpe is the bare foreign ScType (FFancyStr). The import collector harvests imports ONLY from ScType nodes in the rendered tree (ScBaboonTranslator.scala:232-263), so it imports the bare `FFancyStr` (which has NO companion — a Custom foreign emits only _KeyCodec/_JsonCodec/_UEBACodec, ScDefnTranslator.scala:476-482/:505-541), producing an unresolvable `import mcp.stub.{... FFancyStr ...}`. The field itself renders to the host primitive (String), so the bare FFancyStr is never legitimately needed. Worked around in tests by FFancyStrShim.scala."
- sessionLogs: ["docs/logs/20260605-180334-a90489e269db1b75f.md"]
- dependsOn: ["T29"]
- fix: "Resolved by T29 (commit 390fec58): ScJsonCodecGenerator.getDecoder now routes the codec reference through the codecName helper (sites :552/:556) so it emits a real imported FFancyStr_JsonCodec ScType instead of leaking the bare companion-less foreign name. mcp-stub-ok Scala now compiles WITHOUT the FFancyStrShim (removed). Scala matrix + sbt +compile green."

### D5 — resolved

- createdAt: 2026-06-04T22:41:10.641Z
- updatedAt: 2026-06-05T20:11:27.195Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "Scala codegen: Color_JsonCodec referenced but not imported in nested-package codec file (pre-existing)"
- description: "Filed by the T12 reviewer (round 0, out-of-scope, PRE-EXISTING — reproduced without the MCP flag). Generated mcptools_listcollections.in.scala declares `package mcp.stub.mcptools.listcollections` and imports only `mcp.stub.{Color, Color_UEBACodec}` (not Color_JsonCodec), yet references `Color_JsonCodec.instance` in the map-with-enum-key decoder; from the nested package the bare name does not resolve. Independent of MCP; worked around with a listcollections package object re-export. Triage: candidate for separate fix or wontfix; does not affect MCP feature correctness."
- severity: medium
- suggestedFix: "Route the reference at ScJsonCodecGenerator.scala:481 through the existing `codecName` helper (:598-600) so `Color_JsonCodec` is a real ScType node (imported), mirroring the UEBA path and the JSON encode path. Remove the package-object re-export workaround. Same fix idiom as D4 (get(Key)Decoder -> codecName) — fold D4+D5 into ONE Scala-codec task; also fix the value-decoder sites :552/:556 (the D4 site) in the same change."
- ledgerRefs: ["tasks:T12","goals:G1"]
- rootCause: "Confirmed (H7, source-validated). Same root-cause family as D4. ScJsonCodecGenerator.getKeyDecoder's enum branch (ScJsonCodecGenerator.scala:472-481) references `${targetTpe}_JsonCodec.instance.decode(...)` by bare string-concat on the bare enum ScType (Color). Because the import set + final render collect ONLY ScValue.ScType nodes (ScBaboonTranslator.scala:231-239/:283-290), the string-suffix `Color_JsonCodec` is never imported (only bare `Color` and, via the codecName-using UEBA path, `Color_UEBACodec` are) and renders unresolved from the nested package `mcp.stub.mcptools.listcollections`. Worked around in tests by a listcollections package-object re-export."
- sessionLogs: ["docs/logs/20260605-180334-a2d685b9d26f843b9.md"]
- dependsOn: ["T29"]
- fix: "Resolved by T29 (commit 390fec58): ScJsonCodecGenerator.getKeyDecoder's enum branch (:481) now routes through the codecName helper so Color_JsonCodec is a real imported ScType in the nested-package codec file. mcp-stub-ok Scala now compiles WITHOUT the listcollections package-object re-export (removed). Scala matrix + sbt +compile green. (Same root-cause fix as D4.)"

### D6 — resolved

- createdAt: 2026-06-05T00:17:15.479Z
- updatedAt: 2026-06-05T20:11:32.374Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "Python JSON codec mishandles non-string-keyed maps (map[Color,str] enum key) (pre-existing)"
- description: "Filed by the T18 reviewer (round 0, out-of-scope, PRE-EXISTING). The Python JSON codec does not round-trip the {key,value} entry-array shape the MCP/OAS inputSchema declares for a non-string-keyed map like map[Color,str] (listCollections.byColor); the T18 round-trip test passes `byColor: {}` to avoid the enum-key codec path (mirrors the Kotlin replica's `byColor:{}`). Independent of the MCP flag; not introduced by T18. Triage: align the Python JSON codec's non-string-keyed-map handling with the OAS/MCP entry-array representation, under a separate task."
- severity: low
- suggestedFix: "Add a `Typedef.Enum` arm to BOTH mkJsonKeyEncoder (stringify the enum, e.g. `$ref.value`) and mkJsonKeyDecoder (reconstruct via the enum class), mirroring Scala's enum map-key handling, so map[Color,str] round-trips as a string-keyed JSON object consistent with the other languages. Reconcile with the MCP inputSchema: since the data codecs emit a string-keyed object (enum-name keys), align the MCP/OAS inputSchema for non-string-keyed maps with enum keys to the string-keyed-object form (object with enum-named keys), so the generated tool inputSchema matches the actual wire AND a non-empty `byColor` round-trips and validates. Add a python codec / mcp round-trip test exercising map[Color,str] with a NON-empty value."
- ledgerRefs: ["tasks:T18","goals:G1"]
- rootCause: "Confirmed (H8, source-validated). The Python JSON codec's map-key encoder mkJsonKeyEncoder (PyJsonCodecGenerator.scala:317-345) has arms for identifier-DTO / 1-field-DTO / Foreign keys but NO `Typedef.Enum` arm, so an enum map key falls to `case _ =>` and emits a BUG-throw (:344); the symmetric decoder mkJsonKeyDecoder throws at :439. fieldHasUserKeyMap (:222-231) forces enum-keyed maps through this explicit key-walker. So a non-empty `map[Color,str]` raises at runtime (the T18/Kotlin round-trips used `byColor:{}` to dodge it). Scala HAS the enum map-key arm (ScJsonCodecGenerator.scala:317-321, stringifies). NUANCE: the MCP/OAS inputSchema declares non-string-keyed maps as an ENTRY-ARRAY of {key,value} (OasTypeTranslator.scala:141-159) while both Python (:277-284) and Scala (:402-405) DATA codecs emit a STRING-KEYED JSON OBJECT (enum string values as keys) — a schema-vs-codec representation mismatch."
- sessionLogs: ["docs/logs/20260605-180334-a0ec413510e4eb3f5.md"]
- dependsOn: ["T30"]
- fix: "Resolved by T30 (commit c6a8bc1b): added a Typedef.Enum arm to the Python JSON codec's mkJsonKeyEncoder (.value) + mkJsonKeyDecoder (enum-class reconstruct) so map[Color,str] round-trips as a string-keyed object (mirroring Scala) instead of the BUG-throw. Reconciled the MCP + OAS inputSchema for enum-keyed maps to the string-keyed-object form (object + additionalProperties + propertyNames{enum:[wire-values]}) so schema matches the wire all backends emit; replaced the byColor:{} test dodge with a non-empty round-trip validated by the cs (NJsonSchema) + ts (ajv) gates. All 9 MCP lanes + openapi/python/graphql green. (Out-of-scope OAS-parity follow-up filed as D10.)"

### D7 — resolved

- createdAt: 2026-06-05T00:17:22.593Z
- updatedAt: 2026-06-05T20:11:35.471Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "Python codegen: self-referential/recursive pydantic models fail on py3.12 without `from __future__ import annotations` (pre-existing, latent)"
- description: "Surfaced during T18 (filed by orchestrator from the T18 review). A recursive Baboon DTO (e.g. the T22 stub's `Tree { left: opt[Tree], children: lst[Tree] }`) generates Python whose pydantic v2 model raises NameError on py3.12 because the forward self-reference isn't resolvable without `from __future__ import annotations` (PEP 563). Latent until the recursive Tree type was introduced (mcp-stub-ok). The proper fix is in the Python generator (PyBaboonTranslator.renderTree) emitting the future-import for recursive models — but doing so changes flag-off Python output, so it must land as an intentional, separately-scoped baseline change (NOT inside T18, whose acceptance requires flag-off byte-identity). T18 is being re-scoped to apply the future-import only in its test harness (overlay/test-gen action) so the generator stays untouched; this defect tracks the real generator fix."
- severity: medium
- suggestedFix: "Emit `from __future__ import annotations` as the FIRST line of every generated Python module in PyBaboonTranslator.renderTree (recommend UNCONDITIONAL emission: PEP 563 makes all annotations lazy strings, eliminating every forward-ref/ordering hazard incl. mutual recursion, with no recursion-detection logic needed). This changes the flag-off Python baseline for all files — update the Python regular/wrapped generated baseline accordingly (no checked-in golden; the lanes run `python3 -m unittest` against the generated py-stub, which must import/run under py3.12). Q7 ('fix all') approved this baseline change. Add/keep a recursive-DTO round-trip test (the recursive-ok Tree model) that imports + round-trips under py3.12."
- ledgerRefs: ["tasks:T18","goals:G1"]
- rootCause: "Confirmed (H9, ledger-corroborated). The Python generator emits DTO fields as bare (unquoted) annotations `name: <type>` (PyDefnTranslator.scala:532-541); `opt[Tree]` -> `Optional[Tree]` with `Tree` a bare token (PyTypeTranslator.scala:33-36, PyTypes.scala:145); the self-referencing type is filtered out of imports (same module) and rendered as the bare name (PyBaboonTranslator.scala:244-248/:301-303); and renderTree's module preamble (PyBaboonTranslator.scala:297-299) prepends NO `from __future__ import annotations`, with no model_rebuild()/update_forward_refs() and none in the shared runtime (baboon_runtime_shared.py:5-19). Pydantic v2 evaluates self-referencing annotations at model-creation time, so a bare forward self-ref raises NameError on py3.12. (Ledger T18/R21 corroborates: a renderTree future-import was previously reverted precisely because it changes flag-off output.)"
- sessionLogs: ["docs/logs/20260605-180334-acb345f878fd62012.md"]
- dependsOn: ["T31"]
- fix: "Resolved by T31 (commit a53e6499): PyBaboonTranslator.renderTree now emits `from __future__ import annotations` unconditionally as the first line of every generated Python module (PEP 563), so recursive/self-referential pydantic v2 models no longer NameError on py3.12. Removed the test-harness future-import shim (.mdl/defs/tests.md); added a recursive Tree round-trip test. Q7-approved flag-off Python baseline change. py-stub regular/MCP lanes import/run green under py3.12; sbt +compile green."

### D8 — resolved

- createdAt: 2026-06-05T01:03:32.714Z
- updatedAt: 2026-06-05T11:11:55.343Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "Swift codegen: recursive value-type DTOs (e.g. Tree { left: opt[Tree] }) do not compile (BLOCKS T17)"
- description: "Surfaced by T17 (Swift MCP replica). The mcp-stub-ok stub model's recursive DTO `Tree { value: i32, left: opt[Tree], children: lst[Tree] }` generates Swift that fails `swift build`: `error: value type 'Tree' cannot have a stored property that recursively contains it` and `value type 'mcptools.processshape.in' has infinite size`. SwDefnTranslator emits recursive Baboon DTOs as Swift value-type `struct`s without indirection; a value-type struct that (transitively, via opt/lst) contains itself has infinite size and is rejected by the Swift compiler. The other 8 language backends compile this model. Swift `indirect` applies to enums only, so a harness sed-patch cannot fix it; the proper fix is in the Swift generator (represent recursive DTOs via a reference box / `final class`, or an indirect-enum encoding) with corresponding init/Codable/Equatable/Hashable updates and FULL Swift-matrix regression. This materially changes generated Swift for ALL users of recursive DTOs (an external/public-API representation decision), so it is outside T17's MCP-dispatch scope and needs a deliberate design decision. BLOCKS T17 (the shared stub's recursive Tree must compile before `test-swift-mcp` can run). The Swift MCP generator/runtime/inputSchema themselves are complete and correct (sbt +compile, JVM shape test green, codegen matches T7 §2.3 refs)."
- severity: major
- suggestedFix: "Introduce reference-type indirection for recursive DTOs in the Swift generator (SwTypeTranslator/SwDefnTranslator): either box self-referential DTO fields behind a reference type (e.g. a generated `final class` box) or re-encode recursive DTOs analogously to the existing `indirect enum` ADT path. Update the init constructor (SwDefnTranslator.scala:388-407), the JSON Codable encode/decode (SwJsonCodecGenerator.scala), the UEBA codec (SwUEBACodecGenerator.scala), and Equatable/Hashable (currently auto-synthesized via the struct conformance clause) to match the boxed representation. Regress the FULL Swift test matrix (regular-adt, wrapped-adt, manual/compat) since this changes generated Swift for ALL recursive-DTO users. Then re-dispatch T17 (Swift MCP). Per Q6 the user chose 'fix now, ship MCP for all 9 languages'."
- ledgerRefs: ["tasks:T17","goals:G1"]
- rootCause: "Confirmed (H1, all citations source-verified). SwDefnTranslator renders every Baboon DTO as a value-type `public struct ${name}: Equatable, Hashable, ...` (SwDefnTranslator.scala:409-418), and each field becomes a bare stored property `public let <name>: <asSwRef(type)>` (SwDefnTranslator.scala:350-355). `opt[T]` maps to Swift `Optional<T>` / `T?` (SwTypeTranslator.scala:63-65), which stores its payload inline for a value type. Therefore a recursive DTO such as `Tree { value: i32; left: opt[Tree]; children: lst[Tree] }` (mcp_stub.baboon:58-62) emits `let left: Tree?` directly inside `struct Tree`, giving the struct infinite size; swiftc rejects it with 'value type Tree cannot have a stored property that recursively contains it'. `lst[T]` maps to `[T]` (Array, heap-backed, SwTypeTranslator.scala:66-68) so the `children` field alone would NOT trigger it; the `opt[Tree]` field is the trigger. The transitively-recursive method-input DTO `mcptools.processshape.in` (carries `tree: Tree`, mcp_stub.baboon:117-123) fails the same way ('infinite size'). The generator already breaks recursion for ADTs via `public indirect enum` (SwDefnTranslator.scala:822-831) but applies no equivalent indirection to struct DTOs."
- sessionLogs: ["docs/logs/20260605-090917-aca6d3712dc5065b7.md"]
- dependsOn: ["T23"]
- fix: "Resolved by T23 (commit 92ef0547): recursive value-type DTO fields are boxed behind a new @BaboonIndirect reference-box property wrapper in the Swift generator (only transitively-self-recursive inline fields, e.g. `left: Tree?`, are boxed; lst/set/map heap-backed fields are not). Generated Swift for recursive DTOs is now finite-size and compiles; full Swift matrix + sbt +compile green. T17 (Swift MCP) is now unblocked."

### D10 — resolved

- createdAt: 2026-06-05T19:40:17.220Z
- updatedAt: 2026-06-05T21:10:31.570Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: OAS enum-keyed-map inputSchema lacks foreign-rt resolution + test coverage (parity gap vs MCP)
- description: "Filed by the T30 reviewer (file-and-defer, K13; low severity, out-of-scope for T30). After T30 reconciled enum-keyed maps to the string-keyed-object inputSchema form in BOTH the MCP emitter (McpInputSchemaEmitter) and the OAS emitter (OasTypeTranslator.mapSchema), a parity gap remains: the OAS `mapSchema` matches enum keys via a DIRECT `TypeRef.Scalar(id: TypeId.User) if enumKeys.contains(id)` test, whereas the MCP emitter resolves the key ref through foreign runtime (`rt`) resolution FIRST (resolveTypeRef). So a foreign type whose runtime mapping resolves to an enum would be reconciled to a string-keyed object by MCP but emitted as the (old) entry-array by OAS — an inconsistency. Additionally there is NO OAS test model exercising an enum-keyed map, so the new OAS branch is untested. Edge case (no current model has a foreign-resolving-to-enum map key), revealed by the T30 diff. Independent of the MCP feature correctness (MCP path is well-tested + green)."
- severity: low
- suggestedFix: "Low-severity test-coverage + defensive hardening (NO behavior bug to fix): (a) add an OAS-exercised model + golden that includes an enum-keyed map (and ideally a foreign-rt-to-enum-keyed map) so the T30 OAS string-keyed-object branch is exercised and the renderDto pre-resolution invariant is pinned by a test; (b) OPTIONALLY harden OasTypeTranslator.mapSchema to resolve the key ref itself (mirror the MCP enumKey helper using resolveTypeRef) so OAS correctness is local rather than dependent on the caller pre-resolving. Keep `mdl :build :test`/`:ci` green."
- ledgerRefs: ["tasks:T30","goals:G1"]
- rootCause: "Investigation (H10) REFUTED the alleged divergence. OAS mapSchema (OasTypeTranslator.scala:146-160) matches enum keys by a direct enumKeys.contains(id) test with no in-method resolution, BUT OAS renderDto PRE-RESOLVES every field's tpe via resolveTypeRef ONE LAYER UP (OasBaboonTranslator.scala:165), and resolveTypeRef recurses into map-constructor args (OasTypeTranslator.scala:40-41). So a foreign-rt-to-enum map key reaches mapSchema already resolved to the enum -> enumKeys.contains matches -> string-keyed object, the SAME form MCP produces. There is NO entry-array divergence on current code (both typeRefSchema callers go through the renderDto:165 pre-resolution). The GENUINE residual is twofold: (1) COVERAGE GAP — the only enum-keyed map in the corpus is byColor: map[Color,str] in the MCP stub (mcp_stub.baboon:112); no OAS-exercised model/golden covers the T30 OAS string-keyed-object branch (OasTypeTranslator:149-151), and no Oas* unit test exists; (2) OAS mapSchema correctness silently RELIES on the un-pinned renderDto:165 caller-pre-resolution invariant."
- dependsOn: ["T32"]
- fix: "Resolved by T32 (commit 7f2e7f38): no behavior bug existed (investigation H10 refuted the OAS-vs-MCP divergence — OAS pre-resolves foreign-rt map keys at renderDto:165 and already emits the string-keyed-object form). The residual coverage gap is closed: OasEnumKeyMapSchemaTest now exercises the OAS enum-keyed-map string-keyed-object branch (enum key -> type:object+additionalProperties+propertyNames) and PINS the renderDto:165 Constructor-arg-recursion pre-resolution invariant via a foreign-rt-to-enum case (mutation-confirmed: no-op'ing OasTypeTranslator.resolveTypeRef's Constructor recursion fails the test). sbt +compile green; test-only change; no generator modification needed."
