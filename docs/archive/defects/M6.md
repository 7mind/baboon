## M6

### D11 — resolved

- createdAt: 2026-06-05T21:22:15.742Z
- updatedAt: 2026-06-05T22:16:43.214Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "Combined-state regression: T26's processTagged tool addition broke the per-language MCP overlay stubs (test-<lang>-mcp lanes)"
- description: "Surfaced by T33 (final CI closeout) running full `mdl --seq :build :test` on the combined main (7f2e7f38). `mdl :build` GREEN (GraalVM native-image + sbt JVM+Scala.js cross-compile) but `mdl :test` RED on test-cs-mcp with CS0535 'StubMcpTools does not implement interface member IMcpTools.ProcessTagged'."
- severity: major
- suggestedFix: "Add the `processTagged` tool method to the hand-written StubMcpTools (service-interface implementation) in EACH MCP overlay whose stub implements the full generated service interface: test/cs-stub-mcp-overlay/McpTests/McpTests.cs (confirmed CS0535), and analogously test/{scala,rust,kotlin,java,dart,swift,ts}-stub-mcp-overlay/ (statically-typed interface/trait/protocol impls) + test/py-stub-mcp-overlay/ (duck-typed — add the method if the round-trip dispatches or asserts tool count). Implement each as a trivial handler returning the out DTO (ok:true), mirroring the existing tool stubs. Then re-run all 9 test-<lang>-mcp lanes green. (Alternative considered: revert T26's processTagged service method from mcp_stub.baboon + re-home the D1 ADT-common-fields assertion to a dedicated emitter-only test model so the shared stub's tool count stays at 5 — but adding the method to the overlays is the minimal forward fix and gives the overlays coverage of the new tool.)"
- rootCause: "Confirmed (T33 reproduced the CS0535 + orchestrator scoped the overlays). T26 (commit c84ab289, D1 verify) added a contract-bearing ADT `Tagged` AND a service method `processTagged(tagged: Tagged)` to the SHARED MCP stub mcp_stub.baboon so the ADT is reachable as a tool request for the McpInputSchemaEmissionTest assertion. That made the generated per-service MCP interface (IMcpTools etc.) have 6 tools instead of 5. T26 updated the Scala/Python EMISSION-test K1 size guards (5->6) but did NOT update the 9 hand-written per-language MCP round-trip OVERLAY stubs (test/<lang>-stub-mcp-overlay/), each of which implements the full generated service interface. So on the combined main every statically-typed overlay stub (cs/scala/rust/kotlin/java/dart/swift/ts) fails to compile (missing processTagged); python (duck-typed) may fail only if the tool is dispatched. The per-task verifications missed it: T26's reviewer ran only the Scala/Py emission tests (which T26 updated), and T30's reviewer ran the 9 MCP lanes at T30's commit which predated T26's processTagged. All 9 overlays confirmed to have a stub/test file referencing the MCP service."
- ledgerRefs: ["tasks:T33","tasks:T26","goals:G1"]
- dependsOn: ["T34"]
- fix: "Resolved by T34 (commit 7d5dcebe): added the processTagged tool stub/handler + updated the hard-coded tool-count (5->6), the tools/list name/order assertions (processTagged at declaration-order index 3, shifting pagePoints->4, ping->5), the K1 structural-equality reference schemas, and a tools/call success assertion across all 9 per-language MCP overlay round-trip tests. All 9 test-<lang>-mcp lanes now compile AND pass (mdl --seq all-9 green; reviewer ran 5/9 green). Test-overlay only — no generator/feature/mcp_stub.baboon change (the forward fix gives the overlays coverage of the new processTagged tool that T26 added)."
