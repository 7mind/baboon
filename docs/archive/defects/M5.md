## M5

### D9 — resolved

- createdAt: 2026-06-05T12:17:48.194Z
- updatedAt: 2026-06-05T13:08:47.273Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "Full `mdl :test` red: mcp-stub-ok in shared model-dir breaks 8 wiring lanes' fixture compilation (BLOCKS T21 CI closeout)"
- description: |
    Surfaced by T21 (CI closeout), reproduced via a full `mdl :test` run at main HEAD bedabb3d. `mdl :build` (GraalVM native-image) is GREEN and off-by-default byte-identity is verified, but `mdl :test` is RED: 8 wiring lanes fail to COMPILE — test-sc-wiring-{either,hkt,outcome,result} (4), test-rs-wiring-{either,result} (2), test-kt-wiring (1), test-jv-wiring (1).
    
    ROOT CAUSE (structurally confirmed by orchestrator): the MCP test stub model `mcp-stub-ok/mcp_stub.baboon` was placed (by T22) UNDER the shared test-resources directory `baboon-compiler/src/test/resources/baboon/`. The regular/wrapped/wiring test actions invoke codegen with `--model-dir ./baboon-compiler/src/test/resources/baboon/` (the shared root; see .mdl/defs/tests.md L59/172/376/478/1112...), so they now ALSO generate the MCP stub model's types AND its cross-language fixtures. The fixture generator (In_Fixture.*) emits cross-namespace type references (Color, PointPage_Fixture, FFancyStr) WITHOUT proper import qualification, producing uncompilable generated code in Scala/Rust/Kotlin/Java wiring lanes. (The 9 MCP-specific lanes that target `--model-dir .../mcp-stub-ok/` directly are unaffected and pass.)
    
    This is a TEST-INFRASTRUCTURE + fixture-codegen regression introduced by the MCP feature's stub-model placement; the feature code itself is inert when its flags are off (off-by-default byte-identity holds). It BLOCKS T21 (full `mdl :test`/`mdl :ci` green).
    
    NOTE on Scala overlap: the Scala wiring failures (Color/FFancyStr unqualified) are in the same family as parked defects D4 (FFancyStr bare import unresolved) and D5 (Color_JsonCodec not imported); the Rust/Kotlin/Java failures are broader (fixture generator). A clean fix likely addresses the model PLACEMENT (so wiring tests don't generate the MCP stub at all) rather than the per-language fixture-import bug.
    
    FIX-APPROACH CAVEAT (for planning): option (a) 'relocate mcp-stub-ok out of the shared dir' CONFLICTS with T23 — T23 deliberately wired McpStub INTO the shared Swift matrix (Package.swift) to exercise the recursive `Tree` (@BaboonIndirect) regression. Any relocation must preserve that recursive-DTO coverage (or move it to a dedicated harness). Option (b) 'fix fixture-import generation for cross-namespace types' is a multi-language generator change. Investigate to pick the right approach.
- severity: major
- suggestedFix: "Scope the fix to TEST INFRASTRUCTURE only (do NOT touch the per-language fixture/codec generators — the unqualified-ref generator bugs are parked as D4/D5 on Q7 for the user's separate disposition; relocating the stub removes them from the CI path without needing that fix). Concretely: (1) RELOCATE the mcp-stub-ok model OUT of the shared dir — move baboon-compiler/src/test/resources/baboon/mcp-stub-ok/ to a sibling NOT scanned by the wiring/regular/wrapped `--model-dir` (e.g. baboon-compiler/src/test/resources/mcp-stub-ok/); (2) UPDATE the 9 test-gen-<lang>-mcp lanes' `--model-dir` (tests.md: ts:1611, cs:1665, rust:1722, kotlin:1775, java:1829, scala:2633, python:2687, + dart/swift mcp lanes) and any JVM shape tests' model-resource path to the new location; (3) PRESERVE T23's recursive-DTO coverage: add a MINIMAL recursive-only model (`Tree { value:i32; left:opt[Tree]; children:lst[Tree] }`, no Color/Page/FFancyStr) into the shared resources/baboon/ dir so the regular/wrapped matrix exercises recursive DTOs across ALL languages (a bare recursive Tree has no cross-namespace fixture ref → compiles clean), and rewire T23's test/sw-stub/Package.swift McpStub target + Tests/RuntimeTests/RecursiveDtoRoundTripTests.swift to import the new minimal model's module instead of McpStub. (4) Re-run full `mdl :test` + `mdl :ci` green; keep off-by-default byte-identity. Then re-run T21."
- ledgerRefs: ["tasks:T21","goals:G1"]
- rootCause: "Confirmed (H2, citations source-verified + T21 reproduced the 8-lane compile failure). The MCP test stub model lives at baboon-compiler/src/test/resources/baboon/mcp-stub-ok/, INSIDE the shared test-resources root. The 8 failing wiring lanes invoke codegen with `--model-dir ./baboon-compiler/src/test/resources/baboon/` (the shared root, which recursively includes mcp-stub-ok) AND pass `--fixture-output` (tests.md:1263+1269 for sc-wiring-either; identical for sc-result/outcome/hkt, rs-either/result @1878-1885/1927-1934, kt-wiring @2218-2225, jv-wiring @2265-2272). So they generate the mcp-stub model's types AND cross-language fixtures. The mcp_stub model declares Color (enum), Page[T]/PointPage, FFancyStr (foreign), reached by the fixture generator via service request DTOs in mcp.stub sub-namespaces. The Scala/Rust/Kotlin/Java fixture (and Scala main, per D4/D5) emitters reference these cross-namespace symbols WITHOUT a resolvable import/qualification → uncompilable generated code. Swift is unaffected because its fixture emitter qualifies cross-namespace refs (SwCodecFixtureTranslator.scala:270-281). The 9 MCP lanes target the curated `.../mcp-stub-ok/` subdir without --fixture-output (tests.md:1611 etc.), so they pass. `mdl :build` (native-image) is green and off-by-default byte-identity holds — this is purely a test-fixture/model-placement regression, not a feature-code defect."
- dependsOn: ["T24"]
- fix: "Resolved by T24 (commit c04e9eaa): relocated mcp-stub-ok out of the shared resources/baboon/ model-dir so the wiring/regular/wrapped lanes no longer generate its cross-namespace fixtures; the 9 MCP lanes + 11 JVM shape tests point at the new path; recursive-DTO coverage retained via a minimal recursive-only Tree model (resources/baboon/recursive-ok/) + sw-stub rewire to a RecursiveTree module (T23 @BaboonIndirect boxing intact). The 8 previously-failing wiring lanes compile clean (sc-wiring-either/rs-wiring-either full-run green). Test-infra fix only — the per-language fixture/codec import generator bugs remain tracked as D4/D5 (parked on Q7), now off the CI path. Final full mdl :test/:ci green verification is T21."
