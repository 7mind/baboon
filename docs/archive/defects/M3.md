## M3

### D3 — resolved

- createdAt: 2026-06-04T20:46:54.147Z
- updatedAt: 2026-06-05T20:11:20.488Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: TypeScript generator emits nondeterministic import ordering (pre-existing)
- description: "Filed by the T8 reviewer (round 0, out-of-scope, low severity, PRE-EXISTING — not introduced by T8). The non-MCP TS generator produces run-to-run-varying import-statement order in generated files (observed in mcp/stub/mcp-tools/service.ts, .../submitcomposite/in.ts, .../Nested.ts) when codegen is repeated with identical inputs and the SAME flag value. Verified by running codegen on baboon-compiler/src/test/resources/baboon/mcp-stub-ok three times with --ts-generate-mcp-server=false: the import SET is constant but its ORDER differs between runs, indicating iteration over an unordered Set/HashMap in the TS import-collection path. TS-semantically inert (tsc passes) but breaks byte-reproducible codegen. Independent of MCP; surfaced while confirming T8 flag-off byte-identity. Triage note: candidate for a separate fix or wontfix; does NOT affect MCP feature correctness."
- severity: low
- suggestedFix: "Make import ordering total and lexical in TsBaboonTranslator's import emission: replace the non-injective key at :461 with a deterministic one (e.g. sort modules by full module path) and sort the per-module symbols by name at :467 before rendering, mirroring the existing `names.toList.sorted` at :566-568. Add a determinism regression (generate mcp-stub-ok / a model twice and assert byte-identical import blocks, or assert the import block equals a lexically-sorted expectation) — no such golden exists today."
- ledgerRefs: ["tasks:T8","goals:G1"]
- rootCause: "Confirmed (H5). The TypeScript generator's import emission lacks a deterministic lexical sort: module-line order is `groupBy(_.moduleId).toList.sortBy{ path.size + types.size }.reverse` (TsBaboonTranslator.scala:461) — a NON-injective key, so modules with equal sums fall back to immutable-HashMap iteration order; symbols within `{}` follow groupBy order with no sort (:467-473); render joins without re-sort (:498). Refinement to the original filing: Scala immutable HashMaps iterate hash-stably PER BINARY (not per-run seeded), so the order is non-lexical/hash-derived (unstable across toolchain/library changes), not strictly per-run-varying on the same binary. The barrel/re-export path already does it correctly with `names.toList.sorted` (:566-568)."
- sessionLogs: ["docs/logs/20260605-180334-a70ee77bb43014030.md"]
- dependsOn: ["T28"]
- fix: "Resolved by T28 (commit 2c303c90): TS import emission is now totally lexically ordered at BOTH sites in TsBaboonTranslator (renderTree typesByModule + the domain-facade collectTypes), keyed on the rendered `from '...'` source string via a shared renderedImportSource helper (emitter + sort key share one path-computation source). Added a live canonical-order determinism regression (fails pre-fix for both module- and symbol-order). sbt +compile (JVM+JS) green; reorder-only (tsc-inert)."
