## M2

### D1 — resolved

- createdAt: 2026-06-04T19:45:09.373Z
- updatedAt: 2026-06-05T20:11:11.071Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: MCP inputSchema emitter drops ADT common fields
- description: "Filed by the T5 reviewer (round 0, out-of-scope, low severity). McpInputSchemaEmitter.adtSchema (~L209-212) emits only `oneOf` of branch $refs and ignores `Typedef.Adt.fields` (common fields hoisted across branches). A branch schema therefore omits the ADT's shared fields. OUT OF SCOPE for T5: the K6 stub model's ADT case (Shape) carries no common fields, so this is unexercised and untested by T5's acceptance. Triage context: surfaced reviewing tasks:T5 diff 484e1617..60b6a26e; belongs to a later task/decision that exercises ADT-with-common-fields, or a documented wontfix if v1 MCP tool requests never carry such ADTs."
- severity: low
- suggestedFix: "Do NOT add the allOf merge (it would duplicate). Instead LOCK the correct behavior with a regression test: add a contract-bearing ADT (e.g. an ADT whose branches share a contract) to the MCP test stub and assert in McpInputSchemaEmissionTest that each branch's $defs `properties` includes the contract/common fields. If (contrary to the typer-path analysis) the common fields are absent from the branch schemas, THEN add the merge fix (allOf the common fields alongside the branch oneOf, or merge into each branch). Either way D1 resolves with empirical coverage."
- ledgerRefs: ["tasks:T5","goals:G1"]
- rootCause: "Investigation (H3) REFUTED the original claim. adtSchema (McpInputSchemaEmitter.scala:209-212) emits only `oneOf` of branch $refs and does not read adt.fields — BUT `adt.fields` is derived SOLELY from `adt.contracts` (BaboonTranslator.scala:436-446), and every ADT branch DTO inherits the parent ADT's contracts and MERGES their fields into the branch DTO's own `fields` at typing time (BaboonTranslator.scala:289-306, :360-362). Each branch is emitted as a full $defs DTO via dtoBody (McpInputSchemaEmitter.scala:174), so the common (contract) fields ALREADY appear in every branch's $ref schema. An `allOf adt.fields` fix would DUPLICATE the common fields. The OAS emitter uses the identical oneOf-of-refs convention (OasBaboonTranslator.scala:239). So the emitter is correct for the only way ADTs acquire common fields today (via contracts). Caveat: this is static typer-path analysis, not an executed emit."
- sessionLogs: ["docs/logs/20260605-180334-a4b97789b18f7398f.md"]
- dependsOn: ["T26"]
- fix: "Resolved by T26 (commit c84ab289): investigation REFUTED the original 'drops common fields' claim and a regression test now empirically locks it. Added a contract-bearing ADT (Tagged with shared contract HasId{id:str}) to the MCP stub + a McpInputSchemaEmissionTest assertion that each branch's $defs `properties` includes the contract field (id) WITHOUT allOf. The behavior was correct all along (ADT contract fields are merged into each branch DTO at typing, so branch $refs already encode them); no generator change. Combined main sbt +compile + Test/compile green."

### D2 — resolved

- createdAt: 2026-06-04T19:45:14.494Z
- updatedAt: 2026-06-05T20:11:16.233Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "MCP inputSchema: contract in reachable closure would emit a dangling $defs ref"
- description: "Filed by the T5 reviewer (round 0, out-of-scope, low severity). McpInputSchemaEmitter.emitNamedType (~L166-183) returns List.empty for `Typedef.NonDataTypedef` (Service/Contract), but the reachable-closure walk would still enqueue and `localRef` a contract id if a DTO field referenced one, producing a `#/$defs/<Contract>` ref with no matching $defs entry (dangling). In practice Baboon contracts are structural mixins applied via +/^, not field-referenceable types, so this path is likely unreachable; the K6 stub has no contract and the inventory does not include it. OUT OF SCOPE for T5. Triage context: surfaced reviewing tasks:T5 diff 484e1617..60b6a26e."
- severity: low
- suggestedFix: "Fail-fast in the MCP emitter: replace the silent `case _: Typedef.NonDataTypedef => List.empty` (McpInputSchemaEmitter.scala:169) with an explicit throw (matching the eager-failure style at :181/:253) so a contract/service entering the closure surfaces as a hard error instead of a silent dangling ref. ALSO add the proper upstream guard: in the typer (convertTpe, BaboonTranslator.scala:543-548) or validator, reject a plain field whose resolved user type is a NonDataTypedef (a 'data type expected' check, ScalarExpected-adjacent) — contract-as-field-type is not an intended construct. Add a negative test (a model with a contract-typed field) asserting the guard fires."
- ledgerRefs: ["tasks:T5","goals:G1"]
- rootCause: "Confirmed (H4, source-validated). McpInputSchemaEmitter.emitNamedType returns List.empty for a NonDataTypedef (Service/Contract) (McpInputSchemaEmitter.scala:166-169), so it emits NO $defs body for a contract; but the reachable-closure walk enqueues + localRefs ANY referenced TypeId.User with no contract exclusion (:147-158, :231-235, :305-306). A contract carries a TypeId.User which IS a Scalar (Typedef.scala:71, TypeId.scala:29), and convertTpe admits a contract id as a plain field type — it only requires the resolved id to be a Scalar, with NO data-type guard (BaboonTranslator.scala:540-551) — and no validator rejects a plain contract-typed field (the Contract->IneligibleUserType guard is map-KEY only, BaboonValidator.scala:302-310). So a hand-written model with `field: SomeContract` would emit `#/$defs/<Contract>` with no matching $defs entry (dangling). Reachable at the typer/validator gate; not reproduced end-to-end."
- sessionLogs: ["docs/logs/20260605-180334-a5a4414aaf1340e2c.md"]
- dependsOn: ["T27"]
- fix: "Resolved by T27 (commit fb1319fd): added VerificationIssue.DataTypeExpectedField + BaboonValidator.checkDataTypeFields which rejects a plain field / collection element / any-underlying whose resolved user type is a NonDataTypedef (contract/service), so a contract-typed field can no longer reach the MCP inputSchema closure; plus a defense-in-depth eager throw at the MCP emitter's NonDataTypedef arm (replacing the silent List.empty). 3 exhaustive-match sites updated (incl. the Scala.js BaboonJS axis); negative test ContractFieldValidatorTest. sbt +compile (JVM+JS) green; no over-rejection (mixin/DTO/enum/foreign/identifier controls still accepted)."
