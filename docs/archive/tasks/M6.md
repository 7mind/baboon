## M6

### T33 — done

- createdAt: 2026-06-05T20:19:24.416Z
- updatedAt: 2026-06-05T23:54:45.527Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "FINAL CI CLOSEOUT — full mdl :build :test + mdl :ci on combined main (all D1-D10 fixes)"
- description: |
    Combined-state CI gate for the whole MCP-server feature, analogous to T21 for the original feature. The per-task fix verifications (T26-T32) ran in isolated /tmp clones against individual HEADs; this task proves the COMBINED main carrying ALL D1-D10 fixes together is green end-to-end.
    
    Run the FULL CI-equivalent pipeline on the combined main:
      1. `mdl :build :test` (GraalVM native-image build via sbt +compile JVM+Scala.js + full per-language test matrix)
      2. `mdl :ci` (cross-build + GraalVM native-image + :smoke + :test-acceptance + :test-service-acceptance + :test-editors)
    On memory-constrained machines use `mdl --seq :build :test` (Kotlin daemon OOM workaround per CLAUDE.md). Confirm the 9 MCP lanes (test-{ts,cs,scala,rust,kotlin,java,dart,swift,python}-mcp) plus the full regular/wrapped/wiring/manual/compat/service-acceptance/editors matrices all pass. Verify off-by-default byte-identity holds (MCP flags default false => generated output byte-identical to pre-feature) EXCEPT the single intentional Q7-approved Python `from __future__ import annotations` baseline change (D7). Document any residual delta found.
    
    dependsOn the D10 fix (T32) so the closeout runs on a main that already includes the D10 coverage/hardening. Links goals:G1.
- acceptance: "(1) `mdl :build :test` green. (2) `mdl :ci` green (cross-build + GraalVM native-image + smoke + acceptance + service-acceptance + editors). (3) All 9 MCP lanes pass AND the full regular/wrapped/wiring/manual/compat/service-acceptance/editors matrices pass on the COMBINED main. (4) Off-by-default byte-identity holds EXCEPT the intentional Q7-approved Python future-import baseline change (D7); any other residual is documented. (5) Done tasks T1-T32 untouched."
- suggestedModel: standard
- dependsOn: ["T32","T34"]
- ledgerRefs: ["goals:G1"]
- completion: "FINAL CI CLOSEOUT GREEN on combined main (all D1-D11 fixes). Re-run after the D11 fix (T34): `mdl --seq :build :test` 114/114 actions exit 0 (GraalVM native-image + sbt +compile JVM+Scala.js + full per-language matrix incl. all 9 test-<lang>-mcp) AND `mdl --seq :ci` 120/120 actions exit 0 (+smoke + test-acceptance 200/200 + test-service-acceptance 162/162 + test-editors 80/80 + jv-client-roundtrip). Off-by-default verified: zero MCP server files without flags; the ONLY off-by-default delta is the Q7-approved Python `from __future__ import annotations` baseline (D7); Rust gains `pub mod baboon_mcp_server` only with the flag (additive). Reviewer R42 go-ahead (independent /tmp-clone spot-check: cs-mcp green + off-by-default verified). Merged ff to main. The MCP-server feature is complete + green across all 9 backends; all 11 surfaced defects resolved."
- sessionLogs: ["docs/logs/20260605-234830-a7e0e5511478a5a39.md","docs/logs/20260605-234830-a7870f9e7b0234afb.md"]
- resultCommit: 560960969a44ebfa1837df1c3b0c2752e341b3c4

### T34 — done

- createdAt: 2026-06-05T21:23:45.473Z
- updatedAt: 2026-06-05T22:16:39.597Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "Fix D11: add processTagged tool stub AND update count/order/call assertions in all 9 MCP overlay round-trip tests"
- description: |
    Root cause (D11, root-caused): T26 (commit c84ab289, D1 verify) added the contract-bearing ADT `Tagged` AND a service method `processTagged(tagged: Tagged)` to the SHARED MCP stub mcp_stub.baboon so the ADT is reachable as a tool request for the McpInputSchemaEmissionTest assertion. That made the generated per-service MCP interface (IMcpTools etc.) advertise 6 tools instead of 5. T26 updated the Scala/Python emission-test K1 size guards (5->6) but did NOT update the 9 hand-written per-language MCP round-trip OVERLAY stubs/tests, each of which (a) implements the full generated service interface and (b) asserts a HARD-CODED tool count + exact tools/list name/order for the OLD 5-tool set.
    
    TWO-PART GAP (per review R39):
    1. COMPILE gap (statically-typed overlays): the hand-written stub omits processTagged, so the overlay fails to compile against the now-6-method generated interface (confirmed CS0535 in test/cs-stub-mcp-overlay: StubMcpTools missing ProcessTagged).
    2. RUNTIME-ASSERTION gap (ALL overlays incl. python if it asserts count): the round-trip scenario hard-codes count==5 and the exact 5-name tools/list order. Example verified in test/cs-stub-mcp-overlay/McpTests/McpTests.cs: StubMcpTools (L74-90) implements exactly 5 IMcpTools methods; Sec2_ToolsList_ExactlyFiveToolsInDeclarationOrder asserts Assert.AreEqual(5, tools.Count) (L242) and exact tools[0..4] position names (L247-251, 'McpTools_ping' at index 4). Merely adding the interface method clears CS0535 but the round-trip still FAILS at runtime on the count/order assertions because the server now advertises 6 tools. Per R3/K1 every statically-typed replica (scala/rust/kotlin/java/dart/swift/ts) mirrors this T7 scenario and carries analogous count/order assertions; the python overlay too if it asserts count.
    
    FIX — apply BOTH parts in EACH of the 9 MCP overlay round-trip tests:
    - test/cs-stub-mcp-overlay/
    - test/scala-stub-mcp-overlay/
    - test/rust-stub-mcp-overlay/
    - test/kotlin-stub-mcp-overlay/
    - test/java-stub-mcp-overlay/
    - test/dart-stub-mcp-overlay/
    - test/swift-stub-mcp-overlay/
    - test/ts-stub-mcp-overlay/
    - test/py-stub-mcp-overlay/
    For each overlay:
    (a) ADD a trivial `processTagged` handler that returns the out DTO with ok:true, mirroring the existing per-tool stub handlers, to the hand-written service-interface impl (interface/trait/protocol impl for statically-typed; dict/dispatch handler for python).
    (b) UPDATE the hard-coded tool-COUNT assertion 5 -> 6.
    (c) UPDATE the tools/list name/ORDER assertions to insert `McpTools_processTagged` at its correct model-declaration-order position (do NOT assume it is appended last — place it where mcp_stub.baboon declares processTagged relative to the other methods; e.g. the CS test must no longer hard-code 'McpTools_ping' at the final index if ordering shifts).
    (d) ADD the matching tools/call success assertion for `McpTools_processTagged` (input dispatches Tagged, expect out ok:true) IF that overlay's scenario exercises per-tool calls.
    Match each language's existing tool-stub and assertion idiom exactly. Do NOT touch feature/generator code — `mdl :build` is already green; this is a test-overlay-stub gap only. Do NOT touch done tasks T1-T32 or revert T26's mcp_stub.baboon change (the forward fix gives the overlays coverage of the new tool). Keep T34's links (defects:D11 + goals:G1), D11.dependsOn=[T34], and the T33 rewire intact.
- acceptance: "All 9 test-<lang>-mcp lanes (test-{ts,cs,scala,rust,kotlin,java,dart,swift,python}-mcp) COMPILE AND PASS on the combined main — not merely compile. Concretely, in EVERY overlay round-trip test: (a) the service-interface impl implements/handles processTagged returning ok:true; (b) the tool-count assertion reads 6 (was 5); (c) the tools/list name/order assertion includes McpTools_processTagged at its model-declaration-order position; (d) where the scenario exercises per-tool calls, a tools/call success assertion for McpTools_processTagged passes. `mdl :build` remains green (no generator/feature change). No change to T1-T32 or to mcp_stub.baboon."
- suggestedModel: frontier
- ledgerRefs: ["defects:D11","goals:G1"]
- resultCommit: 7d5dcebecac21233f80aceba343d577cb196beb0
- completion: "D11 fixed: added a processTagged tool handler/stub + updated tool-count (5->6), tools/list order (processTagged at declaration-order index 3, shifting pagePoints->4, ping->5), K1 reference schemas, positional indices, and a tools/call success assertion across ALL 9 per-language MCP overlay round-trip tests. Worker reproduced CS0535 first; all 9 test-<lang>-mcp lanes green (mdl --seq all-9 exit 0). Reviewer R41 go-ahead (ran 5/9 lanes green incl. order index 3 + dispatch + K1 gate; K1 refs match emitter byte-for-byte). Test-overlay only; no generator/mcp_stub.baboon change. Merged to main (cherry-picked onto b7db3f05; main now 7d5dcebe). D11 resolved."
- sessionLogs: ["docs/logs/20260605-220537-ad7a09c8c879278d6.md","docs/logs/20260605-220537-a0f14ec55e2dbc6fa.md"]
