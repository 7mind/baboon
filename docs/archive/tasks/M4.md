## M4

### T12 — done

- createdAt: 2026-06-04T18:05:33.107Z
- updatedAt: 2026-06-04T23:01:46.281Z
- author: "opus-4.8[1m]"
- session: 9fa3b56d-80fc-4604-bf8e-7daadaf5957b
- headline: Scala MCP server generator + overlay + test-gen-scala-mcp/test-scala-mcp
- description: "Replicate the reference MCP generator (T8/T10 shape, T3 dispatch contract, T5 inputSchema) to Scala behind --scala-generate-mcp-server: per-service transport-abstract dispatch entrypoint, tool registry, inputSchema, Circe JSON codec reuse, additive runtime types. Add test/scala-stub-mcp-overlay/ + test-gen-scala-mcp (Scala regular-adt flags + flag on) + test-scala-mcp (build+run round-trip) in .mdl/defs/tests.md. Round-trip drives the dispatch entrypoint through initialize/tools-list/tools-call + error via an injected fake transport; unconditional throws + negative control. VALIDITY GATE (per locked decision K1 resolving review R2): no full JSON-Schema validator is required for this replica; instead at tools/list the round-trip MUST (a) assert each returned tool inputSchema parses as well-formed JSON through the Scala/Circe codec, and (b) assert the parsed inputSchema is STRUCTURALLY EQUAL to the reference inputSchema expectation from the T7 language-neutral scenario (the schema already validated well-formed at T5/T9/T11). This catches Circe codec-rendering divergence (key escaping, number/$ref/additionalProperties/nested-map) without a per-language schema validator."
- acceptance: Flag on -> compiling per-service Scala MCP server (sbt +compile green, incl. .js cross-build unaffected); flag off -> byte-identical baseline. test-gen-scala-mcp + test-scala-mcp green; at tools/list each returned inputSchema parses as well-formed JSON through the Circe codec AND is structurally equal to the T7 reference inputSchema (per K1); negative control fails. JVM codegen-shape assertion present.
- suggestedModel: standard
- dependsOn: ["T11"]
- ledgerRefs: ["goals:G1"]
- resultCommit: a36e3bf5072c82f9d46e5af9c58b825d25eca804
- completion: "Scala MCP generator (ScMcpServerGenerator + BaboonMcpRuntime.scala, Circe codec reuse) behind --scala-generate-mcp-server, mirrors T8/T10/K5; overlay test/scala-stub-mcp + test-gen-scala-mcp/test-scala-mcp (in :test); K1 real structural equality vs T7 §2.3 refs (10/10); flag-off byte-identical. Pre-existing Scala codegen issues filed D4/D5."
- sessionLogs: ["docs/logs/20260604-223038-a82b978872a1fa7fc.md","docs/logs/20260604-224034-afc74e884b46a7584.md","docs/logs/20260604-225606-a05bc3d968ba4b235.md","docs/logs/20260604-225919-a7eddde5e8a8b4783.md"]

### T13 — done

- createdAt: 2026-06-04T18:05:38.275Z
- updatedAt: 2026-06-04T23:01:48.487Z
- author: "opus-4.8[1m]"
- session: 9fa3b56d-80fc-4604-bf8e-7daadaf5957b
- headline: Rust MCP server generator + overlay + test-gen-rust-mcp/test-rust-mcp
- description: "Replicate the reference MCP generator to Rust behind --rust-generate-mcp-server: per-service transport-abstract dispatch entrypoint per T3, tool registry, inputSchema from T5, serde JSON codec reuse, additive runtime types. Mind Rust generic-placement gotchas noted for the wiring work. Add test/rust-stub-mcp-overlay/ + test-gen-rust-mcp (Rust regular-adt flags + flag on) + test-rust-mcp (cargo build+run round-trip) in .mdl/defs/tests.md; injected fake transport; unconditional panics/asserts as real failures; negative control. VALIDITY GATE (per locked decision K1 resolving review R2): no full JSON-Schema validator is required for this replica; instead at tools/list the round-trip MUST (a) assert each returned tool inputSchema parses as well-formed JSON through the Rust/serde_json codec, and (b) assert the parsed inputSchema is STRUCTURALLY EQUAL to the reference inputSchema expectation from the T7 language-neutral scenario (already validated well-formed at T5/T9/T11). This catches serde codec-rendering divergence (key escaping, number/$ref/additionalProperties/nested-map) without a per-language schema validator."
- acceptance: Flag on -> compiling per-service Rust MCP server (cargo build clean); flag off -> byte-identical baseline. test-gen-rust-mcp + test-rust-mcp green exercising initialize/tools-list/tools-call + error; at tools/list each returned inputSchema parses as well-formed JSON through serde_json AND is structurally equal to the T7 reference inputSchema (per K1); negative control fails. JVM codegen-shape assertion present.
- suggestedModel: standard
- dependsOn: ["T11"]
- ledgerRefs: ["goals:G1"]
- resultCommit: 1224f1fdc1230adcc92e0ebbdae37e4e015061d8
- completion: "Rust MCP generator (RsMcpServerGenerator + baboon_mcp_server.rs, serde reuse; RsBaboonTranslator emits MCP into module pass) behind --rs-generate-mcp-server, mirrors T8/T10/K5; overlay test/rust-stub-mcp-overlay + test-gen-rust-mcp/test-rust-mcp (in :test); K1 real serde_json::Value structural equality vs T7 §2.3 refs (cargo 12/12, -D warnings); flag-off inert."
- sessionLogs: ["docs/logs/20260604-223038-a8f70596e4f947d08.md","docs/logs/20260604-224034-aed725b66bef6d1e4.md","docs/logs/20260604-225606-abe0566f6578ac4e8.md","docs/logs/20260604-225919-a91a6e21f143ab4ea.md"]

### T14 — done

- createdAt: 2026-06-04T18:05:43.923Z
- updatedAt: 2026-06-04T23:01:53.727Z
- author: "opus-4.8[1m]"
- session: 9fa3b56d-80fc-4604-bf8e-7daadaf5957b
- headline: Kotlin MCP server generator + overlay + test-gen-kotlin-mcp/test-kotlin-mcp
- description: "Replicate the reference MCP generator to Kotlin behind --kotlin-generate-mcp-server: per-service transport-abstract dispatch entrypoint per T3, tool registry, inputSchema from T5, Jackson JSON codec reuse, additive runtime types. Mind Kotlin generic-placement gotchas from the wiring work and the local Kotlin-daemon OOM (use mdl --seq when needed). Add test/kotlin-stub-mcp-overlay/ + test-gen-kotlin-mcp (Kotlin regular-adt flags + flag on) + test-kotlin-mcp (build+run round-trip) in .mdl/defs/tests.md; injected fake transport; unconditional throws (no -ea reliance); negative control. VALIDITY GATE (per locked decision K1 resolving review R2): no full JSON-Schema validator is required for this replica; instead at tools/list the round-trip MUST (a) assert each returned tool inputSchema parses as well-formed JSON through the Kotlin/Jackson codec, and (b) assert the parsed inputSchema is STRUCTURALLY EQUAL to the reference inputSchema expectation from the T7 language-neutral scenario (already validated well-formed at T5/T9/T11). This catches Jackson codec-rendering divergence (key escaping, number/$ref/additionalProperties/nested-map) without a per-language schema validator."
- acceptance: Flag on -> compiling per-service Kotlin MCP server; flag off -> byte-identical baseline. test-gen-kotlin-mcp + test-kotlin-mcp green exercising initialize/tools-list/tools-call + error; at tools/list each returned inputSchema parses as well-formed JSON through Jackson AND is structurally equal to the T7 reference inputSchema (per K1); negative control fails. JVM codegen-shape assertion present.
- suggestedModel: standard
- dependsOn: ["T11"]
- ledgerRefs: ["goals:G1"]
- resultCommit: 67e7eae9834de36fddb74138fa53850486b27895
- completion: "Kotlin MCP generator (KtMcpServerGenerator + BaboonMcpRuntime.kt, Jackson/kotlinx reuse) behind --kt-generate-mcp-server, mirrors T8/T10/K5; overlay test/kotlin-stub-mcp-overlay + test-gen-kotlin-mcp/test-kotlin-mcp (in :test); K1 real kotlinx JsonElement structural equality vs T7 §2.3 refs (gradle 11/11); flag-off byte-identical."
- sessionLogs: ["docs/logs/20260604-223038-a0b38a5b81780930f.md","docs/logs/20260604-224034-a37afe2ff57235d12.md","docs/logs/20260604-225606-a11c98bf42d659334.md","docs/logs/20260604-225919-a5b6dba22ba511da9.md"]

### T15 — done

- createdAt: 2026-06-04T18:05:50.048Z
- updatedAt: 2026-06-05T00:19:29.241Z
- author: "opus-4.8[1m]"
- session: 9fa3b56d-80fc-4604-bf8e-7daadaf5957b
- headline: Java MCP server generator + overlay + test-gen-java-mcp/test-java-mcp
- description: "Replicate the reference MCP generator to Java behind --java-generate-mcp-server: per-service transport-abstract dispatch entrypoint per T3, tool registry, inputSchema from T5, Jackson JSON codec reuse, additive runtime types. CRITICAL gotcha: the codegen action MUST include --jv-write-evolution-dict=true (and the rest of Java regular-adt flags verbatim) or the stub fails to compile. Watch the 64KB JVM constant limit (recent split precedent) for large generated methods. Add test/java-stub-mcp-overlay/ + test-gen-java-mcp + test-java-mcp in .mdl/defs/tests.md; injected fake transport; unconditional throws (no -ea reliance); negative control. VALIDITY GATE (per locked decision K1 resolving review R2): no full JSON-Schema validator is required for this replica; instead at tools/list the round-trip MUST (a) assert each returned tool inputSchema parses as well-formed JSON through the Java/Jackson codec, and (b) assert the parsed inputSchema is STRUCTURALLY EQUAL to the reference inputSchema expectation from the T7 language-neutral scenario (already validated well-formed at T5/T9/T11). This catches Jackson codec-rendering divergence (key escaping, number/$ref/additionalProperties/nested-map) without a per-language schema validator."
- acceptance: Flag on -> compiling per-service Java MCP server (no 64KB-constant violation); flag off -> byte-identical baseline. test-gen-java-mcp (with --jv-write-evolution-dict) + test-java-mcp green exercising initialize/tools-list/tools-call + error; at tools/list each returned inputSchema parses as well-formed JSON through Jackson AND is structurally equal to the T7 reference inputSchema (per K1); negative control fails. JVM codegen-shape assertion present.
- suggestedModel: standard
- dependsOn: ["T11"]
- ledgerRefs: ["goals:G1"]
- resultCommit: 51e99ad0702a54eaeece5fca0114c4a818f5535f
- completion: "Java MCP generator (JvMcpServerGenerator + 10 java runtime files, Jackson reuse) behind --java-generate-mcp-server, mirrors T8/T10/K5; overlay test/java-stub-mcp-overlay (Maven) + test-gen-java-mcp(--jv-write-evolution-dict)/test-java-mcp (in :test); K1 real Jackson JsonNode structural equality vs T7 §2.3 refs (Maven 11/11); flag-off byte-identical; no 64KB violation."
- sessionLogs: ["docs/logs/20260605-000303-a556d7b9a8147e33a.md","docs/logs/20260605-001643-a524651f2843048c7.md"]

### T16 — done

- createdAt: 2026-06-04T18:05:55.211Z
- updatedAt: 2026-06-05T00:19:32.815Z
- author: "opus-4.8[1m]"
- session: 9fa3b56d-80fc-4604-bf8e-7daadaf5957b
- headline: Dart MCP server generator + overlay + test-gen-dart-mcp/test-dart-mcp
- description: "Replicate the reference MCP generator to Dart behind --dart-generate-mcp-server: per-service transport-abstract dispatch entrypoint per T3, tool registry, inputSchema from T5, dart:convert JSON codec reuse, additive runtime types. Gotcha: Dart needs the regular-adt post-codegen step (mv runtime files into packages/baboon_runtime/lib/). Add test/dart-stub-mcp-overlay/ + test-gen-dart-mcp + test-dart-mcp in .mdl/defs/tests.md; injected fake transport; unconditional throws (asserts vacuous without --enable-asserts); negative control. VALIDITY GATE (per locked decision K1 resolving review R2): no full JSON-Schema validator is required for this replica (Dart lacks a first-class stable JSON-Schema validator -- a key driver of K1); instead at tools/list the round-trip MUST (a) assert each returned tool inputSchema parses as well-formed JSON through the dart:convert codec, and (b) assert the parsed inputSchema is STRUCTURALLY EQUAL to the reference inputSchema expectation from the T7 language-neutral scenario (already validated well-formed at T5/T9/T11). This catches dart:convert codec-rendering divergence (key escaping, number/$ref/additionalProperties/nested-map) without a per-language schema validator."
- acceptance: "Flag on -> compiling per-service Dart MCP server (dart analyze/build clean, runtime files relocated); flag off -> byte-identical baseline. test-gen-dart-mcp + test-dart-mcp green exercising initialize/tools-list/tools-call + error; at tools/list each returned inputSchema parses as well-formed JSON through dart:convert AND is structurally equal to the T7 reference inputSchema (per K1); negative control fails. JVM codegen-shape assertion present."
- suggestedModel: standard
- dependsOn: ["T11"]
- ledgerRefs: ["goals:G1"]
- resultCommit: 157eb8e019033e0b6ea63ad8a386ddb82aa2bbc6
- completion: "Dart MCP generator (DtMcpServerGenerator + baboon_mcp_runtime.dart, dart:convert reuse) behind --dt-generate-mcp-server, mirrors T8/T10/K5; overlay test/dart-stub-mcp-overlay + test-gen-dart-mcp/test-dart-mcp (in :test); K1 real recursive structural equality vs T7 §2.3 refs (SetEquality for required; dart test 11/11, analyze clean); flag-off byte-identical."
- sessionLogs: ["docs/logs/20260605-000303-a9952cbf4d9f65ce0.md","docs/logs/20260605-001643-a77ec6e66cd4bc090.md"]

### T17 — done

- createdAt: 2026-06-04T18:06:01.777Z
- updatedAt: 2026-06-05T11:43:38.489Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: Swift MCP server generator + overlay + test-gen-swift-mcp/test-swift-mcp
- description: "Replicate the reference MCP generator to Swift behind --swift-generate-mcp-server: per-service transport-abstract dispatch entrypoint per T3, tool registry, inputSchema from T5, JSONSerialization JSON codec reuse, additive runtime types. Swift is the hardest generic case (abstract-context precedent used protocol associatedtype Ctx + AnyJsonService<R>-style type-erasers); apply the same pattern for the MCP dispatch generics. Gotcha: Swift regular-adt post-codegen fans out CrossLanguageFixturePath.swift into each Tests/BaboonTests/<Module>/. Add test/swift-stub-mcp-overlay/ + test-gen-swift-mcp + test-swift-mcp in .mdl/defs/tests.md; injected fake transport; unconditional throws (Swift release asserts vacuous, use precondition); negative control. VALIDITY GATE (per locked decision K1 resolving review R2): no full JSON-Schema validator is required for this replica (Swift lacks a first-class stable JSON-Schema validator -- a key driver of K1); instead at tools/list the round-trip MUST (a) assert each returned tool inputSchema parses as well-formed JSON through the JSONSerialization codec, and (b) assert the parsed inputSchema is STRUCTURALLY EQUAL to the reference inputSchema expectation from the T7 language-neutral scenario (already validated well-formed at T5/T9/T11). This catches JSONSerialization codec-rendering divergence (key escaping, number/$ref/additionalProperties/nested-map) without a per-language schema validator."
- acceptance: Flag on -> compiling per-service Swift MCP server (swiftc -typecheck / swift build clean, fixtures fanned out); flag off -> byte-identical baseline. test-gen-swift-mcp + test-swift-mcp green exercising initialize/tools-list/tools-call + error; at tools/list each returned inputSchema parses as well-formed JSON through JSONSerialization AND is structurally equal to the T7 reference inputSchema (per K1); negative control fails. JVM codegen-shape assertion present.
- suggestedModel: frontier
- dependsOn: ["T11","T23"]
- ledgerRefs: ["goals:G1"]
- sessionLogs: ["docs/logs/20260605-010305-a52099a8ba3d85d6b.md","docs/logs/20260605-112814-a165d4ba692ec7ac8.md","docs/logs/20260605-114251-a5fed3343bbfbb782.md"]
- resultCommit: b84bf3b497d013ed786cda141af93b8eb033e16f
- completion: Swift MCP server generator (SwMcpServerGenerator) + baboon_mcp_runtime.swift + swift-stub-mcp-overlay + test-gen-swift-mcp/test-swift-mcp; rebased onto the T23/D8 boxing fix. Transport-abstract per K5 (handle in IBaboonMcpServer protocol extension, no I/O loop, tools/call delegates to JSON dispatch). swift test 12/12, JVM shape test 1/1, sbt +compile green, flag-off byte-identical. Worker fixed a latent Swift raw-string lexing defect (##"..."## for #/$defs-bearing inputSchema refs). Reviewer R24 go-ahead (K1 structural equality proven live). Merged ff to main. Completes MCP for all 9 backends.

### T18 — done

- createdAt: 2026-06-04T18:06:06.027Z
- updatedAt: 2026-06-05T09:21:52.006Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: Python MCP server generator + overlay + test-gen-python-mcp/test-python-mcp
- description: "Replicate the reference MCP generator to Python behind --python-generate-mcp-server: per-service transport-abstract dispatch entrypoint per T3, tool registry, inputSchema from T5, the custom Python JSON codec reuse, additive runtime types. Gotcha: ensure Generic/TypeVar imports are present (abstract-context precedent missed them). Add test/py-stub-mcp-overlay/ + test-gen-python-mcp + test-python-mcp in .mdl/defs/tests.md; injected fake transport; unconditional raises; negative control. VALIDITY GATE (per locked decision K1 resolving review R2): no full JSON-Schema validator is required for this replica; instead at tools/list the round-trip MUST (a) assert each returned tool inputSchema parses as well-formed JSON through the Python json codec, and (b) assert the parsed inputSchema is STRUCTURALLY EQUAL to the reference inputSchema expectation from the T7 language-neutral scenario (already validated well-formed at T5/T9/T11). This catches Python-codec rendering divergence (key escaping, number/$ref/additionalProperties/nested-map) without a per-language schema validator."
- acceptance: Flag on -> py_compile-clean per-service Python MCP server (correct Generic/TypeVar imports); flag off -> byte-identical baseline. test-gen-python-mcp + test-python-mcp green exercising initialize/tools-list/tools-call + error; at tools/list each returned inputSchema parses as well-formed JSON through the Python json codec AND is structurally equal to the T7 reference inputSchema (per K1); negative control fails. JVM codegen-shape assertion present.
- suggestedModel: standard
- dependsOn: ["T11"]
- ledgerRefs: ["goals:G1"]
- resultCommit: 9eea5a275fe0b5de934e44dd565029432b11563c
- completion: Python MCP server generator + baboon_mcp_runtime.py + test-gen-python-mcp/test-python-mcp; flag-off byte-identical; reviewer R21 go-ahead after r1 fixes (C1 revert PyBaboonTranslator out-of-scope future-import, C2 real flag-off test). Pre-existing Python issues filed D6/D7. Merged to main (42d6d4c2 + 9eea5a27).
- sessionLogs: ["docs/logs/20260605-000303-a33f488914483727a.md","docs/logs/20260605-003205-afa31bf48e9f7225d.md"]

### T23 — done

- createdAt: 2026-06-05T09:15:48.988Z
- updatedAt: 2026-06-05T11:11:46.818Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: Fix Swift recursive value-type DTO codegen (reference indirection) + full Swift-matrix regression
- description: |
    Resolve defect D8 (root-caused, H1 confirmed): SwDefnTranslator emits every Baboon DTO as a value-type `public struct` (SwDefnTranslator.scala:409-418) with each field a bare stored property via asSwRef (L350-355); `opt[T]` -> Swift `Optional<T>`/`T?` stores its payload INLINE (SwTypeTranslator.scala:63-65), so a recursive DTO such as `Tree { value:i32; left:opt[Tree]; children:lst[Tree] }` (mcp_stub.baboon:58-62) emits `let left: Tree?` directly inside `struct Tree` -> infinite-size value type, rejected by swiftc ('value type Tree cannot have a stored property that recursively contains it'); the transitively-recursive method-input DTO `mcptools.processshape.in` (carries `tree: Tree`, mcp_stub.baboon:117-123) fails identically. `lst[T]` -> `[T]` (Array, heap-backed, SwTypeTranslator.scala:66-68) is NOT the trigger; `opt[Tree]` is. The generator already breaks recursion for ADTs via `public indirect enum` (SwDefnTranslator.scala:822-831) but applies NO equivalent indirection to struct DTOs.
    
    FIX (per D8 suggestedFix): introduce reference-type indirection for recursive DTOs in the Swift generator (SwTypeTranslator/SwDefnTranslator). Either (a) box self-referential DTO fields behind a generated reference type (e.g. a `final class` box) so the struct stores a pointer-sized reference rather than the inline payload, or (b) re-encode recursive DTOs analogously to the existing `indirect enum` ADT path. Detect recursion via the same transitive self-reachability the ADT path already establishes (do NOT box every DTO -- only those that transitively, via opt/direct field, contain themselves). Update ALL co-generated surfaces to match the boxed representation: the init constructor (SwDefnTranslator.scala:388-407), JSON Codable encode/decode (SwJsonCodecGenerator.scala), the UEBA binary codec (SwUEBACodecGenerator.scala), and Equatable/Hashable (currently auto-synthesized via the struct conformance clause -- a reference box may require explicit conformance forwarding so value semantics/equality are preserved).
    
    IMPLEMENTERS MUST re-verify the exact file:line sites above against the current tree before editing (project memory may be dated). Note: editing files under baboon-compiler/src/main/resources/baboon-runtime/swift/ requires `sbt clean` before recompile (PortableResource.embedSources caches resource contents).
    
    SCOPE GUARD: this is a core Swift-codegen representation change that alters generated Swift for ALL recursive-DTO users (baboon's generated code is a versioned external API). It is deliberately scoped as its OWN task (NOT folded into T17's MCP-dispatch scope). Do NOT touch the 8 already-merged per-language MCP replicas (T12-T16, T18) or the approved-but-unmerged T18 work; the only downstream consumer is T17 (Swift MCP), which re-runs after this lands.
- acceptance: "(1) Recursive value-type DTOs compile under Swift: the mcp-stub-ok model (recursive `Tree` + `mcptools.processshape.in`) generates Swift that passes `swift build` clean -- the two infinite-size errors are gone. (2) FULL Swift test matrix regresses green with the new boxed representation: test-gen-swift-regular + test-swift-regular, test-gen-swift-wrapped + test-swift-wrapped, and the swift manual/compat lanes (test-gen-compat-swift / test-manual-swift) all pass -- round-trip JSON and UEBA codecs for recursive DTOs encode/decode correctly and Equatable/Hashable behave (value-equality preserved across the box). (3) sbt +compile green (JVM + .js cross-build), no new exhaustive-match warnings-as-errors. (4) A round-trip (or codec) test exercises a recursive DTO value with a non-trivial `left`/`children` chain and asserts structural round-trip equality with an UNCONDITIONAL throw (asserts are vacuous in Swift release) plus a negative control proving the check is live. (5) Defect D8 is resolvable (its fix tasks complete) once this lands."
- suggestedModel: frontier
- dependsOn: ["T11"]
- ledgerRefs: ["defects:D8","goals:G1"]
- resultCommit: 92ef0547f302d72dde5778bcccddfed0b4f29759
- completion: Box recursive value-type DTO fields behind a new @BaboonIndirect property wrapper (final-class box, value-semantic, Equatable/Hashable-forwarding); generator detects transitive self-recursion via inline edges (scalar/opt) only and boxes exactly those fields; init + JSON/UEBA codecs unchanged (wire surface preserved). Wired McpStub into the Swift matrix + added RecursiveDtoRoundTripTests. swiftc -typecheck clean on generated McpStub, full Swift matrix green, sbt +compile green. Reviewer R23 go-ahead round 0. Merged ff to main.
- sessionLogs: ["docs/logs/20260605-110151-a8b454bdb3a4908d2.md","docs/logs/20260605-111100-a4882ff98e5f4468b.md"]

### T29 — done

- createdAt: 2026-06-05T18:09:41.421Z
- updatedAt: 2026-06-05T20:10:50.733Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "FIX D4+D5: route Scala JSON get(Key)Decoder through codecName + remove Scala test workarounds"
- description: "Resolve D4 AND D5 (both confirmed; same root-cause family, H6/H7) in ONE Scala-codec task. ScJsonCodecGenerator interpolates codec references as bare string-concat where the import collector harvests imports ONLY from ScType nodes (ScBaboonTranslator.scala:231-263), so the `_JsonCodec` suffix is never imported and renders unresolved: D4 — getDecoder (ScJsonCodecGenerator.scala:550-556) emits bare `${targetTpe}_JsonCodec.circeDecoder` on a foreign type (FFancyStr, which has no companion); D5 — getKeyDecoder's enum branch (:472-481) references `${targetTpe}_JsonCodec.instance` on a bare enum (Color) unresolved from a nested package. FIX: route ALL THREE sites through the existing `codecName` helper (:598-600) so the emitted node is a real imported `<X>_JsonCodec` ScType, mirroring the encode/UEBA/_KeyCodec idioms — :481 (enum map-key decoder), :552 and :556 (value decoders). THEN remove the two test workarounds: the FFancyStrShim (test/scala-stub-mcp-overlay) and the listcollections package-object re-export. Re-verify file:line sites against the current tree before editing."
- acceptance: "(1) All three ScJsonCodecGenerator decoder sites (:481, :552, :556) route through codecName, emitting resolvable imported `_JsonCodec` ScTypes; (2) the FFancyStrShim AND the listcollections package-object re-export test workarounds are REMOVED; (3) generated Scala for mcp-stub-ok (foreign FFancyStr + map[Color,_] in nested packages) compiles without the shims; (4) the full Scala matrix is green AND `sbt +compile` (JVM + Scala.js) green; (5) `mdl :build :test` AND `mdl :ci` stay green; (6) BOTH D4 and D5 are resolvable once this lands."
- suggestedModel: frontier
- dependsOn: []
- ledgerRefs: ["defects:D4","defects:D5","goals:G1"]
- resultCommit: 390fec58
- completion: "D4+D5: routed all 3 ScJsonCodecGenerator decoder sites (:481 enum map-key, :552/:556 value) through the codecName helper so each emits a real imported <X>_JsonCodec ScType; removed the FFancyStrShim + listcollections package-object test workarounds. mcp-stub-ok Scala compiles WITHOUT shims; scala matrix + sbt +compile green. Reviewer R33 go-ahead. Merged to main. BOTH D4 and D5 resolved."
- sessionLogs: ["docs/logs/20260605-190539-a910752312cc9761b.md","docs/logs/20260605-193846-a0d12613d4a1f4c83.md"]

### T30 — done

- createdAt: 2026-06-05T18:09:51.326Z
- updatedAt: 2026-06-05T20:10:57.473Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "FIX D6: Python enum map-key codec arm + reconcile MCP/OAS inputSchema for enum-keyed maps"
- description: "Resolve D6 (confirmed, H8 source-validated). The Python JSON codec's mkJsonKeyEncoder (PyJsonCodecGenerator.scala:317-345) has identifier-DTO / 1-field-DTO / Foreign arms but NO Typedef.Enum arm, so an enum map key falls to `case _ =>` and emits a BUG-throw (:344); the symmetric mkJsonKeyDecoder throws at :439; fieldHasUserKeyMap (:222-231) forces enum-keyed maps through this key-walker — so a non-empty map[Color,str] raises at runtime (the T18/Kotlin round-trips used `byColor:{}` to dodge it). Scala HAS the arm (ScJsonCodecGenerator.scala:317-321, stringifies). NUANCE: the MCP/OAS inputSchema declares non-string-keyed maps as an ENTRY-ARRAY of {key,value} (OasTypeTranslator.scala:141-159) while the DATA codecs (Python :277-284, Scala :402-405) emit a STRING-KEYED object — a schema-vs-codec mismatch. FIX: (a) add a Typedef.Enum arm to BOTH mkJsonKeyEncoder (stringify the enum, e.g. $ref.value) and mkJsonKeyDecoder (reconstruct via the enum class), mirroring Scala, so map[Color,str] round-trips as a string-keyed JSON object; (b) reconcile the MCP/OAS inputSchema for non-string-keyed maps with ENUM keys to the string-keyed-object form (object with enum-named keys) so the generated tool inputSchema matches the actual wire; (c) replace the `byColor:{}` test dodge with a NON-empty value round-trip. Re-verify file:line sites against the current tree before editing."
- acceptance: "(1) Python mkJsonKeyEncoder/Decoder have a Typedef.Enum arm so map[Color,str] round-trips as a string-keyed JSON object; (2) the MCP/OAS inputSchema for enum-keyed maps is reconciled to the string-keyed-object form matching the wire; (3) the `byColor:{}` test dodge is REPLACED with a non-empty byColor round-trip that round-trips AND validates against the inputSchema; (4) `mdl :build :test` AND `mdl :ci` stay green; (5) D6 is resolvable once this lands."
- suggestedModel: frontier
- dependsOn: []
- ledgerRefs: ["defects:D6","goals:G1"]
- resultCommit: c6a8bc1b
- completion: "D6: added a Typedef.Enum arm to Python mkJsonKeyEncoder/Decoder (.value encode / Color(k) decode) so map[Color,str] round-trips as a string-keyed object; reconciled the MCP (McpInputSchemaEmitter) + OAS (OasTypeTranslator) inputSchema for enum-keyed maps to the string-keyed-object form (object+additionalProperties+propertyNames{enum}) matching the wire; replaced byColor:{} test dodges with non-empty round-trips across all 9 MCP overlays + golden. Reviewer R34 go-ahead (cs NJsonSchema + ts ajv validity gates green on the non-empty instance; all 9 MCP lanes green). Merged to main (cherry-pick auto-merged the 3 files shared with T26/T27/T29 cleanly). One low out-of-scope OAS parity finding filed as D10."
- sessionLogs: ["docs/logs/20260605-190539-a8e4fc2c9b8094a16.md","docs/logs/20260605-193846-a41a7c7296eb96cd3.md"]

### T31 — done

- createdAt: 2026-06-05T18:10:01.547Z
- updatedAt: 2026-06-05T20:11:01.146Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "FIX D7: emit `from __future__ import annotations` unconditionally + update Python baseline"
- description: "Resolve D7 (confirmed, H9 ledger-corroborated; Q7 approved this flag-off baseline change). The Python generator emits DTO fields as bare unquoted annotations (PyDefnTranslator.scala:532-541); opt[Tree] -> Optional[Tree] with Tree a bare token (PyTypeTranslator.scala:33-36, PyTypes.scala:145); the self-type is filtered from imports and rendered bare (PyBaboonTranslator.scala:244-248/:301-303); and renderTree's module preamble (PyBaboonTranslator.scala:297-299) prepends NO `from __future__ import annotations`, so a recursive DTO (e.g. the T22 stub's Tree { left: opt[Tree], children: lst[Tree] }) raises NameError under pydantic v2 on py3.12. FIX: emit `from __future__ import annotations` UNCONDITIONALLY as the FIRST line of every generated Python module in PyBaboonTranslator.renderTree (PEP 563 makes all annotations lazy strings, eliminating every forward-ref/ordering hazard incl. mutual recursion, no recursion-detection needed). This changes the flag-off Python baseline for all files — update the Python regular/wrapped generated baseline accordingly (no checked-in golden; the lanes run `python3 -m unittest` against the generated py-stub, which must import/run under py3.12). Remove any test-harness future-import shim now that the generator emits it. NOTE T18/R21 previously REVERTED a renderTree future-import precisely because it changes flag-off output — Q7 now authorizes it. Re-verify file:line sites against the current tree before editing."
- acceptance: "(1) PyBaboonTranslator.renderTree emits `from __future__ import annotations` as the first line of EVERY generated Python module unconditionally; (2) the Python regular/wrapped baseline is updated accordingly and the py lanes (`python3 -m unittest` against generated py-stub) import/run green under py3.12; (3) a recursive-DTO round-trip test (the recursive Tree model) imports + round-trips under py3.12; (4) any test-harness future-import shim is REMOVED; (5) `mdl :build :test` AND `mdl :ci` stay green (the intentional Q7-approved Python baseline change is the ONLY off-by-default output delta); (6) D7 is resolvable once this lands."
- suggestedModel: frontier
- dependsOn: []
- ledgerRefs: ["defects:D7","goals:G1"]
- resultCommit: a53e6499
- completion: "D7: emit `from __future__ import annotations` UNCONDITIONALLY as the first line of every generated Python module in PyBaboonTranslator.renderTree (PEP 563) -> recursive pydantic models no longer NameError on py3.12; removed the test-harness future-import shim from .mdl/defs/tests.md; added a recursive Tree round-trip test. Reviewer R35 go-ahead (future-import line 1 of all 13 modules, repro confirmed, shim removed). Q7-approved baseline change. Merged to main."
- sessionLogs: ["docs/logs/20260605-190539-a24401ae895125d77.md","docs/logs/20260605-193846-a67dcf443ab9b0dbb.md"]

### T32 — done

- createdAt: 2026-06-05T20:19:10.177Z
- updatedAt: 2026-06-05T21:10:27.271Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: FIX D10 — OAS enum-keyed-map inputSchema test coverage + optional defensive hardening
- description: |
    Resolve D10 (root-caused; investigation H10 REFUTED the alleged OAS-vs-MCP enum-keyed-map divergence). NO behavior bug exists on current code: OasBaboonTranslator.renderDto:165 PRE-RESOLVES every field tpe via resolveTypeRef, which recurses into map-constructor args (OasTypeTranslator.scala:40-41), so a foreign-rt-to-enum map key reaches OasTypeTranslator.mapSchema (OasTypeTranslator.scala:146-160) already resolved to the enum -> enumKeys.contains(id) matches -> the SAME string-keyed-object schema MCP produces. The GENUINE residual is twofold, both addressed here:
    
    (a) REQUIRED — close the coverage gap. The only enum-keyed map in the corpus is `byColor: map[Color,str]` in the MCP stub (mcp_stub.baboon:112); no OAS-exercised model/golden covers the T30 OAS string-keyed-object branch (OasTypeTranslator:149-151), and no Oas* unit test exists. Add an OAS-exercised model + golden that includes an enum-keyed map (ideally ALSO a foreign-rt-to-enum-keyed map) so the OAS string-keyed-object branch is exercised AND the renderDto:165 caller-pre-resolution invariant is pinned by a real test (so future refactors of the pre-resolution layer cannot silently regress OAS map-key correctness). Assert the emitted component schema for the enum-keyed map is the string-keyed-object form (type:object + additionalProperties + propertyNames{enum:[wire-values]}), matching the MCP/T30 wire form.
    
    (b) OPTIONAL — defensive hardening. Harden OasTypeTranslator.mapSchema to resolve the key ref itself (mirror the MCP enumKey helper using resolveTypeRef) so OAS correctness is LOCAL rather than silently relying on the caller pre-resolving. If done, keep the existing renderDto pre-resolution intact (belt-and-suspenders) and ensure no double-resolution regression.
    
    Read D10's rootCause/suggestedFix (defects:D10). Resolves D10.
- acceptance: "(1) New OAS coverage exercises an enum-keyed map and asserts the emitted component schema is the string-keyed-object form (type:object + additionalProperties + propertyNames{enum:[wire-values]}), parity with the MCP/T30 wire form; ideally also covers a foreign-rt-to-enum-keyed map pinning the renderDto:165 pre-resolution invariant. (2) If hardening (b) is applied, OasTypeTranslator.mapSchema resolves the key ref locally and OAS output is unchanged (no regression). (3) `mdl :build :test` and `mdl :ci` green (sbt +compile JVM+Scala.js, full test matrix incl. test-openapi). (4) D10 resolvable. (5) Leave done tasks T1-T31 untouched; diff confined to OAS test resources/golden (+ optional OasTypeTranslator.mapSchema)."
- suggestedModel: standard
- ledgerRefs: ["defects:D10","goals:G1"]
- resultCommit: 7f2e7f383de654d5b70d7367619eb766cd4a759f
- completion: "D10 OAS enum-keyed-map coverage: added OasEnumKeyMapSchemaTest (6 cases) directly exercising OasTypeTranslator — enum-key -> type:object+additionalProperties+propertyNames($ref Color); str/uid -> no propertyNames; i32 -> entry-array; and Case 4 drives the production resolveTypeRef(Constructor(map,[foreignRef,val])) path (mirroring renderDto:165) so the foreign-rt-to-enum pre-resolution invariant is pinned (mutation-confirmed: no-op'ing the Constructor arg-recursion fails Case 4). No generator change needed (D10's alleged divergence was refuted). Reviewer R38 go-ahead after 1 criticism round. Merged ff to main. D10 resolved."
- sessionLogs: ["docs/logs/20260605-204502-a38bdcc823454fcbf.md","docs/logs/20260605-210159-ae4cbe4cf44e51694.md","docs/logs/20260605-210159-a8c8316a4ae4d9e22.md"]
