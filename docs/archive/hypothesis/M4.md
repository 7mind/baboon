## M4

### H1 — confirmed

- createdAt: 2026-06-05T09:07:00.340Z
- updatedAt: 2026-06-05T09:09:44.286Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: SwDefnTranslator emits recursive Baboon value-type DTOs as Swift `struct`s without indirection, producing infinite-size value types rejected by swiftc
- description: "What would make this true: the Swift definition translator renders Baboon DTOs as value-type `struct`s, and for a recursive DTO (a DTO that, transitively via opt[T]/lst[T]/direct field, contains itself) emits stored properties of the recursive type directly into the struct body with no reference-type indirection (no boxing via final class, no indirect-enum encoding). swiftc rejects a value-type struct that recursively contains itself ('value type X cannot have a stored property that recursively contains it' / 'infinite size'). Confirm by locating SwDefnTranslator/SwTypeTranslator DTO emission and showing recursive field types are emitted as bare struct stored properties."
- ledgerRefs: ["defects:D8"]
- evidence: ["[correct] SwDefnTranslator.scala:409-418 — DTOs emitted as `public struct ${name}: Equatable, Hashable, $conformanceClause { ... }` (value type, no indirection).","[correct] SwDefnTranslator.scala:350-355 — each field becomes a bare stored property `public let $escaped: $t` from `trans.asSwRef(f.tpe,...)`; a self-typed field lands directly in the struct.","[correct] SwTypeTranslator.scala:63-65 — `opt[T]` → `q\"$inner?\"` (Swift Optional, stored inline) — `left: opt[Tree]` → `let left: Tree?`, the infinite-size trigger.","[correct] SwTypeTranslator.scala:66-68 — `lst[T]` → `q\"[$inner]\"` (Array, heap-allocated); `children: lst[Tree]` alone would NOT cause infinite size.","[correct] mcp_stub.baboon:58-62 — `data Tree { value: i32; left: opt[Tree]; children: lst[Tree] }` — direct nullable self-reference.","[correct] mcp_stub.baboon:117-123 — `def processShape(data in { shape: Shape; tree: Tree })` → `mcptools.processshape.in` carries `tree: Tree` (second infinite-size DTO).","[correct] SwDefnTranslator.scala:822-831 — ADTs use `public indirect enum ${name}` — indirection mechanism exists for enums but not for struct DTOs (asymmetry).","[correct] SwDefnTranslator.scala:388-407 — init constructor emission (fix blast radius); Equatable/Hashable auto-synthesized via the struct clause; codec field access in SwJsonCodecGenerator.scala / SwUEBACodecGenerator.scala."]
- sessionLogs: ["docs/logs/20260605-090917-aca6d3712dc5065b7.md"]

### H6 — confirmed

- createdAt: 2026-06-05T17:52:33.035Z
- updatedAt: 2026-06-05T18:05:15.312Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "D4: the Scala generator emits a bare `import mcp.stub.{... FFancyStr ...}` for a foreign type mapped to a host primitive, but emits no bare `object/type FFancyStr` (only _JsonCodec/_KeyCodec/_UEBACodec), so the import does not resolve"
- description: "Confirm the Scala import emission includes the foreign-type bare name while the foreign-type file emits only codec objects (no bare object/type). Pin file:line of the import emission + the foreign-type rendering + the fix site (suppress bare import for primitive-mapped foreigns, or emit a companion)."
- ledgerRefs: ["defects:D4"]
- evidence: ["[correct] ScJsonCodecGenerator.scala:550-556 (orchestrator-validated) — getDecoder interpolates bare `${targetTpe}_JsonCodec.circeDecoder` (string concat), so the import collector harvests the bare foreign ScType node.","[correct] ScDefnTranslator.scala:476-482/:505-541 — a Custom foreign emits only _KeyCodec/_JsonCodec/_UEBACodec, NO bare object/type FFancyStr.","[correct] ScBaboonTranslator.scala:232-263 — imports harvested only from ScType nodes -> bare FFancyStr imported, unresolvable.","[correct] ScJsonCodecGenerator.scala:386-392 + :598-600 (codecName helper, validated) — the correct sibling idiom; FIX = `${codecName(targetTpe)}.circeDecoder`.","[correct] FFancyStrShim.scala:3-21 — overlay workaround confirms generated code doesn't compile without a bare object FFancyStr."]
- sessionLogs: ["docs/logs/20260605-180334-a90489e269db1b75f.md"]

### H7 — confirmed

- createdAt: 2026-06-05T17:52:36.792Z
- updatedAt: 2026-06-05T18:05:20.503Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "D5: in a nested-package codec file, the Scala generator references Color_JsonCodec (map-with-enum-key decoder) but does not add it to the import set (only Color, Color_UEBACodec), so the bare name doesn't resolve"
- description: "Confirm the map-with-enum-key JSON decoder references the enum's _JsonCodec by bare name while the import set for that nested-package codec file omits it. Pin file:line of the decoder reference + the import-set computation + the fix site (add _JsonCodec to imports or fully-qualify)."
- ledgerRefs: ["defects:D5"]
- evidence: ["[correct] ScJsonCodecGenerator.scala:472-481 (orchestrator-validated) — getKeyDecoder enum branch references `${targetTpe}_JsonCodec.instance` by bare string-concat on the bare enum ScType.","[correct] ScBaboonTranslator.scala:231-239/:283-290 — import set + final render collect ONLY ScType nodes, so the string-suffix `_JsonCodec` gets no import and renders unresolved from the nested package.","[correct] ScUEBACodecGenerator.scala:503-508/:666-668 — UEBA routes the same ref through codecName -> real `_UEBACodec` ScType -> imported (the asymmetry).","[correct] ScJsonCodecGenerator.scala:598-600 — codecName helper = the minimal-fix vehicle. FIX = route line 481 through codecName (same fix family as D4)."]
- sessionLogs: ["docs/logs/20260605-180334-a2d685b9d26f843b9.md"]

### H8 — confirmed

- createdAt: 2026-06-05T17:52:39.713Z
- updatedAt: 2026-06-05T18:05:26.672Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "D6: the Python JSON codec does not round-trip the {key,value} entry-array shape for a non-string-keyed map (map[Color,str]); the encode/decode path mishandles the enum-keyed-map representation"
- description: "Confirm how the Python JSON codec encodes/decodes a map with a non-string (enum) key vs the OAS/MCP entry-array representation; pin the file:line of the Python map codec path + the fix site to align non-string-keyed-map handling."
- ledgerRefs: ["defects:D6"]
- evidence: ["[correct] PyJsonCodecGenerator.scala:317-345 (orchestrator-validated) — mkJsonKeyEncoder has identifier-DTO/1-field-DTO/Foreign arms but NO Typedef.Enum arm -> enum key hits `case _ =>` BUG-throw at :344.","[correct] PyJsonCodecGenerator.scala:396-439 — symmetric mkJsonKeyDecoder throws at :439; so map[Color,str] raises at runtime.","[correct] PyJsonCodecGenerator.scala:222-231 — fieldHasUserKeyMap forces enum-keyed maps through the explicit key-walker.","[correct] ScJsonCodecGenerator.scala:317-321 — Scala HAS the enum map-key arm (stringifies); Python's missing arm is the defect.","[nuance] OAS schema declares non-string-keyed maps as entry-array (OasTypeTranslator.scala:141-159) but both Py (:277-284) and Scala (:402-405) DATA codecs emit a STRING-KEYED object — the fix must reconcile the Python enum-key arm with the inputSchema representation."]
- sessionLogs: ["docs/logs/20260605-180334-a0ec413510e4eb3f5.md"]

### H9 — confirmed

- createdAt: 2026-06-05T17:52:43.365Z
- updatedAt: 2026-06-05T18:05:31.614Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "D7: a recursive/self-referential Baboon DTO generates Python pydantic v2 models that raise NameError on py3.12 because the forward self-reference isn't resolvable without `from __future__ import annotations` (PEP 563)"
- description: "Confirm the Python generator (PyBaboonTranslator renderTree / model emission) does NOT emit `from __future__ import annotations` for files with recursive/self-referential types, so a forward self-ref (e.g. left: Optional[Tree]) raises NameError under py3.12. Pin the file:line of the Python model/file emission + the fix site (emit the future-import for recursive files + update the regular-adt baseline)."
- ledgerRefs: ["defects:D7"]
- evidence: ["[correct] PyBaboonTranslator.scala:297-299 — renderTree module preamble = imports + class body; NO `from __future__ import annotations`.","[correct] PyDefnTranslator.scala:532-541 — DTO fields emitted as bare `name: <type>` annotations (not quoted forward-refs).","[correct] PyTypeTranslator.scala:33-36 + PyTypes.scala:145 — opt[Tree] -> Optional[Tree] with Tree a bare token.","[correct] PyBaboonTranslator.scala:244-248/:301-303 — self-type filtered from imports + rendered bare -> unbound name at class-construction.","[correct] baboon_runtime_shared.py:5-19 — no future-import, no model_rebuild/update_forward_refs -> pydantic v2 NameErrors on py3.12. FIX = emit `from __future__ import annotations` (recommend unconditional) at renderTree preamble + update baseline (Q7 approved)."]
- sessionLogs: ["docs/logs/20260605-180334-acb345f878fd62012.md"]

### H10 — wrong

- createdAt: 2026-06-05T20:12:04.070Z
- updatedAt: 2026-06-05T20:17:00.586Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "D10: OasTypeTranslator.mapSchema matches enum map-keys by a DIRECT TypeRef.Scalar(User) in enumKeys test, while McpInputSchemaEmitter resolves the key ref through foreign-rt (resolveTypeRef) first -> a foreign-resolving-to-enum map key is reconciled to string-keyed-object by MCP but emitted as the old entry-array by OAS; + no OAS test exercises an enum-keyed map"
- description: "Confirm against current source (post-T30 c6a8bc1b): (1) OasTypeTranslator.mapSchema enum-key branch uses a direct enumKeys.contains(id) test without foreign-rt resolution; (2) McpInputSchemaEmitter's enum-key handling DOES resolve through resolveTypeRef/foreign rt first; (3) the divergence: a foreign type whose runtime mapping resolves to an enum is handled by MCP (string-keyed object) but not OAS (entry-array). Pin the OAS fix site (route the enum-key match through the same resolution) + confirm no OAS test model uses an enum-keyed map (coverage gap)."
- ledgerRefs: ["defects:D10"]
- evidence: ["[correct] OasTypeTranslator.scala:146-160 — OAS mapSchema enum-key match is a DIRECT enumKeys.contains(id) test, no resolveTypeRef inside mapSchema (H10 part-1 mechanics true).","[correct] McpInputSchemaEmitter.scala:278-309 — MCP enumKey resolves the key via resolveTypeRef first (H10 part-2 contrast true).","[correct] OasBaboonTranslator.scala:165 + OasTypeTranslator.scala:40-41 (orchestrator-validated) — OAS renderDto PRE-RESOLVES every field tpe via resolveTypeRef, which recurses into map-constructor args, so a foreign-rt-to-enum map key reaches mapSchema ALREADY resolved -> enumKeys.contains matches -> string-keyed object, SAME as MCP. REFUTES the divergence (no entry-array bug).","[correct] BaboonValidator.scala:238-241 + Typedef.scala:60 — a foreign with rt=Enum is legal and accepted as a map key at face value, so the INPUT is constructible, but the divergent OUTPUT is unreachable on current code.","[correct] mcp_stub.baboon:112 — the ONLY enum-keyed map in the corpus is byColor: map[Color,str] (in the MCP stub); no OAS-exercised model/golden covers the T30 OAS string-keyed-object branch (coverage gap CONFIRMED)."]
- sessionLogs: ["docs/logs/20260605-201632-acc5506e1d98acebd.md"]
