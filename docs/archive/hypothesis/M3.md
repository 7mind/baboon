## M3

### H5 — confirmed

- createdAt: 2026-06-05T17:52:28.799Z
- updatedAt: 2026-06-05T18:05:09.204Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: "D3: the TS generator iterates an unordered Set/HashMap when collecting imports, producing run-to-run-varying import ORDER for identical inputs"
- description: "Confirm the TS import-collection path uses an unordered collection (Set/HashMap) whose iteration order varies; pin the file:line where imports are collected/rendered + the fix site (sort by module path then symbol before render)."
- ledgerRefs: ["defects:D3"]
- evidence: ["[correct] TsBaboonTranslator.scala:461 — module-line order via `groupBy(_.moduleId).toList.sortBy{path.size+types.size}.reverse` (NON-injective key; ties broken by hash-map iteration order).","[correct] TsBaboonTranslator.scala:467-473 — symbols within {} follow groupBy order, no lexical sort; :498 renders without re-sort.","[correct] TsBaboonTranslator.scala:566-568 — the barrel path already sorts (`names.toList.sorted`) = the fix model.","[refined] root cause is non-deterministic/non-lexical import ORDER (hash-derived; stable per-binary but unstable across toolchain) rather than literal per-run variation — the underlying ordering defect is real and fixable by a deterministic lexical sort."]
- sessionLogs: ["docs/logs/20260605-180334-a70ee77bb43014030.md"]
