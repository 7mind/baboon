## M5

### H2 — confirmed

- createdAt: 2026-06-05T12:18:57.577Z
- updatedAt: 2026-06-05T12:24:16.354Z
- author: "opus-4.8[1m]"
- session: 91173875-8554-45c0-a418-1837a6648f6f
- headline: Wiring/regular/wrapped test actions use --model-dir = the shared resources/baboon/ root, which now contains mcp-stub-ok; they generate the MCP stub model's cross-language FIXTURES, which emit unqualified cross-namespace refs (Color/PointPage_Fixture/FFancyStr) and fail to compile in sc/rs/kt/jv
- description: "What would make this true: (1) the failing wiring lane actions (test-gen-sc-wiring-*, -rs-wiring-*, -kt-wiring, -jv-wiring) invoke baboon codegen with --model-dir pointing at the SHARED resources/baboon/ root (not a curated subset), so mcp-stub-ok/mcp_stub.baboon is included; (2) codegen for that model emits per-language fixture files (In_Fixture.* / *_Fixture) that reference Color, PointPage_Fixture, and FFancyStr without a resolvable import/qualification in the fixture file's namespace; (3) those unqualified refs are the compile errors. ALSO survey the FIX SURFACE: the exact --model-dir of each of the 9 test-gen-<lang>-mcp lanes, the JVM shape tests' model paths, and how T23 wired McpStub into the swift matrix (test/sw-stub/Package.swift) so a relocation can preserve the recursive-Tree coverage."
- ledgerRefs: ["defects:D9"]
- evidence: ["[correct] tests.md:1262-1276 — test-gen-sc-wiring-either: `--model-dir ./baboon-compiler/src/test/resources/baboon/` (shared root, recursively includes mcp-stub-ok) + `--fixture-output` (L1269). Same pattern for the other 7 failing lanes: sc-result/outcome/hkt, rs-either/result (1878-1885/1927-1934), kt-wiring (2218-2225), jv-wiring (2265-2272).","[correct] tests.md:1610-1622 (ts-mcp) + 2686-2700 (python-mcp) — MCP lanes use `--model-dir .../baboon/mcp-stub-ok/` (curated subdir) and pass NO --fixture-output → unaffected. The structural asymmetry that confines the failure to the full-model-dir+fixture lanes.","[correct] mcp_stub.baboon — declares Color (enum), Page[T]/PointPage (template+alias), FFancyStr (foreign); these reach the fixture generator via service request DTOs (Nested.color, submitComposite.in.fancy, pagePoints.in.page) in mcp.stub sub-namespaces → cross-namespace fixture refs.","[correct] SwCodecFixtureTranslator.scala:270-281 — Swift fixture emitter QUALIFIES cross-namespace user-type refs (effectiveSwPkg + importAs); this is why Swift is NOT in the failing set, and by contrast the sc/rs/kt/jv fixture emitters lack the qualification.","[correct] Package.swift:162-181 + RecursiveDtoRoundTripTests.swift:20,31-35 — T23 wired the McpStub target into the Swift matrix (RuntimeTests dependsOn McpStub); the recursive coverage uses ONLY `Tree` (never Color/Page/FFancyStr), so a minimal recursive-only model in the shared dir preserves it without the cross-namespace fixture problem.","[correct] ledger defects:D4/D5 — prior reproduction: mcp-stub Scala main codegen emits unresolvable cross-namespace refs (FFancyStr bare import; Color_JsonCodec not imported) under the plain Scala flags the wiring lanes use; same failure family. Proximate 8-lane compile failure independently reproduced by the T21 worker's full `mdl :test` run."]
- sessionLogs: ["docs/logs/20260605-122341-a4defd6a9945d84f7.md"]
