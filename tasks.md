# Baboon — Task Ledger

Authoritative ledger of planned and completed work. Governing brief: pre-M29 CI hardening blocks BAB-A04 (user-defined generics) per user 2026-05-03 ("we will only proceed to generics once CI is green").

> **Predecessor ledgers (frozen):** `docs/archive/20260503-bab-any-anyopaque-ledgers/{tasks,defects}.md`
> Cover M1–M28 (`any`/AnyOpaque, identifiers, ADT inheritance, JSON/UEBA codecs, map-key encoding, wire-form canonicalisation). Locked invariants there remain authoritative.

Status: `[ ]` planned · `[~]` in progress · `[x]` done · `[!]` blocked

---

## Hotfixes

- [x] **PR-29.10b** (2026-05-04) — service-wiring translators across all 9 backends made tolerant of `TypeId.BuiltinScalar` in method positions. Fixes the CI `ClassCastException` exposed by m29-ok's `Crud[i32, str]` templated service. Resolves [PR-29.10-D07] (end-to-end templated-service cross-language acceptance — petstore now exercises Crud through the live service-acceptance matrix). Verification: `mdl :test-service-acceptance` 81/81 with the fresh binary post `sbt clean;compile`.

---

## Milestones (high-level)

- [x] **M29-prep** — Pre-M29 CI hardening. Two defects fixed: CI-01 (BaboonRuntimeCodec tso UTC-zero `+00:00`↔`Z`), CI-02 (acceptance-tests sbt resolution under `--ignore-environment`). Both CI jobs green on `wip/ids-and-adts` (verified by user 2026-05-03; branch merged into `wip/generics`).
- [x] **M29-prep follow-ups** — PR-29P.4 landed both deferred items (D02 non-UTC tso coverage, D03 RandomJsonGenerator tso/tsu split). Surgical, additive, no wire-format impact.
- [x] **M29** — User-defined generics (BAB-A04). **Complete 2026-05-04**. 12 PRs landed (29.1, 29.1a, 29.2..29.11). The architectural bet held end-to-end: monomorphisation runs as a typer-early raw-AST rewrite; codegen never sees a template; all 9 backends produce concrete code with no synthetic generic identifiers. Cross-language wire-format acceptance 200/200. Two follow-ups deferred: tree-sitter editor grammar (`[PR-29.8-D01]` — needs submodule coordination) and end-to-end cross-language service-acceptance for templated services (`[PR-29.10-D07]`).

---

## Milestone M29-prep — PR breakdown

Detail: see `docs/archive/20260503-bab-any-anyopaque-ledgers/` for historical context, especially M28 close-out (`docs/logs/20260502-2247-m28-wireform-parity-and-docs-log.md`) which explicitly flagged PR-28.4-D02 (the prime suspect for CI-01).

- [x] **PR-29P.1** — Fix CI-01: `BaboonRuntimeCodec` tso UTC-zero `+00:00` ↔ `Z` divergence (M28-N01 violation). Round-1 hypothesis was wrong (PR-28.4-D02 generator map-key sort) — round-1 executor escalated correctly. Round-2 fix: split shared `isoFormatter` into `tsoFormatter`/`tsuFormatter` in `BaboonRuntimeCodec.scala`; widen encode parse to `ISO_OFFSET_DATE_TIME`; split decode arm; deterministic regression test. Local close gates all green. Archived PR-28.4-D02 stays deferred — separate latent issue, not the cause of CI-01.
- [x] **PR-29P.2** — Fix CI-02: `acceptance-tests` sbt 1.11.7 resolution `forbidden` inside `nix develop --ignore-environment`. **Shipped option (a)** — dropped `--ignore-environment` + four `--keep` flags from both acceptance-tests steps. Pending live-CI verification (gate runs in PR-29P.3).
- [x] **PR-29P.3** — Close-out: live-CI verification on `wip/ids-and-adts` (both jobs green per user 2026-05-03; branch merged into `wip/generics`); added deferred-policy comment to workflow per `[PR-29P.2-D05]`; ticked `[PR-29P.2-D06]` cross-cutting policy item; recorded M29 follow-ups (D02 non-UTC tso test coverage, D03 RandomJsonGenerator parity); session log written.

---

## Milestone M29-prep follow-ups — PR breakdown

Bundles the two surgical deferred items from PR-29P.1 review into one PR. Both are additive and have no wire-format impact.

- [x] **PR-29P.4** — Applied formatter split + non-UTC tso coverage. Resolves `[PR-29P.1-D02]` (RTCodecTest +05:30/-08:00 cases) + `[PR-29P.1-D03]` (RandomJsonGenerator tsoFormatter/tsuFormatter split mirroring `BaboonRuntimeCodec`). Verified `sbt baboonJVM/testOnly RTCodecTest` PASS (6/6 incl. both new cases) + `sbt baboonJS/compile` PASS.

---

## Milestone M29 — PR breakdown

Detail: per-PR breakdown will live in `docs/drafts/<YYYYMMDD-HHMM>-m29-generics-plan.md` (seeded by O1 planning subagent after PR-29P.4 lands). PR-29.1 ships the user-facing spec at `docs/spec/generics.md`. Locked design decisions from user 2026-05-03:

1. ADTs, Contracts, Services are all generifiable. AST-level templating handles them uniformly.
2. Self-reference is forbidden. Only DAG-shaped template-and-instantiation graphs are valid.
3. The **only** way to instantiate is through a type alias. Field-position instantiation and nested instantiation in alias RHS are forbidden — intermediate aliases required.
4. Monomorphisation identifier = alias id. No synthetic id `X<i32>` exposed.
5. Templates not subject to migration; instances (concrete `Typedef.User` under alias id) are.
6. `: derived[…]` written **only on the alias**, propagates to monomorphisation.

- [x] **PR-29.1** — Spec + grammar + decision doc `docs/spec/generics.md`.
- [x] **PR-29.2** — Parser: type-param head on declarations.
- [x] **PR-29.3** — Parser: type alias instantiation RHS + alias-side `derived` extension (scope-expanded inline per `[PR-29.3-D01]`).
- [x] **PR-29.4** — Typer: template registry; templates never become `DomainMember`. First M29 `TyperIssue` (`DuplicateTypeParam`) with 3-site exhaustive-match update. JS cross-build canary green.
- [x] **PR-29.1a** — Spec §4/§6 reframing landed. §4 reworked into §4.1-§4.4: matrix #5 reframed as a special case of matrix #1 / decision #3; both direct (`X[T] { rec: X[T] }`) and container-mediated (`Tree[T] { children: lst[Tree[T]] }`) forms rejected on the same grounds. New §6 item flags the auto-rewrite path (`type K = Tree[i32]` does NOT rescue in-body `Tree[T]`) as deliberately out-of-scope. §6 also relocated matrix #1/#2 entries — they were "out of scope" but are actually forbidden; covered by §2.5.1/§2.5.2.
- [x] **PR-29.5** — Typer: monomorphisation via AST substitution; alias-id canonical. The M29 architectural meat: every `type Y = X[Foo]` materialised at compile time into ordinary `RawTLDef.{DTO|ADT|Contract|Service}` whose identity is the alias's name. Codegen never sees a template. Two new TyperIssue cases (`TemplateArityMismatch`, `TemplateInstantiationInBody`) with 3-site update.
- [x] **PR-29.6** — Typer: dependency ordering / cycle diagnostic. **Plan revised mid-PR**: orchestrator's first hypothesis (existing typer toposort catches matrix #9 via `CircularInheritance`) was wrong — `hardDepsOfRawDefn` only walks inheritance edges, not field references. Final disposition: matrix #9 is caught by `BaboonValidator.checkLoops` as `VerificationIssue.ReferentialCyclesFound` (post-typer, post-substitution); reuse this existing validator-side diagnostic rather than introducing `TemplateInstantiationCycle`. Test + spec note locked.
- [x] **PR-29.7** — Validator: forbidden positions + new `TyperIssue` cases for matrix items #7 (`TemplateNotInstantiated`), #8 (`NotATemplate`), and `[PR-29.5-D03]` body-side `derived` validation (`TemplateBodyCarriesDerived` at registry-build time). Matrix #1/#2/#5 covered by PR-29.5's `TemplateInstantiationInBody` (reused, possibly misnamed for #2 — deferred polish in `[PR-29.7-D05]`). Matrix #6 reuses existing `NameNotFound` per CLAUDE.md §4 simplicity. Matrix #4 (PR-29.4), #3 (PR-29.5), #9 (PR-29.6) already covered. `TemplateArityMismatch.ownerName` renamed to `aliasName` per `[PR-29.5-D09]`.
- [x] **PR-29.8** — LSP polish (HoverProvider, DefinitionProvider, CompletionProvider) + 6 new LSP tests + template fixture. **Tree-sitter editor grammar deferred** to a dedicated PR per `[PR-29.8-D01]` — the tree-sitter changes live in a 3-level submodule chain that requires user-authorised pointer bumps across `editors/baboon-zed` and its inner `grammars/baboon` submodule.
- [x] **PR-29.9** — Cross-language emission smoke fixture `m29-ok/m29.baboon` exercises `Page[T]`, `Envelope[T, E]` (incl. ADT-template), and four alias instantiations (`IntPage`, `StrPage`, `ItemPage` with user-type arg, `IntStrEnvelope`). All 9 backends produce valid concrete code with NO synthetic `Page<…>`/`Envelope<…>` identifiers — locked decision #4 verified end-to-end.
- [x] **PR-29.10** — Acceptance fixture `m29-ok` cross-language JSON+UEBA roundtrip across all 9 backends. **200/200 cross-language acceptance** after fixing the latent Python ADT JSON codec defect (`[PR-29.10-D01]`). Service-template typer-side path verified via TemplateInstantiatorTest; **end-to-end cross-language service-acceptance for templated service deferred to `[PR-29.10-D07]` follow-up** (would require extending `test/services/petstore.baboon` + 9 per-language service harnesses).
- [x] **PR-29.11** — Docs catch-up: spec finalisation (§2.5.7/8 diagnostic names, §5.3 `TemplateBodyCarriesDerived`, §6 deferred items 10-12, §7.3 worked m29-ok example, §8 PR-origin diagnostic table) + CLAUDE.md M29 pattern bullet (3-site exhaustive-match bundling).

### Negative test matrix (locked)

1. `field: Foo[Bar]` — instantiation in field position.
2. `type Y = Foo[Bar[i32]]` — nested instantiation in alias RHS.
3. `data X[T] { a: lst[T] }; type XInt = X[i32, str]` — arity mismatch.
4. `data X[T, T] { … }` — duplicate type-param name.
5. `data X[T] { rec: X[T] }` — self-reference (forbidden).
6. `field: T` outside a template — placeholder leak.
7. `type Y = X` — template referenced without instantiation.
8. `type Y = i32[X]` — instantiating a non-generic.
9. `type A = X[B]; type B = X[A]` — cycle through aliases.

---

## Cross-cutting architectural notes (locked)

- [x] **M28-N01 / N02 inheritance** — wire-form canonical for tso (`±HH:MM`), Scala u64 JSON map-key (`Long.toUnsignedString` / `BaboonJsonCodec.decodeKeyU64`), Java u64 parity, Kotlin native ULong. Authoritative copies live in archived M1–M28 ledgers.
- [x] **M28-N01 applies to runtime codec too** — `tso = ±HH:MM` always (UTC = `+00:00`, NOT `Z`). PR-28.3 fixed this in the *generated* codecs (Scala/Java/Kotlin/C#/TS) but missed the *runtime* codec used by `BaboonRuntimeCodec` and consumed by `RTCodecTest`. Lands in PR-29P.1 round 2.
- [x] **PR-28.4-D02 status** — REMAINS DEFERRED. Round-1 PR-29P.1 hypothesis was wrong; this defect is a separate latent issue, not the cause of CI-01. Do NOT mark resolved during PR-29P.1.
- [x] **CI sandbox policy for Nix `--ignore-environment`** — DECIDED 2026-05-03 in PR-29P.2: acceptance and `test-*` jobs run under plain `nix develop`; `--ignore-environment` is NOT adopted as default because (1) GitHub-hosted runner injects load-bearing TLS/proxy/cache env vars (`NIX_SSL_CERT_FILE`, `SSL_CERT_FILE`, etc.) that are not reachable through the flake closure, (2) `build-linux` operates without it successfully, (3) the previous adoption (`ca0a354`, Swift backend) was load-bearing only for an unverified Swift-FHS-bwrap isolation hypothesis, and the same Swift artifacts compile under `build-linux` without the flag. Re-introduction to any test-* CI step requires either pinning `NIX_SSL_CERT_FILE`/`SSL_CERT_FILE` into the flake closure or a `--keep` list. Recorded inline in `.github/workflows/baboon-build.yml:161-163` (per `[PR-29P.2-D05]` resolution).

---

## Completed

- **PR-29P.1** (2026-05-03, two rounds) — Fixed `BaboonRuntimeCodec` tso UTC-zero offset normalisation: split single shared formatter (`appendOffset("+HH:MM", "Z")`) into `tsoFormatter` (UTC = `+00:00`) and `tsuFormatter` (UTC = `Z`); widened encode-side parse to `ISO_OFFSET_DATE_TIME` per M28-N01 decoder-tolerance clause; split decode dispatch into per-type arms. Diff: `baboon-compiler/src/main/scala/io/septimalmind/baboon/typer/BaboonRuntimeCodec.scala` (+21/-5), `baboon-compiler/.jvm/src/test/scala/io/septimalmind/baboon/tests/RTCodecTest.scala` (+34 new test). Verification: deterministic regression test fails before fix (`"...8-02-02T13:22:52.339[Z]" did not equal "...8-02-02T13:22:52.339[+00:00]"`) and passes after; close gates `mdl --seq :build :test` (1623.7s, 308/308 RTCodecTest cases green) + `:test-acceptance` + `:test-service-acceptance` (6m 13s) all PASS. Adversarial review (Opus) cleared with 3 minor/nit follow-ups, all deferred per reviewer recommendation. Round-1 (Opus) escalated correctly when the orchestrator's first hypothesis (generator-wide map-iteration sort) didn't match the failing site; round-2 (Opus) reproduced and fixed against the orchestrator's revised hypothesis (tso UTC-zero `+00:00` ↔ `Z` in `BaboonRuntimeCodec`).
  - Surprises: PR-28.3 (commit `b85fc30`) fixed M28-N01 in the *generated* per-language codecs (Scala/Java/Kotlin/C#/TS via `BaboonTools.scala`/`BaboonTimeFormats.java`/etc.) but missed the *compiler-side runtime* codec at `BaboonRuntimeCodec.scala`, which `RTCodecTest` exercises directly. The miss was invisible until `RTCodecTest` ran against the M28-extended m26 fixture (PR-28.4 added more random datetime values).
  - Operational: `mdl find_project_root` (mudyla 0.4.0 `mudyla/utils/project_root.py:26`) requires `.git` to be a directory; in a worktree it is a gitfile, so `mdl` invoked from inside a worktree silently runs sbt in the canonical checkout. Round-2 executor mirrored the two file edits into the canonical checkout so the close gates exercised the actual fix. Worktree branch left as authoritative reference; canonical checkout is byte-identical (reviewer-verified).
  - Constraint future work must respect: any new compiler-side runtime codec touching `tsu`/`tso` MUST use the per-type `tsoFormatter`/`tsuFormatter`, not a single shared formatter. M28-N01 is a wire-format invariant that applies equally to generated codecs, runtime codecs, and (eventually, per `[PR-29P.1-D03]`) fixture/example generators.

- **PR-29P.2** (2026-05-03) — Dropped `--ignore-environment` and four `--keep` flags (`HOME`, `USER`, `CI`, `GITHUB_ACTIONS`) from the two `acceptance-tests` steps in `.github/workflows/baboon-build.yml`. Result: acceptance and service-acceptance steps now run plain `nix develop --command mdl --github-actions :test-acceptance{,-service}`, matching the working `build-linux` reference. Verification: orchestrator captured local env-diff between `nix develop --ignore-environment --command env` and `nix develop --command env` — 95 vars stripped, including session/runner-injected ones; on GitHub-hosted runners these include the TLS-trust chain (`NIX_SSL_CERT_FILE` and/or `SSL_CERT_FILE`) needed by sbt-launcher's bootstrap HTTPS download. Adversarial review (Opus) returned 5 findings (1 minor + 4 nit/minor); all six PR-29P.2-Dxx defect entries land in `defects.md`. Two findings deferred explicitly to PR-29P.3 close-out (D05: in-tree comment; D06: cross-cutting policy decision in session log). Live-CI verification gate runs in PR-29P.3 after both PRs land.
  - Surprises: original commit-history claim (executor → `646543f`) was inaccurate — flag was actually introduced in `ca0a354` (Swift backend, #50, Feb 2026) alongside the bwrap/Swift-FHS workaround. Substantive conclusion is unchanged but the corrected attribution lives in `[PR-29P.2-D01]` and `[PR-29P.2-D02]`.
  - Constraint future work must respect: do NOT re-introduce `--ignore-environment` to any test-* CI step without (i) pinning `NIX_SSL_CERT_FILE` (or equivalent) into the flake closure or (ii) adding it to a `--keep` list. The flag strips runner-injected TLS trust on GitHub-hosted runners and is therefore incompatible with steps that need outbound HTTPS to public Maven/repo endpoints from inside the JVM.

- **PR-29.11** (2026-05-04, single round — clean) — End-of-milestone documentation sweep. Doc-only PR. Six targeted spec edits in `docs/spec/generics.md`: §2.5.7 named `TemplateNotInstantiated` with the exact `DiagnosticsProvider` message; §2.5.8 named `NotATemplate`; §5.3 replaced "to be finalised" placeholder with `TemplateBodyCarriesDerived`; §6 added items 10-12 for the three M29-deferred architectural items (tree-sitter `[PR-29.8-D01]`, cross-namespace `[PR-29.5-D04]`/`[PR-29.7-D07]`, templated-service acceptance `[PR-29.10-D07]`); §7 added subsection 7.3 with the full `m29-ok` fixture verbatim plus its post-monomorphisation typed model (covers `Page[T]`, `Envelope[T, E]`, `Crud[K, V]`, `IntPage`, `StrPage`, `ItemPage`, `IntStrEnvelope`, `IntStrCrud`); §8 replaced "diagnostic strings finalised in PR-29.7" placeholder with the precise PR-origin table for all M29 diagnostics. CLAUDE.md augmented with one bullet (line 37) capturing the M29 3-site exhaustive-match bundling pattern (PR-29.4/29.5/29.7 as canonical examples). No source changes; no test changes; no build/test gates required (doc-only PR).
  - Surprises: §7 worked-example was already concrete pre-PR-29.11 (used `Page[T]`/`IntPage`/`StrPage`); only the new §7.3 m29-ok subsection was additive. §2.5.6 (matrix #6 → `NameNotFound`) and §2.5.9 (matrix #9 → `VerificationIssue.ReferentialCyclesFound`) were already correct from PR-29.6/29.7 inline updates. The "to be finalised" placeholders were narrower than expected.
  - Constraint future work must respect: M29 spec §6 deferred-items list is the canonical surface for what M29 deliberately does NOT cover. PR-29.10-D07 (templated-service end-to-end acceptance) and PR-29.8-D01 (tree-sitter editor grammar) are explicitly tracked as P0 follow-ups requiring separate PRs. Cross-namespace template instantiation (PR-29.5-D04 / PR-29.7-D07) is a more substantial feature gate, not just a polish item.

- **PR-29.10** (2026-05-03 / 2026-05-04, three rounds) — m29-ok cross-language wire-format acceptance. **Headline result: 200/200 cross-language acceptance** after fixing the latent Python ADT JSON codec defect that pre-M29 CI never exercised (m29-ok is the first cross-language fixture to register a field-bearing ADT in the Python harness). Diff: 10 per-backend conv-test harness files registered for m29-ok cross-language roundtrip (`test/conv-test-{cs,sc,py,rs,ts,kt,jv,dt,sw}/` + `test/conv-test-kt-kmp/` + Swift `Package.swift` `ConvtestM29ok` target); Python codegen fix in `baboon-compiler/src/main/scala/io/septimalmind/baboon/translator/python/PyJsonCodecGenerator.scala` (new `fieldHasAdt` / `dtoHasAdtField` helpers, ADT scalar branch in `mkJsonAnyEncoder`/`mkJsonAnyDecoder`); m29.baboon extended with `service Crud[K, V]` + root `IntStrCrud` alias to exercise typer-side service-template substitution. Verification: `mdl :build` PASS, `mdl :test-acceptance` 200/200 (+55 newly-passing rows from D01 fix vs round-1's 145/200), `mdl :test-service-acceptance` 81/81, `sbt baboonJS/compile` PASS (round-3 cross-build canary), `sbt baboonJVM/testOnly *M29*` 12/12. Adversarial review three rounds: round 1 (5 defects D01-D05 + process D06), round 2 (3 follow-ups F1-F4 → defects D07/D08/D09), round 3 (D08 deviation accepted with parser-grammar discovery; D09 cross-build verified PASS). Net resolution: D01/D02/D06/D08/D09 resolved; D03/D04/D05 deferred (moot or polish); D07 explicit follow-up for end-to-end templated-service cross-language acceptance.
  - **Critical discovery (D01):** Pydantic v2's inherited `@model_serializer(mode='wrap')` on `IntStrEnvelope` dispatched using the parent class's field schema (zero fields), not the runtime subclass — yielding `{"Ok":{}}` regardless of the actual variant's field values. The fix delegates ADT-field encoding to the variant's own `_JsonCodec.instance().encode/decode` rather than pydantic's `model_dump_json()`. Affects ALL ADTs with field-bearing variants in Python (latent generator-wide defect; m29 surfaced it in cross-language acceptance because no pre-existing fixture had registered an ADT-with-fields in `test/conv-test-py/compat_main.py`). Pre-M29 CI green status was vacuous for this code path.
  - **Critical discovery (D08):** Baboon services support exactly ONE input arg per shorthand method (`def name (Arg): Return`); multi-arg `def name (A, B): R` is NOT in the parser's `shorthandSig` rule and the typer's `convertMethod` enforces `inargs.size > 1` → `ServiceMultipleInputs` error. The `unit` type does not exist in Baboon. The original D02 brief assumed multi-arg + `unit` were available; investigation refuted both. The Crud service template ships with `def get (K): V; def put (V): K` — semantically asymmetric for a real CRUD shape, but it's the only valid syntax exercising both type params via shorthand.
  - **D07 follow-up (deferred):** End-to-end cross-language service-acceptance for a templated service is NOT met by the 81/81 PASS — that's the petstore.baboon baseline, and petstore.baboon has no template. Adding a templated service to `test/services/petstore.baboon` + updating 9 per-language service harnesses is its own substantial sub-task; deferred to PR-29.10b or a focused follow-up.
  - Constraint future work must respect: any new generator-side fix to a codec path must be cross-build-verified (`sbt baboonJS/compile` minimum, `sbt +compile` ideal — CLAUDE.md historical PR-47/M21). Any new fixture exercising a previously-unexercised generator code path may surface latent defects (PR-29.10's pattern: m29-ok exposed the Python ADT JSON defect because no pre-existing fixture exercised the path). Templated-service end-to-end cross-language acceptance remains a deferred quality gate (PR-29.10-D07).

- **PR-29.9** (2026-05-03, single round — clean) — Cross-language emission smoke fixture. The first PR exercising real codegen on monomorphised templates across all 9 backends. Diff: 1 new fixture file `baboon-compiler/src/test/resources/baboon/m29-ok/m29.baboon` containing: `data Item` (non-template DTO), `data Page[T] { items: lst[T]; total: u32 }` (single-param template), `adt Envelope[T, E] { data Ok { value: T } data Err { error: E } }` (two-param ADT template), and four `: derived[json], derived[ueba]`-annotated `root` aliases (`IntPage = Page[i32]`, `StrPage = Page[str]`, `ItemPage = Page[Item]` user-type arg, `IntStrEnvelope = Envelope[i32, str]`). Verification: full per-backend matrix `mdl --seq :build :test-gen-regular-adt :test-cs-regular :test-scala-regular :test-rust-regular :test-typescript-regular :test-kotlin-regular :test-kotlin-kmp-regular :test-java-regular :test-dart-regular :test-swift-regular` — **all 10 actions PASS**. Generated output samples confirm locked decision #4 end-to-end: C# emits `public sealed record IntPage(IReadOnlyList<Int32> Items, UInt32 Total)` (no `Page<T>` syntax); Scala emits `final case class IntPage(items: List[Int], total: Int)` (no `Page[T]` syntax); Rust emits `pub struct IntPage { pub items: Vec<i32>, pub total: u32 }` (no generics at all). `IntStrEnvelope` becomes a concrete sealed-trait/record/enum hierarchy in each backend with no `Envelope<T, E>` synthetic identifiers anywhere. No existing fixture's generated output changed (wire-format invariant preserved).
  - Surprises: none. The architectural bet from PR-29.5 (codegen requires zero changes; templates excised before codegen sees them) holds end-to-end across all 9 backends. Single-round clean.
  - Constraint future work must respect: PR-29.10's cross-language wire-format acceptance fixture (`:test-acceptance` JSON+UEBA roundtrip) consumes the same `m29-ok/m29.baboon` fixture; the per-backend `conv-test-{cs,sc,py,rs,ts,kt,jv,dt,sw}/` harnesses need to register the fixture for cross-language roundtrip. Wire-format parity for monomorphised templates is the next gate.

- **PR-29.8** (2026-05-03, two rounds — tree-sitter scope reverted) — LSP polish for templates: HoverProvider, DefinitionProvider, CompletionProvider extended to consult `Domain.templateRegistry`. Diff: 3 LSP feature files modified (HoverProvider +114 with `enclosingTemplateName` cursor-context detection for spec §2.3 type-param shadowing, DefinitionProvider +23 with template-registry go-to-def arm, CompletionProvider +40 with `AliasRhsPosition` context surfacing templates), 1 LSP test file extended (LspFeaturesTest.scala +143, 6 new tests), 1 new fixture `baboon-compiler/src/test/resources/baboon/m29-lsp/m29-lsp.baboon` (template + alias-of-template + non-template + top-level-`T`-shadowing-by-template-`T` scenarios). Test count: 9 → 15 in `*LspFeatures*`; 372 → 378 in full JVM suite. Verification: `sbt baboonJVM/compile` PASS, `baboonJS/compile` PASS, `'testOnly *LspFeatures*'` 15/15, `baboonJVM/test` 378/378.
  - **Tree-sitter editor grammar reverted from PR-29.8 working tree per `[PR-29.8-D01]`.** The grammar.js + corpus tests + regenerated parser.c live in a 3-level submodule chain (outer `baboon` → `editors/baboon-zed` → `grammars/baboon`); shipping requires submodule pointer bumps in each, with cross-repo blast radius warranting user authorisation per CLAUDE.md. Defer to a dedicated tree-sitter PR. The LSP-side polish (the higher-value M29 deliverable) ships independently here.
  - Surprises: round-1 reviewer caught (a) the tree-sitter submodule-chain non-shipment, (b) the executor's false claim that LSP test infrastructure doesn't exist (`LspFeaturesTestBase` is 296 lines and runs hover/definition/completion against a real compiled family), (c) HoverProvider's lookup order didn't honour spec §2.3 type-param shadowing (top-level `data T {...}` would mask the type-param `T` inside `data Page[T] {...}`'s body). All three are real defects; D04 fixed via cursor-position-based enclosing-template detection (option (a) from defect brief).
  - Constraint future work must respect: tree-sitter editor support for the new template syntax requires a dedicated PR with user-authorised submodule pointer bumps. The grammar.js diff is conceptually small (`optional($.type_params)` added to `data_def`/`adt_def`/`contract_def`/`service_def`; `optional($.derivations)` added to `type_alias_def`; `commaSep1($.identifier)` for the type-param list) plus regenerated parser artefacts and 6 corpus tests. LSP correctness today does NOT depend on the tree-sitter side — the LSP runs against the FastParse compiler-side parser, not the editor-side tree-sitter. CompletionProvider's qualified-prefix regex (`type Y = pkg.Pa`) and `[PR-29.8-D06]` cross-namespace template completion are both gated on the same cross-namespace template work tracked at `[PR-29.5-D04]` / `[PR-29.7-D07]`.

- **PR-29.7** (2026-05-03, two rounds) — Validator: forbidden positions; matrix-#7/#8/D03 diagnostics. Three new `TyperIssue` cases (`TemplateNotInstantiated` matrix #7, `NotATemplate` matrix #8, `TemplateBodyCarriesDerived` for `[PR-29.5-D03]`); `TemplateArityMismatch.ownerName` renamed to `aliasName` per `[PR-29.5-D09]`. Diff: 6 modified files (`TyperIssue.scala` +3 cases +3 printers, `TemplateInstantiator.scala` extended with matrix #2 arg-check + matrix #7/#8 detection in `processMember` + `assert(raw.derived.isEmpty)` replacing PR-29.5 TODO + removed `registry.isEmpty` short-circuit guard so the pass runs for template-free models, `TemplateRegistryBuilder.scala` added `validateNoDerived` helper for Dto+Adt path, 3 LSP/JS exhaustive-match sites with all 3 new cases bundled per file). 1 new test file `M29ValidatorTest.scala` (11 tests after round-2 fixes: 3 matrix #7, 3 matrix #8, 3 D03 incl. negatives, 1 matrix #2 nested in args, 1 builtin regression). Per-matrix decision table:
    - #1 (in-body, self-ref) → `TemplateInstantiationInBody` (PR-29.5).
    - #2 (nested in alias args) → reuse `TemplateInstantiationInBody` (name is a misnomer for this case; deferred rename in `[PR-29.7-D05]`).
    - #3 (arity) → `TemplateArityMismatch` (PR-29.5).
    - #4 (dup type-param) → `DuplicateTypeParam` (PR-29.4).
    - #5 (self-ref) → special case of #1.
    - #6 (orphan T) → existing `NameNotFound` (CLAUDE.md §4 simplicity).
    - #7 (template w/o instantiation) → NEW `TemplateNotInstantiated`.
    - #8 (instantiating non-template) → NEW `NotATemplate`.
    - #9 (cycle) → `VerificationIssue.ReferentialCyclesFound` (PR-29.6).
    - D03 body-side `derived` → NEW `TemplateBodyCarriesDerived`. Path (a) chosen: validation in `TemplateRegistryBuilder` at registry-build time; merge site in `TemplateInstantiator` becomes `assert(raw.derived.isEmpty)` (defensive; templates with body-derived fail before reaching the instantiator).
   Verification: `sbt baboonJVM/compile` PASS, `baboonJS/compile` PASS (the **CRITICAL JS cross-build canary** — 3 new TyperIssue cases × 3 sites = 9 case adds; clean), `'testOnly *M29Validator*'` 11/11, `baboonJVM/test` 372/372 (no regressions; 361 baseline + 1 cycle from PR-29.6 + 8 new = 372 — wait, recount: 361 baseline at start of session-2 → 369 after PR-29.7 round 1 → 372 after round-2 fixes). `+compile` cross-build PASS. Adversarial review (Opus) round 1: 0 majors + 4 minors + 3 nits; D01-D04 fixed inline; D05/D06/D07 deferred with rationale. Round 2 implicit (test-only fix surface; orchestrator verified by spot-check + executor's reported test count + assertion shape).
  - Surprises: round-1 reviewer caught that the `M29ValidatorTest` was missing precision-pinning negatives (template without body derived succeeds; non-template alias `type Y = X` succeeds; `MyDto[i32]` fires `NotATemplate`). Without these, a regression that broadly fired the new diagnostics would not be caught by the test file. Fix subagent's deviation from the brief was correct: a successful pipeline run produces `Right(Domain)` with no issue list to introspect, so `assert(outcome.isRight)` is the right operational test for "no issue fired." Discovery: `type Y = X` (non-template alias to a real DTO) does not produce a separate `TypeId.User` for `Y` in `domain.defs.meta.nodes`; `Y` is a structural alias that's resolved transparently. Test assertions adjusted accordingly.
  - Constraint future work must respect: PR-29.7 is the locked landing for matrix-validation diagnostics. Future polish: `[PR-29.7-D05]` (rename `TemplateInstantiationInBody` to broader name + update printer text to cover both in-body and arg-position); `[PR-29.7-D06]` (reference shared builtin-collection list rather than hardcoded `Set("lst",…)`); `[PR-29.7-D07]` (cross-namespace template instantiation detection). All deferred — they don't block M29 acceptance fixture (PR-29.10) or codegen smoke (PR-29.9). The full close gate `mdl --seq :build :test` is still recommended once before M29 branch merge per `[PR-29.5-D08]` precedent.

- **PR-29.6** (2026-05-03, two rounds — plan revised mid-PR) — Cycle diagnostic verification + spec note. **Plan revision**: orchestrator's first hypothesis was that the existing typer toposort `BaboonTyper.order` catches matrix #9 (`type A = X[B]; type B = X[A]`) via `CircularInheritance` after PR-29.5's substitution produces `data A { f: B }; data B { f: A }`. Round-1 executor (Sonnet) escalated correctly: `BaboonEnquiries.hardDepsOfRawDefn` only walks inheritance/parent edges (ADT `+`/`-`/`^` arms, contract refs), NOT field-type edges (`baboon-compiler/src/main/scala/io/septimalmind/baboon/typer/BaboonEnquiries.scala:242-246`). Matrix #9 produces two field-referencing DTOs that are **not** an inheritance cycle, so the typer toposort doesn't fire. The cycle IS caught — by `BaboonValidator.checkLoops` (post-typer) as `VerificationIssue.ReferentialCyclesFound`, in `validator/BaboonValidator.scala:85`. Round-2 dispatched with revised plan: lock the validator-side behaviour via test + update spec §2.5.9. Diff: new test file `M29TemplateCycleValidatorTest.scala` (1 test, mirrors `AnyValidatorTest` full-pipeline pattern), spec §2.5.9 updated to document the disposition (CLAUDE.md §4 simplicity — reuse existing diagnostic rather than introducing `TemplateInstantiationCycle`). Verification: `sbt baboonJVM/compile` PASS, `baboonJS/compile` PASS, `'testOnly M29TemplateCycleValidatorTest'` 1/1, `baboonJVM/test` 361/361 (no regressions, +1 from 360).
  - Surprises: typer toposort scope narrower than the orchestrator initially understood. Inheritance-only edge set (`hardDepsOfRawDefn`) is intentional — it's used for ADT/contract/inheritance ordering, not generalised dependency analysis. Field-reference cycles are validator concerns. Round-1 executor's escalation prevented shipping a wrong test that would have asserted a diagnostic that never fires for this case.
  - Constraint future work must respect: M29 cycle detection for matrix #9 lives in `BaboonValidator.checkLoops` (validator stage), NOT in the typer's monomorphisation or toposort. Diagnostic name is `VerificationIssue.ReferentialCyclesFound` — same as for hand-written non-template cycles. Spec §2.5.9 documents this. Future cycle-related diagnostic upgrades (e.g. user-friendlier message for alias-cycles vs. plain-DTO cycles) live in the validator, not the typer.

- **PR-29.5** (2026-05-03, two rounds) — Typer monomorphisation via AST substitution; the M29 architectural meat. New `TemplateInstantiator` pass wires between `adtInheritanceExpander.expand` (`BaboonTyper.scala:414`) and the second `builder.buildScopes` (L427), inserted at L420. For each `RawTLDef.Alias` whose `target` is `RawTypeRef.Constructor(name, args, prefix)` resolving to a registered template: substitute the cached raw body (replace `RawTypeRef.Simple(T)` placeholders with resolved args, recurse into `Constructor.params` and `AnyRef.underlying`); synthesize a concrete `RawTLDef.{DTO|ADT|Contract|Service}` keyed by the alias's name (locked decision #4) with merged `derived` set (alias's `derived` per spec §5 / decision #6); replace the alias in the member list. Diff: 2 new files (`typer/TemplateInstantiator.scala`, `tests/TemplateInstantiatorTest.scala` — 17 tests after round-2 fixes), 7 modified files (`BaboonTyper.scala` pipeline wiring, `BaboonModule.scala` distage binding, `TyperIssue.scala` two new cases + IssuePrinters, 3 LSP/JS exhaustive-match sites, `RawDto.scala` drop of `= Nil` defaults per `[PR-29.2-D05]`, `ScopeBuilder.scala:126` updated to pass `typeParams = Nil` explicitly). New `TyperIssue` cases: `TemplateArityMismatch` (matrix #3) and `TemplateInstantiationInBody` (covers spec §4 / matrix #1 — both direct `X[T] { rec: X[T] }` AND container-mediated `Tree[T] { children: lst[Tree[T]] }`); naming chosen per PR-29.1a spec reframing — no separate `TemplateSelfReference`. Verification: `sbt baboonJVM/compile` PASS, `baboonJS/compile` PASS (the **CRITICAL JS cross-build canary** clean for two new TyperIssue cases × 3 sites = 6 case adds), `'testOnly *TemplateInstantiator*'` 17/17, `'testOnly *Template*'` 38/38, `sbt baboonJVM/test` 360/360 (no fixture md5 churn — emission tests included; 339 baseline + 4 PR-29.4 D02 + 17 new = 360). `sbt +compile` cross-build PASS. `mdl --seq :build :test` skipped per `[PR-29.5-D08]` resolved-deferred (per `[PR-29.4-D06]` precedent; full close-gate recommended once before M29 branch merge, not gated per PR). Adversarial review (Opus) round 1: 2 majors + 6 minors + 2 nits; round 2 clean. Fixes applied inline: D01 (ADT/Contract/Service substitution test coverage), D02 (`any[T]` substitution test), D03 (TODO PR-29.7 comments at body-side `derived` merge sites), D05 (alias-chain test strengthened with third assertion), D07 (namespaced template + alias test). Deferred: D04 (in-body matcher namespaced flag → PR-29.7), D06 (`Domain.templateRegistry` write-only → PR-29.8/PR-29.11), D08 (full close gate → M29 branch merge), D09 (`ownerName` field name → PR-29.7), D10 (case-arm formatting → `mdl :fmt`).
  - Surprises: round-1 reviewer caught test-coverage gaps for ADT, Contract, Service templates (only DTO covered); the substitution code paths existed but were unverified. Fix subagent verified real Baboon syntax for each declaration kind (`adt Result[T,E] { data Ok {...} data Err {...} }`, `contract Acked[T] { ... }`, `service { def query (Q): R }`) against actual fixtures and parser sources rather than inventing syntax. Discovery: plain aliases land in `TyperOutput.aliases` (then `Domain.aliases`), NOT in `domain.defs.meta.nodes` like materialised template-instantiations — important for PR-29.6+ test assertions. Discovery: `root` keyword required on a namespaced alias for the materialised def to survive the dependency-graph closure (root-reachable filter).
  - Constraint future work must respect: PR-29.6 (typer dependency ordering) consumes the post-substitution member list — no template references survive into the toposort, so `CircularInheritance` will fire for matrix #9 (`type A = X[B]; type B = X[A]`) with the existing diagnostic; PR-29.7 may rename to `CircularAliasInstantiation`. PR-29.7 (validator) owns the namespaced-template in-body matcher tightening (`[PR-29.5-D04]`), the body-side `derived` validator-side error (`[PR-29.5-D03]` TODO), and matrix #6/#7/#8 diagnostics. PR-29.8/PR-29.11 may consume `Domain.templateRegistry` for hover/completion or drop it as dead state.

- **PR-29.1a** (2026-05-03, two rounds) — Spec doc tightening per user clarification. §4 reworked from a misleading Option A/B "container-mediated self-reference" callout into §4.1-§4.4 reframing: matrix #5 (`X[T] { rec: X[T] }`) and the container-mediated form (`Tree[T] { children: lst[Tree[T]] }`) are both special cases of matrix #1 / locked decision #3 (field-position template instantiation forbidden) — not a separate self-reference rule. Hand-written non-template recursive types remain valid via `BaboonValidator.checkLoops` (L69-87) and `terminatesLoop` (L89-111); two existing fixtures cited with mechanism-accurate descriptions: `RecTest1` (`pkg03.baboon:53-56`) — ADT-branch alternative termination; `RecTest2` (`pkg03.baboon:59-61`) — option-mediated termination. New §6 item: typer does NOT structurally rewrite in-body instantiations to match existing aliases (e.g. `type K = Tree[i32]` does not rescue `data Tree[T] { f: lst[Tree[T]] }`); rationale: order-dependence, structural-scan cost, fragile non-local constraint. §6 also relocated former items 5-6 (matrix #1/#2 — "out of scope" wording was misleading because they're actively forbidden, not deferred); rule now lives only in §2.5.1/§2.5.2 worked counter-examples. Adversarial review (Opus) round 1: 6 minors + 1 nit, all fixed inline by single-subagent round 2. Verification: `grep` for dropped phrases (`Option A`/`Option B`/`strict reading`/`permissive reading`/`DESIGN NOTE`/`container-mediated self-reference`/`RawTypeRef.Constructor`/`L69-89`) all return no matches; cross-reference walk clean across §1, §2.5.5, §4.1-4.4, §6. Doc-only PR — no source changes.
  - Surprises: round-1 reviewer caught that the cited `BaboonValidator` line range elided the actual function being named (`L69-89` covers `checkLoops` + only the signature line of `terminatesLoop`); also caught that `RecTest1`'s mechanism is ADT-branch alternative termination via `exists`, NOT container-mediated as the §4.4 prose initially framed. Both verified against `BaboonValidator.scala:69-111` and `pkg03.baboon:53-61`.
  - Constraint future work must respect: spec §4 now states the rule once (decision #3 / matrix #1 covers all field-position template instantiation, including in-body forms). PR-29.5/PR-29.7 implementation honours this: the substitution pass and the validator must reject any `RawTypeRef.Constructor`-over-template in field position, no special-case "self-reference" handling needed. The auto-rewrite escape hatch (`type K = Tree[i32]` rewriting in-body `Tree[T]`) is permanently out of scope per §6.

- **PR-29.4** (2026-05-03) — Typer template registry; first M29 `TyperIssue` (`DuplicateTypeParam`) with 3-site exhaustive-match update. **Critical milestone deliverable**: this PR establishes that templates parse and are acknowledged but DO NOT become `DomainMember`s — they live in a typer-side registry consumed downstream by PR-29.5's monomorphisation pass. Diff: 3 new files (`typer/model/TemplateRegistry.scala` — `TemplateRegistry`, `TemplateBody`, `RawTemplateDefn` ADT; `typer/TemplateRegistryBuilder.scala` — `Impl[F]: Error2` mirroring `AdtInheritanceExpander`; `tests/TemplateRegistryBuilderTest.scala` — 8 tests after round-2 fixes), 6 modified files (`TyperIssue.scala` + IssuePrinter, `BaboonTyper.scala` pipeline wiring, `Domain.scala` adds `templateRegistry` field, `BaboonModule.scala` distage binding, 3 LSP/JS exhaustive-match sites). Registry key shape: `(Pkg, Owner, TypeName)` matching `TypeId.User`'s components — `Owner` is `Toplevel` or `Ns(path)`. Pass placement: BEFORE `adtInheritanceExpander.expand` in `runTyper`, so subsequent passes never see template members. Verification: `sbt baboonJVM/compile` PASS, `baboonJS/compile` PASS (the **CRITICAL JS cross-build canary** per CLAUDE.md PR-47/M21 historical regression — clean, no exhaustive-match warnings), `'testOnly *Template*'` 26/26 (4 baseline + 4 round-2 D02 tests + 18 PR-29.2 TemplateHead tests), `sbt baboonJVM/test` 339/339 (no fixture md5 churn — emission tests included), `sbt +compile` cross-build PASS. `mdl --seq :build :test` skipped per `[PR-29.4-D06]` — orchestrator + reviewer agreed per-target gates suffice for this PR's typer-early-excision surface. Adversarial review (Opus) round 1: 1 major (registry dropped at process boundary, fixed inline as `[PR-29.4-D01]`) + 2 minors (`[PR-29.4-D02]` test gaps fixed inline, `[PR-29.4-D03]` first-only-duplicate emission deferred) + 3 nits (deferred).
  - Surprises: round-1 reviewer caught that `TyperOutput.templateRegistry` was built but DROPPED at the `process` boundary because `Domain` did not carry a `templateRegistry` field. PR-29.5 would have had nothing to consume — this would have been a load-bearing dead computation. Fixed by adding `templateRegistry: TemplateRegistry = TemplateRegistry.empty` to `Domain` (last position, default for source-compat). Round-2 fix subagent additionally caught that the brief said `namespace foo {…}` but the actual Baboon keyword is `ns` per `Keywords.scala` — corrected the test fixture syntax. Test (a) for nested-namespace excision required adding a non-template anchor inside the `ns foo` block because a namespace becomes structurally empty after template-only excision and `ScopeBuilder` rejects with `ScopeCannotBeEmpty`.
  - Constraint future work must respect: PR-29.5 consumes `Domain.templateRegistry`. Default `TemplateRegistry.empty` is intentional now to preserve source-compat; once PR-29.5 lands the consumer, dropping the default is optional cleanup. The 3-site exhaustive-match discipline (`DiagnosticsProvider.scala`, `WorkspaceState.scala`, `BaboonJS.scala`) is now established as the M29 precedent — every subsequent `TyperIssue` add MUST update all three or break the cross-build.

- **PR-29.3** (2026-05-03) — Parser: alias instantiation RHS + alias-side `derived` extension. Two contracts locked: (a) `type Y = X[Foo]` already produces `RawAlias(target = RawTypeRef.Constructor(...))` via the existing `typeRef` rule (verification only — no parser change required for the X[Foo] shape); (b) NEW: `RawAlias` extended with `derived: Set[RawMemberMeta] = Set.empty` field (penultimate position before `meta`, matching `RawDto`/`RawAdt` convention) + `DefModel.scala` alias rule extended to consume optional `: derived[…], derived[…]` clause via the existing `meta.derived` rule (reused, not duplicated). Diff: `RawDto.scala` (+1/-1, field add), `DefModel.scala` (+3/-3, rule extension + positional call site update), new test file `AliasInstantiationParserTest.scala` (+9 tests covering positive `X[Foo]` parse with prefix/multi-arg, positive `: derived[…]` clause with multi-derivation, backwards-compat for both, documents `Foo[Bar[i32]]` parse-acceptance for PR-29.7 validator-side rejection). Verification: `sbt baboonJVM/compile` PASS, `baboonJS/compile` PASS (cross-build clean), `'testOnly *AliasInstantiation*'` 9/9, `'testOnly *Parser*'` 84/84, full `baboonJVM/test` 335/335 (no regression vs PR-29.2 baseline of 323 + 12 new tests this session), `+compile` cross-build PASS. Adversarial review (Opus) found 1 major (process — scope expansion beyond plan brief, orchestrator-deliberate; documented as `[PR-29.3-D01]`) + 1 minor (`derivations` → `derived` rename for sibling-type convention alignment, fixed inline) + 1 nit (`Set` discards source-order for codec annotations; deferred — matches sibling-type convention; future-codegen-determinism risk only).
  - Surprises: scope deliberately expanded beyond plan brief — plan said PR-29.3 was test-only verification, but `[PR-29.1-D02]` (spec ships `RawAlias`-with-`derived` syntax that the parser cannot consume) made the alias-side `derived` extension a load-bearing closure of a spec-vs-parser gap. Bundling into PR-29.3 (the parser-side alias PR) keeps parser changes co-located. Documented as `[PR-29.3-D01]`.
  - Constraint future work must respect: PR-29.4 (typer template registry) and PR-29.5 (typer monomorphisation) consume `RawAlias.derived` for propagation per spec §5 / locked decision #6. Default `Set.empty` is intentional now to preserve source-compat. Matrix #2 (`type Y = Foo[Bar[i32]]` nested instantiation) parses successfully today; PR-29.7 owns the validator-side rejection.

- **PR-29.2** (2026-05-03) — Parser: optional `[T1, T2, …]` type-param head on `data` / `adt` / `contract` / `service` declarations; `id Foo[T] { … }` rejected at parse time per spec §6.7. Diff: 5 modified parser files (+29 lines), 1 new test file `TemplateHeadParserTest.scala` (18 tests). AST changes: added `typeParams: List[RawTypeName] = Nil` to `RawDto`/`RawAdt`/`RawContract`/`RawService` (last position, default preserves source-compat for the lone non-parser positional call site at `ScopeBuilder.scala:126`). Parser change: new `templateHead[$: P]` rule in `DefDto.scala` parsing `[ idt.symbol ("," idt.symbol)* ]` (min 1 param; uses `idt.symbol` not `typeRef`); wired into `dtoEnclosed` / `adtEnclosed` / `contractEnclosed` / `service` between name and body. Identifiers (`identifierEnclosed`) intentionally left unchanged — `[T]` before `{` is unconsumed → clean parse failure. Verification: `sbt baboonJVM/compile`, `sbt baboonJS/compile`, `sbt 'baboonJVM/testOnly *TemplateHead*'` (18/18 after round-2 fixes), `sbt 'baboonJVM/testOnly *Parser*'` (72/72), `sbt baboonJVM/test` (323/323), `sbt +compile` cross-build PASS. Adversarial review (Opus) found 2 minors + 3 nits round 1; D01/D02 (test coverage gaps) fixed inline (see PR-29.2 section in `defects.md`); D03/D04/D05 (nit/process flags) deferred with rationale to PR-29.3+ / PR-29.7.
  - Surprises: round-1 reviewer caught two test-coverage gaps the executor missed: (D01) no negative test for non-bare-identifier type-param like `data X[lst[i32]]`, leaving the load-bearing `idt.symbol`-vs-`typeRef` distinction unguarded; (D02) all positive tests used uppercase, no test demonstrating spec §2.2's "casing is convention only" rule. Both fixed by single-subagent round-2 fix pass (3 new tests). The original `assertDtoFails("data X[]")` and `assertIdentifierFails("id Foo[T] { … }")` cases were both present but not enough.
  - Constraint future work must respect: PR-29.3 (alias instantiation RHS) and PR-29.4 (typer template registry) consume `RawDto.typeParams` etc. The default `= Nil` is intentional now to preserve source-compat — once PR-29.4/PR-29.5 lands typer plumbing, drop the default per `[PR-29.2-D05]` and update `ScopeBuilder.scala:126` to pass `Nil` explicitly.

- **PR-29.1** (2026-05-03, two rounds) — User-facing spec doc `docs/spec/generics.md` (658 lines) for BAB-A04 user-defined generics. Doc-only PR — no source changes. Covers: surface syntax for `data X[T]`, `adt X[T]`, `contract X[T]`, `service X[T]` template declarations + `type Y = X[Foo]` alias-only instantiation; semantics of monomorphisation under decision #4 (alias id is canonical); §2.5 worked counter-examples for all 9 negative-test matrix items in matrix order; §4 self-reference treatment defaulting to **Option A (strict)** — container-mediated recursion within a template body is forbidden; §5 codec-annotation propagation rules including a flagged `RawAlias` parser extension required by PR-29.2/PR-29.3; §6 explicit out-of-scope list. Adversarial review (Opus) round 1 found 14 defects (5 major + 6 minor + 3 nit) — most major findings were spec syntax inaccuracies (`derived` chained-colon form, non-existent `=` form for ADT/contract templates, `;` field separator). Round-2 fix subagent applied all 14 fixes; round-2 review cleared with `clean — no findings, all 14 round-1 defects resolved correctly with no regressions`. Verification commands: `grep -n 'derived\['` (no chained-colon residue), `grep -nE '(adt|contract).*='` in code blocks (no `=`-form residue); cross-checked all 9 matrix items against §2.5 by reviewer.
  - Surprises: spec author's first round invented several syntax forms that don't match real Baboon grammar — the `derived` chained-colon (`: derived[json] : derived[ueba]`) and the `adt … =` / `contract … =` forms in particular. Round-1 reviewer caught all of them by spot-checking against `pkg0/*.baboon` fixtures and the `DefMeta.scala` / `DefAdt.scala` / `DefContract.scala` parser sources. Operational lesson: spec authors must verify surface syntax by reading actual fixtures, not from memory or analogy with adjacent languages.
  - Constraint future work must respect: §4 chose Option A (strict — container-mediated recursion within a template body is forbidden, e.g. `data Tree[T] { children: lst[Tree[T]] }` is rejected). This is the literal reading of locked decision #2. The §4 design-note callout flags it as user-revisitable; user may switch to Option B before PR-29.7 lands. PR-29.5 (monomorphisation) and PR-29.7 (validator) MUST honour Option A unless this changes.
  - Constraint: PR-29.2/PR-29.3 must extend `RawAlias` to carry an optional `derived` set per the §5 intro — pre-M29 aliases have no `derived` field, so this is a parser-side schema change, not just a grammar addition.

- **PR-29P.4** (2026-05-03) — M29-prep follow-up cleanup; bundled the two PR-29P.1 deferred items into one surgical PR. Diff: `baboon-compiler/.jvm/src/test/scala/io/septimalmind/baboon/tests/RTCodecTest.scala` (+51, two new deterministic non-UTC `tso` regression cases at offsets `+05:30` and `-08:00`); `baboon-compiler/src/main/scala/io/septimalmind/baboon/explore/RandomJsonGenerator.scala` (+18/-9, split shared `appendOffset("+HH:MM", "Z")` formatter into `tsoFormatter` (`+00:00`) + `tsuFormatter` (`Z`), dispatch `if (id == \`tso\`) tsoFormatter else tsuFormatter` mirroring `BaboonRuntimeCodec` shape). Verification: `sbt baboonJVM/testOnly io.septimalmind.baboon.tests.RTCodecTest` PASS (6/6 tests, 3 cancelled — pre-existing skips that need full mdl pipeline); `sbt baboonJS/compile` PASS (48s, 83 pre-existing warnings, none from changed files). Adversarial review (Opus) cleared with three nits, no defects requiring fix (see PR-29P.4 section in `defects.md`).
  - Surprise: PR-29P.1's regression test was honestly framed as "fail-first against the original UTC-zero bug"; the new non-UTC cases are forward-looking guards rather than fail-first reproductions, because the original bug only manifested at offset==0. Documented in the inline test comment and in `[PR-29P.1-D02]` Fix block.
  - Constraint future work must respect: M28-N01 invariant now hard-guarded across three layers — generated per-language codecs (PR-28.3), compiler-side runtime codec (PR-29P.1), and example generator (PR-29P.4). Any new code touching `tsu`/`tso` MUST use per-type formatters; never a single shared formatter.
